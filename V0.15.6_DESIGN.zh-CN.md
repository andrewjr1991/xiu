# Xiu v0.15.6 设计：托管搜索设备只读可观测性

## 目标

在不增加浏览器自动化、不暴露设备密钥、不改变模型 Provider 代理的前提下，为托管搜索提供服务端设备列表、当前设备状态和撤销审计的只读诊断能力。客户端仍只保存必要的设备凭证引用，服务端负责鉴权、脱敏和审计事实。

## 范围

- 新增只读服务端接口契约：当前设备详情、设备列表和撤销事件摘要。
- 客户端新增显式诊断命令（暂定 `/web devices`），默认不执行网络请求；只有用户明确调用时才访问服务端。
- 使用现有设备凭证换取短期 Bearer token；不把 `deviceSecret`、访问 token 或完整 URL 查询参数写入输出、日志或审计记录。
- 展示稳定设备 ID 的末尾脱敏片段、状态、创建/最近使用时间、撤销时间（如有）、服务端 request ID 和有限长度的撤销审计摘要。
- 设备已撤销、权限不足、接口不可用、响应格式异常和 TLS/代理错误均失败关闭，不触发自动重新注册、不清理本地凭证、不重试写操作。

## 非目标

- 不在客户端实现设备创建、删除、撤销或恢复。
- 不提供批量管理、设备密钥轮换、服务端管理后台或浏览器登录流程。
- 不上传本地审计记录，不改变现有 `/web status`、`/web doctor`、`/web reset` 语义。

## 安全与数据边界

- 所有新增请求仅允许 HTTPS、固定相对路径和 `GET`；禁止 URL 凭证、跨域重定向和任意 endpoint 拼接。
- 服务端响应按严格 schema 校验，数组长度、字符串长度、时间格式和事件数量均有上限。
- “撤销”仅作为服务端事实展示；客户端不能把列表接口当作授权结果，也不能据此绕过工作区信任、风险审批、Plan 只读或危险操作确认。
- 本地安全审计只记录命令类别、结果、脱敏设备标识和有限 request ID，不记录响应正文、token、密钥、完整路径或模型内容。

## 验收

- 正常返回、分页/数量上限、空列表、已撤销设备和部分字段缺失均有确定性输出。
- 401/403、429、5xx、超时、取消、TLS、代理和 malformed JSON 均按失败关闭处理，且不产生注册、撤销或凭证清理副作用。
- Plan 模式和未信任工作区不会加载服务端设备列表；显式命令在已信任工作区仍需用户主动触发。
- 完成类型检查、全量测试、构建、包清单检查，并验证 npm 包不包含 `.xiu/` 或任何凭证材料。
