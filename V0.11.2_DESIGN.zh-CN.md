# Xiu v0.11.2：可解释的任务阶段模型路由

## 目标

在不改变 Xiu 安全边界的前提下，让主 Agent 可以为规划、实现和验证三个任务阶段使用用户明确指定的 Provider/模型，并让每次切换、跳过和恢复都可见、可诊断、可关闭。

## 配置模型

路由配置保存在本机 `~/.xiu/providers.json`：

- `enabled` 控制总开关。
- `planning`、`implementation`、`verification` 分别保存一个 Provider Profile ID。
- 每个 Profile 使用其最近选择的模型；模型、上下文窗口与能力探测继续由现有 Provider Registry 管理。
- 未配置的阶段跟随本次任务开始时用户手动选择的 Provider/模型。

终端命令：

```text
/routing
/routing on
/routing off
/routing set planning
/routing set implementation
/routing set verification
/routing clear planning
/routing clear implementation
/routing clear verification
```

## 阶段判定

- 第一次模型请求和只读 Plan 模式属于 `planning`。
- 正式计划中正在执行的验证、测试、检查或构建步骤，以及完成门禁要求补充验证后的请求属于 `verification`。
- 其余请求属于 `implementation`。

阶段判定是程序级、确定性的，不依赖模型声称自己正在做什么。

## 安全边界

- 只在一次新模型请求开始前切换，不在已输出流式文字后切换。
- 目标必须满足当前请求的工具能力和安全上下文预算；否则保留当前模型并显示原因。
- 路由只替换模型运行时，不改变工作区信任、审批、Plan 模式只读限制、恢复点、工具白名单或危险操作确认。
- 图片和视频生成不参与阶段路由，继续使用持久化幂等与恢复机制，避免跨 Provider 重复计费。
- 任务结束后恢复用户开始任务时手动选择的 Provider/模型，不写回默认选择。
- 路由目标发生瞬时故障时，仍由既有有界重试和主 Provider 备用链处理；不会重放已经完成的工具副作用。

## 可观测性

- 成功切换在终端留下永久回执，说明阶段、来源、目标与配置原因。
- 因能力或上下文跳过时显示具体原因。
- 会话 JSONL 记录 `provider_route` 事件。
- `/diagnostics` 不仅汇总切换与跳过，还记录每次模型请求实际所属阶段；即使规划阶段恰好使用当前模型、没有发生切换，也会显示规划调用次数与“首轮分析”依据。
- 验证失败后返回实现、完成门禁再次进入验证等阶段变化会保留原因，避免把合理返工误读为无意义抖动。
- Token 显示明确标注为所有模型请求的累计输入/输出量；同一段上下文在多次请求中会重复计入，并同时显示平均每次输入，避免与当前上下文占用混淆。

## 真实任务集中修复

- Windows 下 `run_process` 调用 `npm`/`npx` 时优先通过 Node 附带的 CLI 脚本启动，避免 `.cmd` 被 `execFile` 直接执行时出现 `EINVAL`。
- `validate_project` 是 package.json 中 typecheck、lint、test、build 的首选入口；系统提示提供正确参数示例，并禁止把 `cmd`、`/c`、`/p` 或第二个程序名塞进参数数组。
- `write_file`、精确编辑、直接进程和项目验证提供窄范围的“本次会话始终允许”选项；dangerous 操作仍不能继承该授权。
- 审批诊断区分实际交互提示、`--yes` 自动放行、会话规则放行和内部策略检查，不再把自动决策误报成人工审批。
- 已完成但中途发生并恢复的工具失败会显示“可恢复失败”，并报告工具成功率；最终成功证据和过程质量不再混为一谈。

## v0.11.2 范围限制

- 本版本只对主 Agent 启用任务阶段路由；并行子 Agent 继续使用任务启动时配置和 v0.11 的故障转移。
- 本版本不根据未知的价格或质量数据自动猜测模型优劣。速度、质量、成本策略由用户通过阶段绑定明确表达。
- Prompt Cache、请求缓存和重复调用合并留给后续版本。

## 自动化验收

- 路由策略能持久化，删除 Provider 后不会留下悬空引用。
- 规划阶段可切换到指定 Provider，任务结束后恢复原 Provider。
- 目标上下文或工具能力不满足时不会切换，并留下跳过诊断。
- 无切换的首轮规划也会出现在阶段调用和阶段变化诊断中。
- Windows `npm --version` 可由 `run_process` 成功启动，不出现 `EINVAL`；审批统计能区分提示、自动和记忆决策。
- 现有故障转移、媒体幂等、安全审批、Plan 模式和会话恢复测试继续全部通过。
