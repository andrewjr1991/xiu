# Xiu v0.10.2 实时模型状态显示修复

## 问题

启动欢迎卡片会进入终端滚动历史，Provider 或模型切换后无法安全回写。旧标题使用“当前会话”，会让启动时模型看起来仍是当前模型。

## 方案

- 欢迎卡片明确标为“启动时配置 · 实时状态见底栏”，不再把历史输出伪装成实时状态。
- 输入框下方的实时状态栏同时显示 `Provider ID/模型 ID`，切换成功后立即更新。
- 保留切换成功回执，不清屏、不删除终端历史，也不引入全屏 TUI 对右键粘贴和 PowerShell 兼容性的影响。

## 验收

- [x] 启动卡片明确说明其值是启动时快照。
- [x] Provider 或模型切换后，底部状态栏立即显示新的 Provider 与模型。
- [x] 窄终端仍按原规则裁剪，不发生物理换行。
- [x] 类型检查、全部测试和构建通过。
