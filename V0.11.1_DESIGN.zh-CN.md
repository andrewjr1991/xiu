# Xiu v0.11.1 设计：媒体生成可靠性验收与恢复闭环

## 一、目标

让用户在不知道原始提示词、参数和模型上下文的情况下，也能通过稳定请求 ID 找回已有媒体任务，并明确区分“安全恢复”和“可能再次计费的新生成”。

## 二、非目标

- 不让图片或视频任务跨 Provider 自动故障转移。
- 不在缺少任务 ID 和资产 URL 时猜测服务端任务。
- 不把“恢复”实现为重新调用生成接口。
- 不自动取消供应商侧任务，也不绕过供应商计费与限流。

## 三、用户入口

`/media` 从项目本地 `.xiu/media-operations.json` 读取最近记录，显示稳定请求 ID、种类、状态、Provider/模型、任务 ID和保存路径。签名 URL、完整供应商错误和凭证不会显示。

用户可以用自然语言提出：

```text
恢复媒体请求 12ab34cd 到 assets/recovered.mp4
```

Agent 可先调用只读的 `list_media_operations`，再调用 `resume_media_operation`。请求 ID 支持完整 ID，或者至少 8 个字符且唯一的前缀；歧义前缀必须拒绝。

## 四、恢复状态机

1. 已有保存文件或缓存：复制到用户指定的工作区路径。
2. 已有视频任务 ID：继续查询同一任务，完成后下载。
3. 已有资产 URL：重新下载并缓存。
4. `submitting` 或 `ambiguous`：提交结果未知，保持阻断。
5. `failed`：终态失败，保持阻断；新生成必须走独立的生成工具和显式审批。

恢复工具没有 `createImage` 或 `createVideo` 调用路径，因此不能产生新的生成请求。记录的 Provider 与当前 Provider 不一致时拒绝恢复，避免跨供应商语义和计费混乱。

## 五、安全边界

- 输出仅允许写入受信任工作区，并沿用文件检查点和变更摘要。
- 恢复属于执行操作，但不继承为危险操作或付费生成授权。
- 新生成、强制重复生成和危险操作的原有审批规则保持不变。
- 媒体账本最多保留 200 项；列表默认显示 30 项，工具最多返回 100 项。

## 六、自动化验收

- 列表按更新时间倒序。
- 完整请求 ID、唯一前缀和任务 ID均可解析。
- 过短、找不到和歧义前缀明确失败。
- 缓存恢复不调用下载或生成 API。
- 视频恢复只轮询既有任务一次或多次并下载，`createVideo` 调用次数为零。
- 歧义提交恢复时下载和生成调用次数均为零。
- 中文活动提示完整，TypeScript 类型检查、全量测试、构建和 npm 打包预览通过。

## 七、真实端点人工验收

发布前在可控预算下逐项验证：

1. 已完成任务再次恢复，供应商没有新增生成任务。
2. 模拟资产下载中断，再恢复时只重新下载。
3. 视频轮询中断后退出并重启，按请求 ID继续原任务。
4. 模拟提交结果未知，`/media` 可见请求 ID，但恢复明确拒绝自动重放。
5. 切换到其他 Provider 后恢复被拒绝；切回原 Provider 后可继续。

若无法从供应商控制台或请求日志确认“没有产生第二次提交”，不能宣称真实付费验收完成。
