# Xiu v0.11.3：安全缓存与重复请求治理

## 目标

降低长任务中重复系统提示、Provider 元数据发现和能力探测造成的 Token、延迟与请求浪费，同时确保任何工具调用、文件修改、审批、媒体生成或流式回答都不会被 Xiu 本地缓存后重放。

## 缓存边界

- 主 Agent 的回答不进入本地结果缓存。即使请求字节完全相同，也必须重新经过 Provider，以免重放工具调用或过期工作区判断。
- OpenAI 与 OpenAI-compatible 端点使用服务端自动 Prompt Cache；Xiu读取 API 返回的 cached token 统计，不伪造命中。
- Anthropic 对稳定系统提示设置 `ephemeral` 缓存边界，并读取 cache creation/read token 统计。
- 能力探测继续持久化，但增加协议版本、Provider 配置指纹和 TTL；端点、代理、能力声明、鉴权配置或模型发生变化时旧结果失效。
- 模型发现和能力探测允许在同一进程内合并完全相同的并发请求。失败不会写入完成缓存，也不会长期抑制重试。
- 媒体生成继续使用既有幂等请求记录，不纳入本版本的通用请求缓存。

## 数据隔离与失效

- 缓存键必须包含 Provider Profile、模型和不含密钥明文的配置指纹。
- 不跨 Provider、模型或配置复用探测结果。
- Provider 配置文件升级为版本 2；版本 1 自动迁移，旧探测结果因缺少可信指纹而丢弃并按需重探。
- 修改本地 API Key 时主动失效该 Provider 的探测缓存，但不得把 Key 或 Key 哈希写入项目、会话或诊断。
- 内存模型发现缓存有界、短 TTL，并限制最大条目数。

## 可观测性

- `/diagnostics` 显示 Prompt Cache 读取 Token、写入 Token、命中请求数和命中率。
- 没有 Provider 缓存统计时明确保持为 0，不根据请求相似度猜测命中。
- Token 总量仍表示 Provider 实际处理的累计输入/输出；缓存读取 Token 是输入 Token 的子集。

## 验收标准

- OpenAI-compatible usage 中的 `cached_tokens` 能进入任务诊断。
- Anthropic 请求包含系统提示缓存边界，并正确统计 cache creation/read tokens。
- 两个完全相同的并发模型发现或能力探测只执行一次底层请求。
- Provider 配置版本 1 可迁移；旧探测不被错误复用，版本 2 探测按配置指纹恢复。
- 修改端点、代理、能力声明或鉴权后，相关能力缓存失效。
- Agent 回答、工具调用、媒体生成、审批与工作区写入没有本地结果缓存或自动重放路径。
- 既有工作区信任、风险审批、Plan 模式、恢复点和危险操作确认测试继续通过。
