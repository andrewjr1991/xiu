# Xiu v0.9.7 Repository Map 与符号关系设计

## 目标

- 为 Agent 提供有界、可分页的项目模块地图，减少反复列目录和广泛文本搜索。
- 对 JavaScript、TypeScript、JSX、TSX、MJS 和 CJS 使用官方 TypeScript AST 提取符号定义、模块依赖、引用和调用。
- 符号数据与 v0.9.6 文件索引一起增量缓存，未变化文件不重新读取或解析。
- 搜索结果包含文件、行列、符号种类、容器、导出状态和关系类型，便于模型继续精确读取源码。

## 非目标与语言边界

- 本版本不实现跨语言 Tree-sitter、LSP、类型推断或运行时动态调用分析。
- Python、Go、Rust、Java 等文件会出现在 Repository Map，但本版本不宣称拥有精确符号关系。
- JavaScript/TypeScript 的动态属性、反射、字符串调用和运行时重绑定无法静态确定；结果是代码导航证据，不是编译器级完整调用图。
- 不把 AST 或完整源码写入缓存，也不自动把大段代码注入模型上下文。

## 数据模型

每个受支持文件最多保存：

- 500 个符号：名称、种类、行列、容器、是否导出和有界签名。
- 300 条模块依赖：模块说明符、导入绑定和可解析的工作区目标。
- 2000 个引用：名称、行列、引用或调用类型、可选限定符和所在符号容器。

索引缓存版本升级。旧缓存、损坏字段、越界数组或工作区外路径会触发完整重建。文件路径、大小和修改时间未变化时，词项与符号数据整体复用。

## 只读工具

### `repository_map`

按路径前缀分页展示模块。每项包含语言、主要符号、内部依赖和被依赖数量；同时返回顶层目录统计、总模块数、已分析模块数、符号数和依赖数。

### `find_symbol`

按名称查询定义。精确匹配优先，其次前缀和包含匹配；可按种类和路径前缀过滤。结果包含稳定位置和有界签名。

### `find_references`

查询某个符号的定义、导入和普通引用。存在同名定义时可通过 `defined_in` 消除歧义；未指定时结果明确列出匹配定义，不静默假定唯一目标。

### `find_callers`

查询直接静态调用位置，覆盖同文件调用、命名导入别名和命名空间导入属性调用。结果包含调用方容器以及调用位置。

四项工具均为 `read` 风险，可在 Plan 模式运行；统一返回有界有效 JSON、`returned_count` 和 `next_offset`。

## 关系解析

- 相对导入按当前模块目录解析，尝试显式路径、常见 JS/TS 扩展和 `index.*`。
- 命名导入记录 `imported -> local`；默认导入使用 `default`；命名空间导入记录限定符。
- 同文件引用按原符号名关联。
- 跨文件引用只在导入边明确指向目标模块时关联，避免仅凭全局同名制造大量误报。
- 调用通过 `CallExpression`、`NewExpression` 和标签调用的 AST 位置确定；属性调用保留限定符。

## 安全、性能与失败恢复

- 仅解析工作区边界内、允许扩展名且不超过 512 KB 的普通文件，不跟随符号链接。
- 解析错误不会阻断整个索引；该文件保留路径词项和模块节点，并标记为未分析。
- 输出最多 100 条，单个签名和上下文有长度限制，总 JSON 输出保持有界。
- TypeScript 解析器作为明确运行时依赖打包，不依赖用户项目自行安装 TypeScript。
- 缓存仍采用临时文件写入后替换，损坏时自动重建。

## 验收标准

- 能从多文件 TypeScript 示例中找到函数、类、方法、接口、类型和变量定义。
- 能解析相对模块依赖、命名导入别名、命名空间调用以及同文件直接调用。
- 同名符号不被错误合并；`defined_in` 能稳定选择目标定义。
- Repository Map 支持路径过滤和分页，结果不超过边界且不包含完整源码。
- 修改一个源文件时只重新分词和解析该文件，未变化文件的符号数据从缓存复用。
- JavaScript 语法错误、超大文件、缓存损坏和符号链接不会破坏启动或泄露工作区外内容。
- 类型检查、全部测试、构建、npm 审计、打包预览和隔离安装通过。
