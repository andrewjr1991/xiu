# Xiu v0.12.0 远程 MCP OAuth 与协议安全基线设计

## 1. 版本定位

v0.12.0 是 v0.12“安全与团队能力”的第一个可交付切片，目标是让 Xiu 能安全连接需要用户授权的远程 MCP Server，并为后续 MCP Resources、Prompts、权限清单和审计日志建立可信身份基础。

本版本不是简单地给 HTTP Header 填入一个 Token。它需要完成 OAuth 发现、客户端注册、浏览器授权、本地回调、Token 持久化与刷新、Scope 升权、注销、取消、脱敏和诊断的完整闭环。

## 2. 规范与兼容基线

- 以 MCP `2026-07-28` Authorization 规范为目标基线。
- 继续兼容 Xiu v0.11.4 已支持的 stdio 和旧版 Streamable HTTP Server。
- 使用 `@modelcontextprotocol/client` 提供的 `OAuthClientProvider`、授权发现、PKCE、Issuer 校验和 Streamable HTTP 认证接入能力，不在 Xiu 内重新实现一套协议解析器。
- 新协议已取消传输层握手和 Session。Xiu 不得再把 `Mcp-Session-Id` 是否存在当作连接成功或正确关闭的必要条件；旧 Server 返回 Session 时仍按兼容路径处理。
- Client 注册优先级遵循：预注册信息 > Client ID Metadata Document（CIMD）> Dynamic Client Registration（DCR）兼容回退 > 提示用户补充配置。
- DCR 已被新规范弃用，因此只作为旧 Server 兼容方案，不作为长期默认架构。

Xiu 暂无可长期承诺的公开 HTTPS Client Metadata URL，因此 v0.12.0 不伪造 CIMD 地址。首版必须支持预注册和 DCR；只有在项目拥有稳定 HTTPS 元数据地址后才启用内置 CIMD。

## 3. 用户体验

### 3.1 配置与命令

保留现有命令，并增加：

- `/mcp add`：向导增加认证方式：无认证、Bearer 环境变量、OAuth。
- `/mcp auth [name]`：查看认证方式、登录状态、Issuer、已授权 Scope 和到期状态，不显示 Token。
- `/mcp login [name]`：显式启动 OAuth 登录；未指定名称时用上下键选择 Server。
- `/mcp logout [name]`：尝试撤销 Token，然后清理本地授权状态。
- `/mcp test [name]`：未登录时返回“需要登录”，不自动弹出浏览器。
- `/mcp reload`：重新加载配置和连接，但不清除有效 OAuth 凭证。

### 3.2 登录流程

1. 用户执行 `/mcp login`。
2. Xiu 展示 MCP Server、授权服务器、请求 Scope 和回调地址。
3. 用户确认后，Xiu 在 `127.0.0.1` 启动一次性回调监听器。
4. Xiu 校验授权 URL 后，通过无 Shell 的平台 URL 处理器请求打开浏览器；终端始终输出可复制的完整 URL，因为进程成功分发不等于浏览器一定在受管设备上出现。
5. 终端显示“等待浏览器授权”，并持续显示剩余时间；`Ctrl+C` 可取消。
6. 回调成功后验证 `state`、`iss` 和 PKCE，交换 Token，立即关闭监听器。
7. Xiu 输出已授权 Scope 和到期时间，重新连接该 MCP Server，并刷新 `/mcp` 状态。

回调页只显示简短的成功或失败信息，不包含授权码、Token、Scope 详情或项目路径。

### 3.3 状态模型

MCP 状态扩展为：

- `connected`：已连接。
- `auth-required`：Server 要求 OAuth，尚未登录。
- `authorizing`：正在等待浏览器回调。
- `refreshing`：正在刷新 Token。
- `scope-required`：当前操作需要额外 Scope。
- `failed`：协议、网络或配置失败。

启动摘要只显示数量；`/mcp` 和 `/mcp auth` 提供详细原因。普通启动不能因为一个远程 MCP 未登录而阻止 Xiu 使用其他能力。

## 4. 配置与凭证存储

### 4.1 非敏感 MCP 配置

用户级 `~/.xiu/mcp.json` 或受信任项目级 `.xiu/mcp.json` 可包含：

```json
{
  "mcpServers": {
    "example": {
      "transport": "streamable-http",
      "url": "https://example.com/mcp",
      "auth": {
        "type": "oauth",
        "registration": "auto",
        "scopes": ["files:read"]
      }
    }
  }
}
```

可选非敏感字段：

- `registration`: `auto`、`pre-registered`。
- `clientId`: 预注册的公共 Client ID。
- `clientMetadataUrl`: 用户明确配置的 HTTPS CIMD 地址。
- `scopes`: 用户预期的最小 Scope；最终以 Server Challenge 为准。
- `callbackPort`: 预注册 Server 要求固定回调端口时使用。

Client Secret 不允许写入项目级配置。若兼容 Server 必须使用 Secret，只能引用用户环境变量；v0.12.0 默认实现公共客户端授权码 + PKCE 流程。

### 4.2 敏感授权存储

OAuth 凭证独立保存到用户级 `~/.xiu/mcp-auth.json`，而不是 MCP 配置、项目目录或 Session JSONL。文件使用版本化结构、原子写入和尽可能严格的用户权限。

每条记录使用以下稳定身份共同绑定：

- MCP canonical resource URI。
- Authorization Server issuer。
- Client ID 或 Client Metadata URL。

记录可保存：

- Access Token、Refresh Token、Token Type、到期时间和已授权 Scope。
- DCR 返回的 Client 信息。
- 经过验证的 Issuer 与资源元数据地址。

不得保存浏览器授权码、`state`、PKCE verifier 或完整错误响应。它们仅存在于当前登录流程内存中，结束、超时或取消后立即清理。

本地 Token 与 Provider API Key 一样属于本机敏感配置，不提交 Git。v0.12.0 明确告知用户它使用本地文件保护而非系统钥匙串；Windows Credential Manager、macOS Keychain 和 Linux Secret Service 作为后续加固项。

## 5. OAuth 状态机

```text
unconfigured
  -> discovering
  -> auth-required
  -> authorizing
  -> exchanging
  -> authorized
  -> connected

authorized
  -> refreshing -> authorized
  -> scope-required -> authorizing
  -> invalid -> auth-required
  -> logout -> unconfigured
```

### 5.1 发现

- 优先解析 `401 WWW-Authenticate` 中的 `resource_metadata` 和 `scope`。
- 缺失时按 RFC 9728 顺序尝试路径级、再尝试根级 Protected Resource Metadata。
- Authorization Server Metadata 同时支持 RFC 8414 和 OpenID Connect Discovery，并严格验证 Metadata 的 `issuer` 与发现 URL 对应的 Issuer 完全一致。
- 多个 Authorization Server 时不自动猜测。首次让用户选择并持久化精确 Issuer；不同 Issuer 的 Client 信息和 Token 完全隔离。
- 授权与 Token 请求都必须带 canonical `resource` 参数。

### 5.2 注册

1. 若用户为该 Issuer 配置预注册 Client ID，则使用它。
2. 若 Server 宣告支持 CIMD 且配置了可信 `clientMetadataUrl`，使用 URL 作为 Client ID。
3. 若 Server 仅提供 `registration_endpoint`，可通过 DCR 兼容注册，并设置 `application_type: "native"`。
4. 均不可用时停止并给出需要填写的字段，不循环尝试。

DCR Client 信息必须绑定 Issuer；Issuer 变化时不得复用。

### 5.3 授权码与 PKCE

- 只支持 Authorization Code + PKCE S256。
- Metadata 未声明 S256 时拒绝继续，不降级为明文 verifier 或无 PKCE。
- `state` 使用密码学安全随机数、单次有效，并与 Issuer、resource、redirect URI、PKCE verifier 绑定。
- Authorization Response 携带 `iss` 时做无规范化的精确字符串比较；Metadata 声明必须返回 `iss` 而回调缺失时拒绝。
- 回调监听器只绑定 `127.0.0.1`，默认使用稳定端口；端口被占用时清楚报错，不静默换到未注册 URI。
- 等待默认 5 分钟，超时、`Ctrl+C`、第二个回调或状态不匹配都会关闭监听器并清除瞬时状态。

### 5.4 Token 与刷新

- 每次请求前由 OAuth Provider 提供有效 Bearer Token。
- 到期前留出时钟偏差安全窗口；同一 Server 的并发刷新使用 single-flight，避免 Refresh Token 被并发轮换后互相作废。
- 保存新 Token 时原子替换旧记录；若 Server 轮换 Refresh Token，立即丢弃旧值。
- Refresh Token 缺失或刷新被明确拒绝时进入 `auth-required`，不无限重试。
- 网络瞬时失败可有限退避，但不得把无效凭证错误当作网络错误。

### 5.5 Scope 升权

- 初次 Scope 优先使用 Challenge 中的 `scope`，其次使用资源元数据的最小 Scope。
- `403 insufficient_scope` 时计算“已有 Scope ∪ 新要求 Scope”。
- Xiu 必须展示新增 Scope 并征求用户同意，再打开浏览器；不得在后台静默扩大权限。
- 同一操作最多进行一次升权授权和一次协议重试，跨请求记录失败组合，避免循环弹登录。
- 只有明确的 `401/403` 鉴权拒绝允许在成功授权后重试。超时、断网或响应不确定时不得自动重放可能有副作用的工具调用。

### 5.6 注销

- 若 Metadata 提供撤销端点，先尝试撤销 Refresh Token，再撤销 Access Token。
- 无论远端撤销成功与否，用户确认后都清理本地 Token；远端失败需要明确提示。
- 默认保留 DCR Client 信息以便再次登录；提供“同时忘记 Client 注册”的二次选项。

## 6. 安全边界

### 6.1 URL 与 SSRF

- OAuth 协议端点默认必须是 HTTPS。
- HTTP 只允许本地开发场景中的回环地址，并沿用 Xiu 现有远程 MCP URL 安全策略。
- 拒绝 `javascript:`、`data:`、`file:`、`vbscript:` 等协议。
- 每次重定向都重新校验目标，不盲目跟随跨协议或内部地址跳转。
- `resource_metadata`、Issuer 和各 OAuth endpoint 可能来自恶意 Server，必须经过私有、保留、链路本地和云元数据地址检查。
- 跨 Origin Authorization Server 第一次使用时展示来源并让用户确认；确认仅绑定当前 MCP resource 与精确 Issuer。
- 企业内网 OAuth 以后通过显式网络策略开放，不能为了兼容而默认关闭 SSRF 防护。

### 6.2 浏览器启动

- 授权 URL 必须先解析为 URL 对象并通过 scheme、host 和发现绑定校验。
- 禁止把 URL 拼接进 `cmd.exe`、PowerShell、`sh` 或任意 Shell 字符串。
- Windows 使用无 Shell 的进程参数调用系统 URL Handler；macOS/Linux 同样使用参数数组调用平台 opener。
- 自动打开失败不影响授权，终端始终提供完整 URL 作为后备。

### 6.3 项目信任与工具审批

- 不受信任项目不能加载项目级 MCP 配置，也不能触发 OAuth。
- 项目文件只能声明“这个 Server 需要 OAuth”，不能读取、写入或选择其他 Server 的 Token。
- Token 不进入模型上下文、工具参数、Session、诊断、错误文本、Diff 或审批记录。
- OAuth 登录只证明远端身份，不代表用户批准 MCP Tool 的副作用。
- 远程工具继续通过现有工作区信任、风险审批、Plan 模式、取消、检查点和危险操作确认。
- MCP Server 名、Tool 描述和返回内容都视为不可信输入，不能修改 Xiu 的安全策略。

## 7. 代码结构

建议新增：

- `src/mcp-oauth.ts`：`OAuthClientProvider` 实现和授权状态机。
- `src/mcp-auth-store.ts`：版本化凭证存储、Issuer/resource 绑定和原子更新。
- `src/oauth-loopback.ts`：一次性回调监听、state/iss 校验、超时和取消。
- `src/oauth-url-policy.ts`：协议 URL、重定向和 SSRF 策略。

调整：

- `src/mcp.ts`：为 HTTP transport 注入 OAuth Provider，区分 `auth-required` 与连接失败，兼容有/无 Session 的协议实现。
- `src/cli.ts`：新增 auth/login/logout 命令、选择器、授权确认和动态状态刷新。
- `src/types.ts` 或 MCP 内部类型：定义版本化配置和认证状态，禁止用松散对象穿透安全边界。

所有时间、随机数、浏览器 opener、HTTP fetch、回调监听器和凭证存储都要可注入，以便测试不依赖真实浏览器和公网。

## 8. 实施顺序

### 阶段 A：协议兼容与数据模型

状态：已完成（2026-08-11）。

1. 已固化当前 v0.11.4 回归测试。
2. 已增加新旧 Streamable HTTP Server 兼容测试：旧路径回退 initialize + Session；新路径协商 `2026-07-28`，不使用 initialize 或 Session。
3. 已增加严格 OAuth 配置 Schema、认证状态和用户级版本化凭证存储；真实登录仍属于阶段 B。

### 阶段 B：登录闭环

状态：已完成（2026-08-11）。

1. 已实现 OAuth URL、逐跳重定向与 DNS 连接地址校验；拒绝危险 Scheme、凭据、私有/保留/链路本地/云元数据地址及 POST 重定向。
2. 已通过官方 SDK 接入 RFC 9728、RFC 8414/OIDC 发现、Issuer/resource 绑定，以及预注册、配置 CIMD、DCR 兼容优先级。
3. 已实现 PKCE S256、精确且一次性 state、`iss` 校验、固定 `127.0.0.1` 回调、无 Shell 浏览器启动与 `/mcp login`；授权前显示 MCP、Issuer、Scope 和回调地址。
4. 已把 OAuth Provider 注入 Streamable HTTP Transport；本地模拟 Server 已覆盖 Token 持久化、进程内重建 Manager 后复用凭证、重连、发现工具和真实工具调用；系统浏览器启动失败时保留授权流程并显示可复制链接。
5. `/mcp add` 已可选择无认证、Bearer 环境变量或 OAuth，并支持预注册 Client ID、配置 CIMD 与 DCR 兼容方式。
6. 已使用 Cloudflare 官方远程 MCP 完成人工验收：DCR、PKCE、浏览器回调、Token 复用、重连、3 个工具发现和真实只读查询均成功；同时修正远程 `write` 风险被误判为本地文件变化的问题。

### 阶段 C：生命周期与安全

状态：已完成（2026-08-11）。

1. 已实现绝对到期时间、提前一分钟刷新、按 OAuth 身份 single-flight、Refresh Token 轮换原子保存，以及缺失/明确失效凭证清理。
2. 已实现 `403 insufficient_scope` 新增 Scope 展示与审批；授权成功后只重试明确被拒绝的请求一次，不重放网络结果不确定的操作。
3. 已实现 `/mcp auth`、`/mcp logout`、Refresh/Access Token 撤销顺序、可选忘记 Client 注册、Ctrl+C/AbortSignal、五分钟超时和回调监听器竞态清理。
4. Bearer、Token、授权码和 Client Secret 会在 OAuth 状态、连接错误、工具错误与撤销警告中脱敏。
5. 本地自动化覆盖刷新 single-flight、Token 轮换、Scope 升权一次重试、撤销顺序、取消和脱敏；阶段 C 完成时全量基线为 295 项测试。

### 阶段 D：真实验收与发布

状态：已完成（2026-08-11），等待用户发布。

1. 本地模拟 OAuth Server 已覆盖发现、PKCE、回调、刷新、轮换、Scope 升权、撤销、取消、脱敏和失败恢复路径。
2. Cloudflare 官方远程 MCP 已完成 `/mcp auth`、3 个工具连接、注销、重新登录、Windows 浏览器回调和 Token 复用人工验收。
3. Windows 自动打开曾把 Explorer 进程创建误判为浏览器已打开；现已改为优先使用系统 URL 协议处理器、检查分发结果并始终显示完整手动链接，真实复测通过。
4. typecheck、296 项全量测试、build、npm pack 和官方 Registry 隔离干净安装均通过；安装包版本冒烟为 `0.12.0`。
5. 发布包已移除误加入的 `@xiu-ai/cli: file:` 自依赖，README、USAGE、PUBLISHING 和 ROADMAP 已同步，候选版本可交由用户发布。

## 9. 自动化验收矩阵

### 正常流程

- 401 Challenge 能发现资源元数据和 Authorization Server。
- RFC 8414 与 OIDC Discovery 两种路径均可连接。
- 预注册 Client、CIMD 配置路径和 DCR 兼容路径分别可用。
- 登录 URL 包含 S256 PKCE、state、正确 Scope 和 canonical resource。
- 回调成功后 Token 被保存，Server 重新连接，Tool 可正常调用。
- 重启 Xiu 后仍可使用未过期 Token。

### 刷新与升权

- 到期 Token 自动刷新且不打开浏览器。
- 并发请求只触发一次刷新，旋转后的 Refresh Token 不被旧值覆盖。
- 403 Scope Challenge 显示新增 Scope；拒绝后不重试，允许后最多重试一次。
- 刷新明确失败后进入 `auth-required`，不会无限重试。

### 攻击与错误流程

- 非 HTTPS OAuth endpoint、危险 scheme、私有/保留目标和恶意重定向被拒绝。
- Metadata Issuer 不一致、Authorization Response `iss` 不一致或应有但缺失时拒绝兑换授权码。
- 缺失 S256、state 错误、重复回调、超时和取消都不能产生 Token。
- 不同 resource、Issuer 或 Client ID 之间不能复用 Token 或注册信息。
- OAuth URL 中的 Shell 元字符不会形成命令执行。
- Token、授权码、Verifier 和 Secret 不出现在日志、Session、错误、诊断和测试快照中。

### 安全回归

- 未信任项目仍不能加载项目 MCP。
- OAuth MCP Tool 仍经过审批、Plan 模式和危险操作确认。
- 取消登录或关闭 Xiu 会关闭回调端口和网络请求。
- 未登录或单个 OAuth Server 失败不影响其他 MCP 和 Agent 基础功能。
- stdio、Bearer 环境变量和旧 Streamable HTTP 行为不退化。

## 10. 发布验收标准

只有以下条件全部满足，才把 v0.12.0 标记为可发布：

- 用户能通过终端完成“添加 OAuth MCP → 登录 → 调用工具 → 重启复用 → 注销”的完整闭环。
- 没有 Token/授权码/Verifier/Secret 泄漏路径。
- PKCE、state、issuer、resource 和 URL/SSRF 防护均有反向测试。
- Token 刷新、Scope 升权和请求重试不会重复执行不确定的副作用。
- 新旧 MCP HTTP 协议路径和 stdio 回归全部通过。
- 真实远程 OAuth MCP 至少完成一次连接、调用、刷新或重新登录、注销验收。
- 中英文 UI 均无阻断，`Ctrl+C` 在发现、登录和连接阶段均可立即生效。

## 11. v0.12.0 非目标

- MCP Resources、Prompts、Apps、Tasks 和授权扩展。
- Client Credentials、JWT Bearer、Device Authorization Grant 和企业托管授权。
- 内置云端 CIMD 托管服务。
- 操作系统钥匙串集成和多设备凭证同步。
- 同一 Server 多账号同时在线。
- 通过 OAuth 自动信任项目、自动批准工具或降低风险等级。
- 在本版本一次完成组织策略、完整沙箱和团队审计平台。

后续优先级为：Resources/Prompts 只读浏览与引用、MCP/Skill 权限清单、系统钥匙串、审计事件，以及企业托管授权。
