# Xiu v0.11 Provider 故障转移设计

## 目标

v0.11 解决单一模型服务发生短时网络故障、限流或服务端错误时，长任务只能整体失败的问题。实现必须优先保证不重复副作用，并让用户清楚知道为什么重试、为什么切换、哪些候选被跳过，以及最终由哪个 Provider 继续。

## 配置模型

- 每个主 Provider ID 对应一条最多 8 项的有序备用链。
- 备用链保存在用户级 `~/.xiu/providers.json`，不进入项目仓库或会话配置。
- `/provider fallback` 查看当前链。
- `/provider fallback add|remove|clear` 追加、移除或清空。
- 自动切换不修改 `active` 和 `activeModels`，因此不会覆盖用户手动选择的下次启动默认项。

## 请求状态机

1. 当前 Provider 对同一模型请求最多尝试 3 次，退避为 500ms、1000ms。
2. 仅 HTTP 408、425、429、5xx，以及连接重置、拒绝、DNS 临时错误、超时等瞬时错误可继续。
3. 401、403、404、请求格式错误、模型不存在、上下文超限和用户取消等确定性错误立即失败。
4. 三次仍失败后，从主 Provider 的备用链中按顺序选择尚未尝试的候选。
5. 当前请求需要工具时，候选必须具有有效工具能力；当前输入估算必须低于候选的安全压缩线。
6. 候选成功后，本任务后续模型轮次继续使用它；同一任务不会重新尝试已经失败的 Provider。

## 不重复副作用边界

故障转移只包围模型请求，不包围工具执行。只有本轮模型尚未流式输出任何文字时才允许切换。模型返回结构化工具调用后，请求已经成功，工具仍由原有审批、Plan 模式、工作区约束和检查点系统执行；后续 Provider 失败不会重放已经完成的工具结果。

以下情况禁止自动切换：

- 用户已经看到部分流式回答。
- 用户主动取消或 AbortSignal 已触发。
- 错误被分类为确定性请求错误。
- 没有满足能力或上下文预算的备用 Provider。

## 第二阶段：压缩与子 Agent

- 上下文压缩是无工具、无流式输出的纯文本模型请求，因此可以复用相同的有限重试和备用链；压缩请求切换后，后续任务也继续使用实际成功的 Provider。
- 并行子 Agent 各自创建独立故障转移控制器和“本任务已尝试 Provider”集合。主 Agent 与不同子 Agent 之间不会共享失败游标。
- 子 Agent 切换 Provider 后仍保留自己的 Worktree、最大轮次、会话命名空间、审批策略和按角色裁剪后的工具集合。
- 压缩即使所有 Provider 均失败，仍使用原有本地检查点摘要兜底；这类可恢复失败不会额外输出一条误导性的主任务失败信息。

## 可观测性

- 终端永久输出 `Provider 故障转移：A/model → B/model` 回执。
- 被跳过候选显示简短原因。
- 会话 JSONL 写入 `provider_failover` 语义事件。
- 任务诊断保存切换次数、来源、目标、模型和脱敏原因；`/diagnostics` 可复查。
- 实时状态栏随运行配置更新为实际 Provider/模型。

## 第三阶段：媒体请求幂等与计费边界

- 图片和视频生成请求使用“Provider ID + 模型 ID + 规范化生成参数”的 SHA-256 幂等键；输出路径、轮询超时等本地参数不参与键计算。
- 每个请求在付费提交前写入 `.xiu/media-operations.json`。图片 URL、视频任务 ID 或 base64 资产一旦返回就立即持久化；base64 和已下载视频另存到 `.xiu/media-assets/`。
- 相同的已完成请求直接复用本地资产；图片下载失败只继续下载，视频轮询或下载失败只从已保存任务继续，不创建第二个任务。
- 付费提交阶段若发生断网、超时或取消，服务端是否已接受请求无法确定。Xiu 将状态标记为 `ambiguous` 并禁止自动重放；只有用户明确批准 `force_new_generation=true` 才会创建新的、可能重复计费的请求。
- 服务端明确返回终态失败时同样不自动新建任务。所有图片和视频生成工具均按危险操作审批，`--yes` 不会绕过首次计费确认；用户可以选择仅允许一次，或在当前 Xiu 进程内始终允许同类图片/视频操作。两类授权相互隔离、退出即失效，`force_new_generation=true` 不继承会话授权。
- 媒体请求不会跨 Provider 自动故障转移，因为不同 Provider 无法共享幂等语义或任务 ID。
- 视频状态查询采用低频轮询；查询接口出现瞬时 429/503 时在同一次工具调用内退避并继续原任务，不让模型通过改写提示词创建新任务。
- 新任务提交被 429/503 明确拒绝后，Provider/模型进入持久化冷却；新提示词和强制标志都不能绕过。`force_new_generation=true` 只接受完全匹配的历史请求。
- 工具失败或审批拒绝不生成成功回执；如果最后一次操作仍失败，Agent 以 `failed` 而不是 `completed` 结束。
- 同一模型轮次包含多个工具调用时，单个调用的参数校验异常被隔离为该调用的失败回执，不会击穿 Agent 主循环；若同批次已经有有效验证通过，冗余检查失败不会覆盖已成立的完成证据。

## 本阶段非目标

- 不根据价格或质量自动选择日常请求模型。
- 不自动跨 Provider 执行或重放图片/视频生成。
- 不把 Provider 错误转换为工具错误，也不绕过任何审批或工作区安全边界。

## 自动化验收

- 有序备用链可保存、恢复并在删除 Provider 时清理陈旧引用。
- 瞬时 503 连续失败三次后切换备用 Provider 并完成请求。
- 已有流式文字后发生连接重置时不调用故障转移控制器。
- 故障转移写入任务诊断并更新实时模型状态。
- 压缩请求在三次瞬时失败后切换备用 Provider，且不会向请求暴露工具。
- 并行子 Agent 使用独立备用链状态，不改变主 Agent 的 Worktree 和工具权限。
- 相同媒体请求复用已有资产；提交结果不确定时不自动重复计费；视频任务和资源下载中断后可从持久化状态恢复。
- 全量类型检查、测试、构建和 npm 包内容检查通过。
