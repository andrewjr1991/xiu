# Xiu v0.9.1：终端右键附件粘贴

## 目标

让支持终端鼠标上报的 Windows 终端中，右键与 `Ctrl+V` 使用同一套 Xiu 剪贴板读取逻辑，可直接导入普通文字、剪贴板图片和资源管理器复制的文件。

## 实现

- 仅在 Xiu 交互输入框存活期间开启基础鼠标跟踪和 SGR 扩展坐标模式。
- 收到右键按下事件后调用现有 `ClipboardAttachmentManager`，在当前光标位置插入文字或 `@.xiu/attachments/...` 引用。
- 右键释放不会重复粘贴，其他鼠标报告不会进入输入内容。
- 输入框结束、取消或切换后立即关闭鼠标跟踪并恢复终端模式。
- 保留 `Ctrl+V` 和 `/paste`，作为不支持鼠标上报或会拦截右键的终端的兼容入口。

## 兼容性边界

- Windows Terminal、支持 SGR 鼠标报告的 ConPTY 终端可直接右键导入附件。
- 部分旧版控制台或自定义右键菜单可能在事件到达 Xiu 前拦截右键，这时继续使用 `Ctrl+V` 或 `/paste`。
- 鼠标跟踪期间需要使用 `Shift+拖动` 进行终端文本选择。

## 验收标准

- SGR 右键按下只触发一次粘贴，释放事件不重复触发。
- 兼容 X10 右键报告。
- 普通按键不会被误判为鼠标事件。
- 输入开始时启用、结束时关闭鼠标报告。
- 完整测试、类型检查、构建与 npm 打包检查通过。
