# Xiu v0.7.2 设计：实时进展与长任务可靠性

## 1. 真实失败样本

v0.7.1 在闪购预标注任务中出现以下链路：

1. 主任务在 30 轮内反复读取相同规则、HTML 和助手代码，没有生成目标结果；
2. 运行界面只显示 `Thinking - turn N`，用户无法看出重复读取；
3. 主任务达到轮数上限后，Xiu 自动执行一条排队的补充要求；
4. “在线表格要可复制，也转成 JSONL”被当成独立开发任务，错误修改了标注助手；
5. 验证命令超时且 Git Diff 不可用，最终仍显示绿色 `Done`。

v0.7.2 将这个案例作为发布阻断回归样本。增加轮数不是解决方案；必须让进展可见、循环可阻断、补充语义正确、失败不会级联、完成状态有证据。

## 2. 目标

- 默认状态栏显示 `Turn 当前/上限`、阶段、耗时、补充数量、显式队列数量和最近活动。
- `Ctrl+O` 无需提交输入即可展开/收起最近 8 条实时活动。
- 运行中的 `/details` 立即切换实时活动，不进入模型任务队列。
- 普通运行期输入作为 steering 注入当前 Agent 的下一轮上下文。
- 只有 `/queue <task>` 才创建独立的下一项任务。
- 当前任务失败、取消或未验证时暂停，不自动执行显式队列。
- 相同成功工具调用或短工具调用周期重复时，在轮数上限前阻断。
- 修改文件但没有通过验证时显示 `Stopped unverified`，不得显示绿色成功。

## 3. 状态模型

### 3.1 Agent 结果

Agent 明确记录：

- `idle`
- `running`
- `completed`
- `unverified`
- `failed`
- `cancelled`

`onTaskComplete` 同时携带 `outcome`。`unverified` 可以保留模型的总结和已完成修改，但 CLI 必须把它视为未完成状态，并暂停后续调度。

### 3.2 Steering 与显式队列

- `agent.steer(text)` 只在 Agent 活跃时接受输入，最多暂存 20 条。
- Steering 在下一次模型请求前合并成明确的“当前目标补充”用户消息。
- 如果补充恰好在当前任务结束时提交，无法注入时才转为下一条同会话任务，不能静默丢失。
- `/queue <task>` 是用户明确要求的独立后续任务；`/queue` 只查看。

### 3.3 失败后的选择

当前任务失败、取消或未验证后，终端显示默认选择为“停止并返回”：

1. 停止并返回，清空显式队列；
2. 基于已有证据重试未完成任务；
3. 如果存在显式队列，可跳过当前任务并继续。

Esc 或非交互环境使用安全默认：停止，不自动继续修改。

## 4. 循环检测

工具签名由工具名和稳定排序后的输入组成。每次 Agent 运行维护独立记录：

- 同一签名第三次出现时阻断；
- 长度 1 到 4 的工具调用序列连续重复三轮时阻断；
- 将阻断结果返回模型，要求汇总现有证据并改变策略；
- 同一任务累计三次循环阻断后终止任务，不等到 `maxTurns`。

循环阻断不执行工具，因此不会重复写入、执行命令或扩大副作用。

## 5. 实时进展

`RunningTaskView` 保存最多 50 条轻量活动和 256,000 字符输出缓冲：

- 模型轮次开始；
- 工具开始、进度、结束或失败；
- 用户 steering 被接受；
- 计划更新和失败原因。

默认显示最近一条活动；`Ctrl+O` 展开最近八条。所有行复用 v0.7 的物理行宽裁剪和 Resize 重绘，不让活动输出直接争抢 stdin/stdout。

## 6. 完成门禁

- 文件修改后，只有工具明确报告验证成功才设置 `verified=true`。
- `run_command` 只有明显属于 test、typecheck、lint、build、check 或 verify 时才算验证尝试。
- 开发服务器超时不算验证成功，也不因为工具具有验证接口就自动算“已尝试”。
- 修改后未通过验证时，模型获得一次完成门禁提醒；仍无法验证则结果为 `unverified`。
- Implementer 子 Agent 的未验证修改视为失败，不能被主 Agent 当作已完成结果集成。

## 7. 验收标准

- 长任务运行时无需提交命令即可通过 `Ctrl+O` 看到最近活动。
- 状态栏始终显示 Turn 上限和耗时，Resize 后无残影。
- 运行期补充“同时生成 JSONL”进入当前目标，不会变成“修改导出工具”。
- 三次重复读取相同文件或重复短周期会在 30 轮前停止。
- 达到轮数上限、循环阻断、取消或未验证后，显式队列不自动运行。
- 验证超时或不可用时不显示绿色 `Done`。
- typecheck、全部测试、build、pack、安全审计和干净安装通过。
