# Xiu v0.7 设计：专业终端交互基础

## 1. 目标

v0.7 首先解决真实使用中的终端交互问题：长输入难编辑、文件引用靠手打、选择器残影、审批易误触、长日志淹没状态、终端 Resize 后布局错乱。

本版本的完成标准不是“换一个更花哨的启动页”，而是建立可测试的终端 UI 状态模型，并交付以下闭环：

- 多行输入、任意位置光标编辑、Home/End、Delete/Backspace。
- `Ctrl+J` 插入换行，Enter 提交；窄终端和中文输入正确换行。
- 输入 `@` 后按项目索引补全文件路径。
- `Ctrl+R` 搜索历史输入。
- 未提交草稿持久化，异常退出或重启后恢复。
- 终端 Resize 后当前编辑器和选择器重新布局，不留残影。
- 审批请求使用方向键选择，默认选择拒绝。
- 工具活动默认显示摘要，完整输入/输出进入可回看的 `/details` 记录。
- 状态栏清楚区分 Auto/Plan、模型、上下文、Skills、MCP、Agent 和后台任务。

## 2. 本版本非目标

- 不引入 React/Ink 等大型 UI 框架。
- 不使用 Alternate Screen 隐藏用户已有滚动历史。
- 不在 v0.7.0 自动接受 Diff 区块；先建立活动记录和安全审批基础。
- 不声称已经提供全屏 IDE 式面板。
- 不允许 UI 重构绕过工作区信任、风险审批或 dangerous 确认。

交互式 Diff、固定多 Agent 面板、任务运行中持续输入队列属于 v0.7 系列后续增量；必须复用本版本状态模型，不能再直接争抢 stdout/stdin。

## 3. 架构

### 3.1 纯编辑状态机

`EditorState` 保存：

- 完整文本和 Unicode 字符光标位置。
- 命令、文件、历史搜索三类候选。
- 当前候选索引和历史搜索查询。

键盘操作先转换为纯状态变更，再由渲染器输出。这使中文、Emoji、多行和边界行为可以不依赖真实 TTY 自动测试。

### 3.2 响应式帧

渲染器根据 `columns` 生成物理行，并返回光标所在行列。UI 清理使用“上次实际物理行数”，Resize 时先清旧帧再按新宽度绘制。

### 3.3 候选来源

- `/`：Slash 命令。
- `@`：项目索引中的相对文件路径。
- `Ctrl+R`：当前进程输入历史。

候选只展示有限条，Tab 接受，Up/Down 选择，Esc 只关闭候选或清空搜索，不再默认清空用户全部草稿。

### 3.4 草稿与活动记录

- 草稿写入 `.xiu/draft.json`，只属于当前项目。
- 提交后删除草稿内容，不删除其他 Xiu 状态。
- 工具活动保存在内存中的有界环形记录，包含阶段、描述、结果摘要和完整详情。
- `/details` 打开选择器查看一项完整记录。

## 4. 键位

| 键位 | 行为 |
| --- | --- |
| Enter | 提交输入或接受完整 Slash 命令 |
| Ctrl+J | 插入换行 |
| Left/Right | Unicode 字符级移动光标 |
| Ctrl+Left/Right | 按单词移动 |
| Home/End | 当前逻辑行首/行尾 |
| Backspace/Delete | 删除光标前/后的字符 |
| Up/Down | 候选导航；无候选时浏览历史 |
| Tab | 接受命令或文件候选 |
| Ctrl+R | 打开/继续历史搜索 |
| Esc | 关闭候选或历史搜索，保留草稿 |
| Ctrl+C | 取消输入；任务执行时沿用现有取消语义 |

## 5. 安全设计

- 审批菜单默认高亮“拒绝”，Enter 不得等同允许。
- dangerous 操作继续要求显式批准，`--yes` 行为不变。
- `@` 补全只使用项目索引中的工作区相对路径。
- 草稿不得保存 API Key 配置；它只保存用户尚未提交的自然语言输入。
- 活动详情显示模型已经看到的工具输入/输出，不额外读取敏感文件。

## 6. 验收矩阵

| 场景 | 验收证据 |
| --- | --- |
| 中文/Emoji 多行编辑 | 字符级移动、删除和光标列自动测试 |
| 窄终端换行 | 每个物理行不超宽，光标落点正确 |
| `@` 补全 | 只返回索引路径，Tab 替换当前引用片段 |
| 历史搜索 | Ctrl+R 查询和选择结果可重复测试 |
| 草稿恢复 | 新进程加载上次未提交文本，提交后清空 |
| Resize | SIGWINCH 触发重绘且清理旧物理行 |
| 审批安全 | 默认拒绝，方向键后才可选择允许 |
| 活动详情 | 摘要有界、完整详情可通过 `/details` 查看 |
| 回归 | typecheck、全部测试、build、pack 和干净安装通过 |

## 7. 后续 v0.7.x

在本版本状态模型稳定后继续：

1. 任务执行期间保持输入编辑器可用，并把新要求排队或追加给活动 Agent。
2. 固定任务树、多 Agent 和后台进程面板。
3. 交互式 Diff，按文件或区块接受/拒绝。
4. 拖入图片/文件识别、主题、中英文 UI 和无障碍配色。
