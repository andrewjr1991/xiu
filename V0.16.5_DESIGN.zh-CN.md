# Xiu v0.16.5 设计：运行过程持久可见

## 目标

修复长任务运行时只看到“思考中”、模型阶段摘要和工具过程被留在内存缓冲区的问题。在 `补充> ` 编辑器持续可用的同时，把关键过程增量写入终端滚动历史，让用户随时知道当前做了什么、依据是什么、改了哪些文件以及验证到哪里。

## 用户可见行为

- 每次模型调用开始显示轮次。
- 模型在工具调用前明确给出的用户可见分析显示为“思考摘要”。
- 工具开始、进度和有界结果持续显示。
- 文件创建、修改、删除显示路径、增删统计、关键 hunk 和最多八行预览。
- 验证、重试、路由、失败和完成门禁继续显示并进入滚动历史。
- 底部继续显示当前计划、阶段、诊断和下一步；`Ctrl+O` 或 `/details` 切换最近活动详情。

## 渲染模型

运行期编辑器拥有可重绘区域。每次刷新先清除旧输入框和底部面板，再原子排出尚未消费的过程缓冲，将其写入不可重绘的终端滚动区，最后在新位置重绘输入框和底部面板。缓冲内容只消费一次，任务结束前再刷新尾部残留。

该模型不能直接从任意回调写终端，否则会破坏光标位置；所有运行期输出必须先进入 `RunningTaskView`，由编辑器刷新边界统一排出。

## 安全与隐私

- 不显示或保存模型私有思维链。
- 只显示模型明确面向用户的摘要和程序可核验的操作事件。
- 流式草稿在完整 Assistant Turn 通过语言规范化、联网来源和完成门禁前不进入永久滚动历史。
- 工具结果、文件预览和错误保持有界；凭证和已知秘密继续使用现有脱敏路径。
- 安全审计账本、任务运行记录和执行报告不新增思维链、Prompt、完整工具输出或源码正文。

## 非目标

- 不要求所有 Provider 返回内部推理。
- 不为没有返回摘要的模型伪造推理内容。
- 不把完整文件、网页正文或无界构建日志倾倒到终端。
- 不改变工作区信任、Plan 只读、审批、检查点、恢复或验证门禁。

## 验收标准

- 模型轮次、工具调用、文件变化和验证在任务仍运行时可见并保留在滚动历史。
- 用户正在输入的补充文字和光标在 250ms 自动刷新中保持稳定。
- 每批过程输出只出现一次，任务结束不重复打印文件卡片。
- 最终回答和完成回执顺序正确，没有丢失尾部输出。
- 中文、英文、窄终端、`Ctrl+O`、取消和审批切换均无回归。
- 类型检查、全量测试、构建、打包预览和干净安装通过。
