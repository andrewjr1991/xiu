# Xiu v0.9.0 可靠进程执行设计

## 背景

真实长任务中，复杂 `python -c`、`node -e`、JSON、正则表达式和包含空格的路径经常同时经过 JSON、PowerShell 与目标程序三层解析。即使模型表达的参数本身正确，也可能在 PowerShell 引号、反引号、美元符号或管道字符处失败。

## 目标

1. 新增原生 `run_process` 工具，以 `program` 和 `args[]` 分离传参，通过 Node.js `execFile` 直接启动程序。
2. 参数不经过 PowerShell 拼接、插值或二次解析；空格、单双引号、美元符号、分号、管道符、Unicode 和 JSON 按原值传递。
3. `run_command` 继续用于 PowerShell cmdlet、变量、管道和确实需要 Shell 组合的命令。
4. 模型默认优先使用 `run_process` 执行 Node、Python、Git、npm 及项目工具，只有需要 Shell 语法时才使用 `run_command`。
5. PowerShell 解析失败或复杂内联解释器命令失败时，结果附带迁移到 `run_process` 的明确提示。

## 安全边界

- `run_process` 禁止把 `powershell`、`pwsh`、`cmd`、`bash`、`sh` 或 `wsl` 作为程序，从而绕过 Shell 风险识别。
- 带目录分隔符的程序路径必须位于当前工作区；PATH 中的普通程序名可以直接使用。
- 危险程序与危险 Git 参数继续要求危险操作确认；普通执行仍遵循现有风险审批。
- 保留取消、超时、输出上限、UTF-8 子进程环境和 Windows 控制台编码回退。
- 不自动重写用户或模型提供的 PowerShell 命令，避免静默改变语义。

## 验收标准

- 带空格的参数、嵌套单双引号、`$`、`;`、`&`、`|`、JSON 和中文能逐字节传入 Node/Python 等目标程序。
- 复杂参数不触发 PowerShell 变量展开或管道解析。
- Shell 包装器在审批前被拒绝，并提示使用 `run_command`。
- `run_process` 可被识别为验证命令，成功和失败仍受完成门禁约束。
- 进程找不到、超时、取消和非零退出码都有清晰结果。
- 原有 `run_command`、后台任务与安全审批行为不退化。

## 后续里程碑

1. v0.10：结构化 HTML/JSON/CSV 提取工具。
2. v0.11：增量项目索引、Repository Map、符号定义/引用/调用关系。
3. v0.12：长任务 Token、耗时、失败命令与停滞诊断。
4. v0.13：Provider 插件化与更多云端、本地模型接入。
