# Xiu v0.8.5 终端交互与语言设计

## 目标

v0.8.5 把已具备的任务执行能力整理成稳定、清楚且可审计的终端对话体验：用户能区分自己的输入、Xiu 的回答、关键操作、文件修改与最终验证状态，并可选择完整的简体中文或英文体验。

## 交付范围

1. 渲染模型常用 Markdown，而不是原样显示标记符号。
2. 任务运行输入框只作为临时编辑区，完成后不把 `xiu[working]>` 写入永久记录。
3. 每轮任务有明确的用户输入标题、Xiu 回答和结束分隔。
4. 文件变化按文件合并，创建文件显示类型与大小，修改文件只显示有界关键片段，完整内容继续由 `/diff` 提供。
5. 永久记录重要的执行、验证和文件操作，但不倾倒完整工具日志。
6. 启动页为中文宽字符和终端末列预留安全空间，避免右边框换行断裂。
7. `/language` 持久设置界面语言，`--language` 和 `XIU_LANGUAGE` 提供单次或环境覆盖。

## 语言契约

- 支持 `zh-CN` 与 `en-US`。
- 设置保存在用户目录 `~/.xiu/settings.json`，不进入项目仓库。
- 中文模式下，菜单、状态、审批、进度、计划标题、用户可见推理说明与最终回答必须使用简体中文。
- 命令、代码、路径、工具名、模型名和外部原始输出不翻译。
- Xiu 不展示模型私有思维链；“思考”仅指用户可见的阶段说明、计划、依据和结论。
- 切换语言会立即刷新 Agent 系统提示，后续模型轮次使用新语言。

## 安全与兼容

- 不改变工作区信任、风险审批、Plan 模式、检查点和危险操作确认。
- 保持普通可滚动终端，不切换全屏缓冲区。
- 无颜色或非 TTY 输出仍保留可读文本和确定性的完成状态。
- `/details` 和会话日志继续保存完整工具证据。

## 验收标准

- 中文模式启动、执行、审批、帮助、进度和完成摘要没有英文界面标签。
- 模型提示明确要求简体中文用户可见输出，英文模式具有对称约束。
- Markdown 粗体、行内代码、列表、代码块和表格不泄漏结构标记。
- 连续修改同一文件不会在任务结尾倾倒全部历史日志。
- 宽窄终端中每一物理行都不超过可用列数，中文启动框边界连续。
- 全量测试、类型检查、构建、npm 打包和干净安装通过。
