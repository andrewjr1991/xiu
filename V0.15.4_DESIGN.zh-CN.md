# Xiu v0.15.4 设计：托管搜索自诊断与凭证恢复

## 目标

1. 用户无需猜测即可确认当前联网搜索配置和本机托管凭证状态。
2. 用户可显式检查服务健康与认证链路，并获得短小、可操作、无秘密的结果。
3. 托管设备凭证损坏、被撤销或状态异常时，可安全清除本机凭证并重新登记。
4. 保持普通启动零联网，不恢复此前已经解决的 MCP/联网等待问题。

## 非目标

- 不提供服务端设备列表、远程撤销或管理后台。
- 不删除用户自行配置的 API Key、代理或其他 Provider 凭证。
- 不加入忽略 TLS 错误、关闭证书校验或自动修改企业代理的开关。
- 不扩展为浏览器自动化、账号登录或网页写入。

## 命令语义

### `/web` 与 `/web status`

只读取本地设置、凭证引用、兼容凭证文件和当前进程的 Token 缓存状态。不得发起网络请求或触发设备登记。输出仅包含后端、端点、代理、凭证是否存在及存储类型。

### `/web doctor`

由用户显式触发，先请求托管服务 `/healthz`，再验证设备认证和短期 Token 签发。若本机无设备凭证，会在执行前说明可能登记新设备。诊断沿用搜索专用代理、TLS 信任和超时边界，结果不得返回 Token 或设备秘密。

### `/web reset`

采用默认取消的二次确认。确认后只删除托管搜索的本机长期设备凭证并清除内存 Token，保留 installation ID，方便服务端审计同一安装重新登记。下一次搜索或诊断再按需登记，不在重置命令中额外联网。

## 安全边界

- 系统凭证引用存在而系统凭证后端不可用时失败关闭，不能假装删除成功。
- 状态、诊断、审计、会话和错误信息不得包含 Authorization、设备秘密或短期 Token。
- 重置不影响 Tavily、Brave、自建 SearXNG、模型 Provider 或 MCP 凭证。
- 普通启动不执行状态诊断；只有用户命令或真实搜索可触发联网。

## 验收标准

1. 本地状态检查以网络调用计数为零通过测试。
2. 健康、登记、Token 签发路径有确定性成功与失败测试，返回对象不含秘密。
3. 兼容文件凭证可重置并按需重新登记；系统后端不可用时重置被拒绝。
4. 中英文命令说明、README、使用指南、发布门禁和路线图一致。
5. `npm run typecheck`、`npm test`、`npm run build`、`npm pack --dry-run`、差异检查和秘密扫描全部通过。
