# Xiu v0.9.10 完整会话回放设计

## 问题

v0.9.9 的恢复界面不是终端回放，而是临时摘要：只选最后 12 条用户或助手消息，把所有换行压成空格，并把每条裁到 600 字。它没有复用正常交互的 Markdown、文件变化、关键操作、提问和完成回执组件，因此长会话恢复后既不完整，也不像原来的对话。

## 目标

1. `/resume`、`xiu --resume` 和 `/history` 展示同一份完整会话。
2. 回放复用正常终端组件，保留多行正文、列表、代码块和表格。
3. 新会话能恢复文件变化、关键操作、等待问题和完成状态。
4. 已有会话继续兼容，并尽可能从全部事件还原。
5. 不把 ANSI 转义序列写入日志，不让旧终端宽度污染新终端布局。

## 实现

会话加载器新增 `SessionReplayTurn`：每轮包含显示输入类型、补充要求、最终回复、工作区变化、关键操作、阻塞问题、完成回执和任务诊断。

旧日志按 `task` 划分轮次，取该轮最后一个有效 assistant 文本作为最终回复，并关联 steering 与最后诊断快照。旧版内部继续任务会显示为简短的系统继续提示；能识别到用户回答时则恢复成“请回答>”输入。

新日志在 CLI 完成正常渲染数据汇总后追加版本化 `ui_turn` 事件。该事件保存语义对象，不保存颜色控制码或物理行位置。恢复时重新调用当前版本的 Markdown、文件变化、关键操作和完成回执渲染器，因此窗口尺寸变化后仍可正确排版。

## 兼容边界

旧版从未写入日志的临时进度动画和已经丢弃的 UI 卡片无法事后恢复；v0.9.10 会明确说明旧会话是“从保存事件重建”，不会谎称字节级原样。模型上下文恢复仍沿用原有 messages 与压缩逻辑，终端回放数据不会重复送入模型。

## 验收标准

- 超过 12 轮的旧会话全部出现，不裁掉较早轮次。
- 多行 Markdown 回复保持段落和列表，不压成一行。
- 新会话恢复后能显示与正常任务相同的文件变化、关键操作、问题和完成回执。
- `/resume`、启动参数恢复和 `/history` 输出结构一致。
- 内部重试提示不会把整份任务契约伪装成用户输入。
- 类型检查、全量测试、构建和 npm 打包预览通过。
