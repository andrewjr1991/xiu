# Xiu v0.9.4：终端原生右键与企业剪贴板降级

## 问题

v0.9.3 会在原生 `Get-Clipboard -Raw` 可用时开启鼠标上报，但部分企业策略只允许读取文字，禁止 `FileDropList` 和 `Image`。这会造成能力探测误判：Xiu 截获右键后无法读取复制的文件，终端原本的右键粘贴也同时失效，并可能把 PowerShell CLIXML 错误显示在输入框下方。

## 决策

- Xiu 不再开启鼠标上报，不再截获右键。
- 右键始终由终端宿主处理；Xiu 接收宿主最终写入字符流的文字或文件路径。
- `Ctrl+V` 和 `/paste` 保留增强附件逻辑，继续尝试 Windows `Get-Clipboard`。
- FileDropList、Text、Image 分别在独立错误边界内读取，单个格式失败后继续尝试其他格式。
- 只有成功检测到位图时才尝试可选 helper。原生读取失败或被策略限制时不会启动 helper。
- 对用户隐藏 CLIXML、命令参数和内部异常，仅提示使用终端原生粘贴或 `@path`。

## 安全边界

- 不绕过 PowerShell 执行策略、应用程序控制或集团安全软件。
- 不读取终端宿主未主动粘贴到字符流的内容。
- 文件附件仍受数量、单文件大小、总大小和目录拒绝规则限制。
- 纯位图在 helper 被禁用时必须先保存为文件。

## 验收标准

- 无论系统剪贴板 API 是否可用，Xiu 都不会吞掉终端原生右键。
- FileDropList 被禁止但 Text 可用时，文字粘贴仍能工作。
- 原生 API 返回 CLIXML 时，界面不显示原始错误内容。
- 原生读取失败不会调用 helper；位图检测成功后才允许调用。
- `Ctrl+V`、`/paste`、普通输入、文件路径解析和附件限制测试全部通过。
