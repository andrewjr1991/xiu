# Xiu v0.8.0 上下文检查点与大文件读取设计

## 背景

真实闪购预标注任务需要读取规则、两个百万字符级 HTML 和辅助项目，并生成最终在线表格。v0.7.4 已允许在获得新证据后重新读取文件，但任务经历多次自动上下文压缩后不断从头调查，最终达到 30 Turn 安全上限。

问题不在单次模型推理，而在两个工程边界：

1. 旧压缩把整段会话替换为模型生成的一段自由摘要。原始主任务、运行中补充、计划和下一步都可能丢失。
2. `read_file` 默认读取整份文本。压缩成单行的大 HTML 会产生超长工具结果，只保留前部内容，模型无法稳定向后推进。

## 目标

- 压缩后仍能逐字保留当前主任务，并把 steering 视为附加要求。
- 明确记录已完成证据、失败方法和唯一下一步，减少重复调查。
- 普通大文件按行分页，单行或 minified 文件按字符分页。
- 所有部分读取都明确显示范围、总量和下一页参数。
- 不改变工作区信任、风险审批、Plan 只读、检查点和 dangerous 明确确认边界。

## 上下文检查点

压缩输入首先包含由程序构造的权威任务契约：

- `PRIMARY GOAL`：活动任务直接使用最初提交的原文；手动压缩则从最近真实用户任务恢复。
- `ADDITIONAL REQUIREMENTS`：活动任务期间收到的全部 steering。
- `CURRENT PLAN`：任务计划管理器的当前快照。

模型只负责把历史证据整理为固定章节：当前进度、完成证据、关键决定、失败方法、文件与命令、下一步、验证状态和约束。最终上下文由“权威任务契约 + 模型检查点 + 继续规则”组成。因此，即使摘要质量下降，也不能覆盖主任务。

继续规则要求模型从 `Next action` 恢复；有完成证据的调查不应重做，除非证据缺失或已经失效。压缩事件仍写入项目会话 JSONL，因此关闭终端后 `/resume` 能加载同一检查点。

## 大文件窗口

`read_file` 有两种互斥使用方式：

- 行窗口：默认从 `start_line` 开始返回最多 200 行；未到文件结尾时提示下一 `start_line`。
- 字符窗口：提供零基 `start_character`，并可用 `max_characters` 控制窗口大小；适合单行 HTML、JSON 和压缩代码。

结果头部包含当前范围和文件总量，尾部包含下一偏移量。若行窗口本身仍过大，结果会提示切换到字符窗口。其他超长工具输出采用头尾保留、中间省略，便于同时看到命令开头和错误尾部。

## 参考实现取舍

Codex 的公开实现把压缩视为交接检查点，保留真实用户消息，并对工具输出做有界截断。Xiu 采用其“交接而不是泛化总结”的原则，同时结合 Xiu 已有的主任务契约和 steering 机制，增加程序层确定性字段。

Claude Code 的核心 CLI 未在公开仓库提供源码；可借鉴的是公开插件和变更记录中体现的阶段化任务、Todo/进度、停止钩子、部分文件读取和长工具心跳等产品模式，不能把这些模式误称为其核心源码实现。

## 验收标准

- 自动压缩后的下一次模型调用包含完整原始主任务、检查点下一步和附加要求。
- 手动 `/compact` 后保存的会话仍包含任务契约与摘要。
- 260 行文件默认只返回 1–200 行并提示 `start_line=201`。
- 超过 60,000 字符的单行文件可用字符偏移读取头部和尾部。
- 全部自动化测试、类型检查、构建和 npm 打包通过。
- 使用造成 v0.7.4 达到 Turn 上限的真实任务进行人工复测；在复测通过前只视为候选版本。

## 后续

v0.8 后续继续增量索引、Repository Map、符号/调用关系和混合检索。固定 30 Turn 上限暂时保留为安全边界；积累评测数据后再设计基于 Token、时间和实际进展的加权预算，避免简单提高上限掩盖停滞。
