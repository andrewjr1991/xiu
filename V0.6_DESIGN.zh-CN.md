# Xiu v0.6 设计：多 Agent、任务调度与 Worktree 隔离

## 1. 版本目标

v0.6 要让 Xiu 从单 Agent 循环升级为可观察、可恢复、受统一安全策略约束的多 Agent 工程系统。

成功不以“能同时发起几个模型请求”为标准，而以以下用户结果为标准：

- 主 Agent 能把一个目标拆成带依赖关系的任务图。
- 至少三个互不依赖的只读调查任务可以并行执行。
- Explorer、Implementer、Reviewer、Tester 使用独立上下文和独立统计。
- 写任务默认进入独立 Git Worktree，不覆盖主工作区或其他 Agent 的修改。
- 用户能看到每个 Agent 的角色、任务、状态、耗时和 Token。
- 单个 Agent 可以取消或重试，不影响同一运行中的其他 Agent。
- 终端关闭后，多 Agent 运行状态仍存在；未完成任务会标记为中断并可重试。
- 主 Agent 集成修改前必须展示 Diff；集成后仍使用原有完成门禁运行验证。

## 2. 非目标

以下能力不在 v0.6 承诺范围内：

- 全屏 TUI 和任务树固定面板（属于 v0.7）。
- 自动解决语义级合并冲突。
- 跨机器分布式 Agent。
- 自动删除包含修改的 Worktree。
- 子 Agent 绕过工作区信任、审批、Plan 模式或 dangerous 确认。
- 为追求并发而自动拆分所有简单任务。

## 3. 核心模型

### 3.1 角色

| 角色 | 默认模式 | 责任 |
| --- | --- | --- |
| Explorer | `shared_readonly` | 调查代码、定位文件、分析方案 |
| Reviewer | `shared_readonly` | 审查实现、风险和回归 |
| Tester | `shared_readonly` | 识别并运行安全的只读检查；需要执行时仍走审批 |
| Implementer | `worktree` | 在独立 Worktree 中修改代码并验证 |

角色只影响提示词和默认隔离方式，不扩大权限。

### 3.2 任务图

任务包含：稳定 ID、标题、详细指令、角色、依赖、执行模式、状态、预算、结果和统计。

状态机：

```text
pending -> running -> completed
                   -> failed
                   -> cancelled
running --进程退出--> interrupted -> pending（显式重试）
pending --依赖失败--> blocked
```

调度器只运行依赖全部完成的任务；无依赖任务受并发上限控制并行运行。循环依赖、重复 ID、缺失依赖在启动前拒绝。

### 3.3 安全继承

- 所有子 Agent 复用主 Agent 的审批回调。
- `shared_readonly` 只暴露风险为 `read` 的工具，并在 Agent 边界启用只读 Plan 模式。
- `worktree` Agent 使用同一工具集合，但所有路径被现有工作区边界限制在该 Worktree 内。
- 创建 Worktree、集成补丁和执行命令仍走审批。
- dangerous 操作永远不能被 `--yes` 自动批准。

## 4. Worktree 生命周期

1. 确认主工作区是 Git 仓库。
2. 在 `.xiu/worktrees/<run-id>/<task-id>` 创建独立 Worktree 和 `xiu/agent-...` 分支。
3. 子 Agent 仅在该目录工作。
4. 完成后保留 Worktree、分支、Diff 和状态，等待主 Agent 审查。
5. 集成时先生成 Diff，再执行 `git apply --check`；检查通过且用户批准后才应用到主工作区。
6. v0.6 不自动删除 Worktree。尤其是存在未集成修改时，必须保留以便恢复。

## 5. 交互与工具

主 Agent 获得以下编排工具：

- `spawn_agents`：校验任务图并启动后台调度。
- `list_agents`：读取运行和各任务状态。
- `wait_agents`：等待状态变化或运行结束并返回汇总。
- `cancel_agent`：只取消指定任务。
- `retry_agent`：重试 failed、cancelled、interrupted 或 blocked 任务。
- `integrate_agent`：审查并将指定 Worktree 的补丁安全应用到主工作区。

交互命令 `/agents` 展示已有运行；后续子命令提供详情、取消和重试。任务运行期间，状态变化通过现有事件输出，不要求用户盯着无反馈的终端。

## 6. 持久化与恢复

协调器把状态写入 `.xiu/agents/<run-id>.json`。文件包含任务图、状态、结果、错误、Worktree 信息、时间和统计，不包含 API Key。

Xiu 启动时加载这些文件：

- 已完成状态保持不变。
- 上次仍为 `running` 的任务改为 `interrupted`。
- 用户或主 Agent 可显式重试中断任务。
- Worktree 不自动清理，原修改仍可检查和集成。

## 7. 并发与预算

- 默认并发数为 3，可由配置覆盖并设硬上限。
- 每个 Agent 有独立 `maxTurns`，继承主模型与 Provider 配置。
- 每项任务记录模型调用、工具调用、输入/输出 Token 和耗时。
- 调度器通过 `AbortController` 和现有 `Agent.cancel()` 单独取消任务。
- 一个任务失败不会取消无依赖的其他任务；依赖它的任务进入 `blocked`。

## 8. 验收矩阵

| 场景 | 自动化证据 |
| --- | --- |
| 三个只读任务并行 | 峰值并发至少为 3，结果全部完成 |
| 依赖调度 | 下游任务只在依赖完成后启动 |
| 非法任务图 | 重复 ID、缺失依赖、环均被拒绝 |
| 单任务取消 | 目标变为 cancelled，其他任务继续 |
| 崩溃恢复 | running 状态重新加载为 interrupted，可重试 |
| 权限继承 | 只读 Agent 看不到写/执行工具 |
| Worktree 隔离 | 两个写 Agent 修改互不覆盖，主工作区不提前变化 |
| 安全集成 | `git apply --check` 失败时主工作区不改变 |
| 状态可见 | `/agents` 和工具返回角色、状态、耗时、Token |
| 完整回归 | typecheck、全部测试、build、pack dry-run 通过 |

## 9. 实施顺序

1. 先实现任务类型、图校验、状态存储和纯调度器测试。
2. 接入角色提示词、独立 Agent 实例、并发与取消。
3. 加入 Worktree 创建、Diff 和安全集成。
4. 接入主 Agent 工具与 `/agents` UI。
5. 补恢复、失败路径、文档和完整回归。
6. 验收全部通过后才把包版本升级为 `0.6.0`；发布 npm 仍需用户明确执行发布流程。
