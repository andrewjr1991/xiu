# Xiu v0.16.3 设计：安装路径与版本冲突诊断

## 目标

在 v0.16.2 只读分发诊断的基础上，解释“已经更新但运行的还是旧版”“同一台电脑存在多个 Xiu”“npm prefix 与 PATH 不一致”等常见安装问题，并给出可复制但不自动执行的处理建议。

## 用户能力

- `xiu --update-doctor`：无需进入交互会话即可检查运行时、当前包、命令解析链、npm prefix、更新代理、缓存和官方 npm Registry。
- `/update doctor`：在交互会话中运行同一套诊断。
- 诊断明确展示当前运行包、PATH 中最先命中的 `xiu` 启动器、对应包名与版本，以及可识别的其他安装位置。

## 命令解析检查

- 按 PATH 顺序检查直接可执行的 `xiu` 启动器。
- Windows 检查 `xiu.ps1`、`xiu.cmd`、`xiu.exe`、`xiu.bat` 和 `xiu`；其他平台检查 `xiu`。
- 将同一 npm 全局安装生成的多个 shim 按真实包根目录归组，避免把 `.ps1` 与 `.cmd` 误报为两份安装。
- 识别 PATH 首命中版本与当前运行版本不一致、多个真实安装、无法解析目标的旧或损坏 shim，以及显式 npm prefix 的命令目录未进入 PATH。
- 输出只提供排查顺序和可复制建议，不修改 PATH、npm prefix、shim 或全局安装。

## 判定模型

- Node.js 版本过低、当前发布包必需文件缺失或不安全的更新代理仍属于硬失败。
- PATH 缺少 Xiu、PATH 首命中旧版、重复安装、旧 shim 和 npm prefix 不一致属于警告，因为当前进程仍可能正常运行。
- Registry、DNS、代理出口或网络暂不可用仍属于外部警告。
- 只有本地硬失败令 `xiu --update-doctor` 返回非零退出码。

## 安全、隐私与性能边界

- 诊断只读，不运行 npm，不安装、卸载、降级、修复或重写任何文件。
- 不读取 npm Token、Provider Key、项目内容、会话内容或用户凭证。
- 最多检查 128 个 PATH 目录，不递归扫描磁盘或用户目录。
- 单个启动器与 `package.json` 最多读取 64 KiB；符号链接和包路径使用规范化真实路径归组。
- 普通 `xiu` 启动不执行 PATH 诊断，也不新增任何版本检查网络请求。

## 验收标准

- 同一 npm 安装的 `.ps1` 与 `.cmd` shim 只计为一个安装。
- PATH 首命中旧版本时明确指出实际命令来源、检测版本和当前运行版本。
- 真正的多安装、损坏 shim 和显式 npm prefix 未进入 PATH 均有确定性测试。
- 这些安装路径问题只产生警告，不把仍可运行的 Xiu 判为硬失败。
- 诊断全程不执行 npm、不修改 PATH、不修复 shim、不扫描项目内容。
- 类型检查、全量测试、构建、发包预览与干净安装验收全部通过。
