# Xiu v0.9.5：结构化 HTML、JSON 与 CSV 提取

## 目标

让 Xiu 在处理大型 HTML、JSON 和 CSV 时直接查询结构化数据，避免反复分页读取原文或临时编写 Python、Node、PowerShell 解析脚本，从而减少工具调用、失败命令和上下文 Token。

## 工具契约

### `extract_html`

- 输入工作区相对路径、CSS 根选择器、可选字段定义、偏移和数量上限。
- 每个字段可以从根节点或相对子节点提取 `text`、`html`、`outer_html` 或指定属性。
- 返回 JSON：匹配总数、本页范围、字段结果、截断信息和检测到的编码。
- 适合表格行、商品卡、对话 Hop、表单、链接和嵌入脚本片段。

### `extract_json`

- 使用标准 JSON Pointer（RFC 6901）选择对象、数组或标量。
- 对数组和对象支持偏移与分页；对象保持键名。
- 返回选中值类型、总项数、本页值、截断信息和检测到的编码。
- 解析失败时报告确定性位置，不把整份文件回显到终端。

### `extract_csv`

- 支持表头、无表头、自动/显式分隔符、列选择、精确条件过滤、偏移和分页。
- 正确处理引号、逗号、换行、BOM 和常见中文编码。
- 返回列名、过滤前后行数、本页记录、截断信息和检测到的编码。

## 共同边界

- 三项工具均为只读，Plan 模式可以使用，不触发写入或执行审批。
- 只允许访问工作区内普通文件；拒绝路径逃逸。
- 单文件默认上限 50 MB，避免一次性解析耗尽内存。
- 单次最多返回 100 条、20 个字段；单字段和总输出都有硬上限。
- 所有结果使用可解析 JSON，不输出不完整的 JSON 片段。
- 自动编码识别支持 UTF-8 BOM、UTF-16 BOM、有效 UTF-8；其他字节默认按 GB18030 解码，也允许显式指定 iconv-lite 支持的编码。
- HTML 解析使用 `cheerio`，CSV 使用 `csv-parse`；依赖版本必须支持 Node.js 20。

## Agent 行为

- HTML、JSON、CSV 需要结构化字段时优先使用对应提取工具。
- 只有工具契约无法表达任务时才使用 `read_file` 或执行自定义脚本。
- 分页结果必须根据 `matched_count`、`returned_count` 和 `next_offset` 决定是否继续，不能重复请求同一页。

## 验收标准

- HTML 能从多行及单行压缩文档中提取表格行、相对子元素、属性和中文文本。
- JSON 能解析根值、转义 Pointer、数组分页、对象分页，并清楚报告错误 Pointer。
- CSV 能处理 BOM、中文、引号内逗号和换行、列选择、过滤和无表头文件。
- 非法选择器、未知字段、路径逃逸、超大文件、错误编码和格式损坏均返回有界错误。
- 大结果始终返回完整有效 JSON，并带有下一页信息。
- 自动化测试、类型检查、构建、npm 打包和隔离安装全部通过。
