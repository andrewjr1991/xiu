# Xiu v0.15.5 设计：来源引用体验与搜索质量评测

## 目标

1. 同一网页不会因追踪参数或片段差异重复占用搜索结果。
2. 搜索发现与正文打开使用稳定来源标识，用户和模型能核对引用链。
3. Provider 返回的异常长文本在进入上下文前被清理和限制。
4. 用确定性覆盖指标暴露搜索结果的来源数量、域名多样性和日期可用性。
5. 对“最近一周”等时效请求执行程序级日期处理：优先范围内结果，来源提供日期时保留真实日期，来源完全无可核验日期时由程序标注“日期未知”，不能靠模型或记忆补足日期或条目。
6. 限制同一任务中失败的正文打开次数，避免在不可访问站点之间长时间空转。
7. 提供可发现、可持久化且立即生效的联网工具独立代理命令，不改变模型 Provider 代理。

## 非目标

- 不根据标题、排名或域名自动断言网页真实、权威或已验证。
- 不把搜索摘要升级为时效事实的完成证据。
- 不自动登录、执行 JavaScript、操作网页或扩大到浏览器自动化。
- 不改变现有 HTTPS、SSRF、域名策略、代理、认证和重定向边界。

## 设计

### URL 规范化与去重

搜索结果只保留无凭证的 HTTPS URL。规范化过程删除片段、常见 `utm_*` 与广告追踪参数，对剩余查询参数排序；安全策略和用户要求的域名过滤完成后，按规范化 URL 保留首次出现的结果。

### 稳定来源标识

规范化 URL 的 SHA-256 前八位形成 `WEB-xxxxxxxx`。`web_search` 与 `web_open` 均输出来源标识、精确引用 URL 和来源域名。标识只用于关联证据，不是可信度评分，也不替代最终 URL。

### 有界质量摘要

搜索结果末尾报告唯一来源数、唯一域名数、Provider 提供日期的结果数以及被折叠的重复数量。来源不少于两条但全部来自同一域名，或完全没有 Provider 日期时给出提示。指标不判断正文内容、日期真实性或时效范围。

### 证据门禁兼容

输出继续保留 `Results (N)`，`web_open` 继续输出 `URL:`，从而兼容既有搜索成功判断和最终 URL 打开门禁。时效任务必须逐条打开最终引用，不能以来源标识或质量摘要作为验证结果。

### 时效范围与失败预算

Xiu 从用户请求中解析“最近/过去 N 天、周、月、年”等滚动日期范围，并把它作为搜索与排序偏好，而不是任务完成的硬门禁。请求明确要求日期且答复引用网页时，来源提供可核验日期就保留真实日期；来源完全没有可核验日期时，由程序固定标注“日期：未知（来源未提供可核验日期）”。范围内证据不足时允许返回更早的已核验结果，但必须保留真实日期并清楚说明其位于首选范围之外；不得推断、伪造日期、事件或靠记忆补足数量，也不再发起让模型补日期的纠正轮次。

日期识别按最终回答中的引用结果块逐条执行，支持 Markdown 强调标签、中文年月日、ISO 日期以及标题括号日期。URL 路径中的数字不作为发布日期；每个保留的引用结果必须保留其明确真实日期，或在来源未提供可核验日期时带有程序生成的“日期未知”标记。模型不得自行推断日期，条目也不会仅因日期早于首选范围而被程序拒绝。

同一任务最多执行 3 次真实 `web_search` 搜索发现，每个 `web_open` 页面只真实尝试一次。尚无成功来源时最多累计 6 次真实打开失败；已有至少一个成功来源后，上限收紧为 4 次。达到上限后，程序进入不可逆的“联网证据收尾”状态：不再执行任何工具，并把唯一允许引用的精确 URL 清单交给模型，只允许基于这些已经成功打开的来源生成一次最终答复。答复可以少于用户要求的数量。若最终答复混入清单外 URL，程序按空行或顶层编号结果块确定性删除包含该 URL 的整个候选条目；只要仍保留至少一个已打开来源，并且每个引用条目带有真实日期或程序生成的“日期未知”标记，就返回缩减后的可靠结果。无法安全分离、没有已核验结果或模型继续请求工具时，任务立即以当前界面语言失败，不再发起纠正轮次。该预算只约束搜索发现与失败打开，不影响已经成功获取的证据。

## 验收标准

1. 追踪参数清理、查询参数排序、重复折叠和稳定 ID 有确定性测试。
2. 标题、摘要和日期有长度边界，不把 Provider 任意长文本灌入上下文。
3. 搜索输出包含覆盖指标和 discovery-only 提示；打开输出能复现同一来源 ID。
4. 现有域名策略、SSRF、防凭证跳转、托管认证和时效证据门禁测试无回归。
5. “最近一周”等请求优先使用范围内结果；范围内不足时，明确标注真实日期的更早来源可以完成任务；来源无日期时由程序确定性标注未知，模型不得补写或推断日期。
6. 真实搜索发现不超过 3 次；不可访问页面在无成功来源时最多失败 6 次、已有成功来源时最多失败 4 次，不能靠反复换关键词或站点拖延任务。
7. 最终回答同时包含已打开和未打开来源时，未打开来源所在的完整结果块被移除，剩余已核验结果可正常完成；不能把坏 URL 单独删掉而保留其无来源事实陈述。
8. `/web proxy set [URL]`、`/web proxy clear` 与 `/web proxy` 可通过命令面板发现；设置立即应用于搜索、网页打开和托管认证，重启后保留，且不修改模型 Provider 代理。
9. 类型检查、完整测试、构建、包预览和秘密扫描全部通过。
