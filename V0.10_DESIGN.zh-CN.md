# Xiu v0.10 Provider 架构设计与验收

## 目标

v0.10.0 建立可继续扩展的 Provider 基础层，让用户能在同一套会话、工具与安全边界中使用云端模型、本地模型和自定义 OpenAI-compatible 服务。

本版本完成第一阶段闭环：Provider 配置、连接、模型发现、运行时切换和会话恢复。自动按任务路由、多级故障降级、速度/质量/价格策略和缓存留到后续 v0.10.x；界面不得把尚未实现的策略描述成已支持。

## 配置模型

用户级文件为 `~/.xiu/providers.json`。文件采用版本化结构：

```json
{
  "version": 1,
  "active": "office-gateway",
  "profiles": [
    {
      "id": "office-gateway",
      "name": "Office Gateway",
      "kind": "openai-compatible",
      "model": "coder-model",
      "baseURL": "https://models.example.com/v1",
      "apiKeyEnv": "OFFICE_MODEL_KEY",
      "contextWindow": 128000,
      "features": {
        "text": true,
        "tools": true,
        "vision": false,
        "image": false,
        "video": false
      }
    }
  ],
  "credentials": {
    "office-gateway": "用户选择保存在本机的 Key"
  }
}
```

配置支持两种凭证方式：`apiKeyEnv` 保存环境变量名；`credentials` 保存由用户明确选择的本机明文 Key。凭证与可共享的 Profile 元数据分开存放。写入使用临时文件和原子替换，并在支持的平台请求 `0600` 文件模式。ID、URL、环境变量名、凭证长度、上下文窗口、数量和长度均须验证；内置 Provider 不可覆盖或删除。

直接保存 Key 更方便，但不是加密保险箱。同一系统账号、管理员或恶意软件仍可能读取它，因此整个 `providers.json` 不得提交到 Git、同步到公共位置或发送给别人。环境变量值优先于同一 Profile 的本地保存值。

## 内置 Provider

| ID | 类型 | 默认地址 | 密钥 |
| --- | --- | --- | --- |
| `agnes` | Agnes/OpenAI-compatible | `https://apihub.agnes-ai.com/v1` | `AGNES_API_KEY` |
| `openai` | OpenAI | SDK 默认 | `OPENAI_API_KEY` |
| `anthropic` | Anthropic | SDK 默认 | `ANTHROPIC_API_KEY` |
| `ollama` | 本地 OpenAI-compatible | `http://127.0.0.1:11434/v1` | 不要求 |
| `lmstudio` | 本地 OpenAI-compatible | `http://127.0.0.1:1234/v1` | 不要求 |
| `vllm` | 本地 OpenAI-compatible | `http://127.0.0.1:8000/v1` | 不要求 |

本地预设没有假定用户一定安装了某个具体模型。首次切换会请求 `/v1/models`；默认占位模型为 `local-model` 时，使用发现到的第一个模型。用户之后可通过 `/models` 明确选择。

## 配置优先级

Provider ID 按以下顺序决定：

1. 命令行 `--provider <profile-id>`。
2. 环境变量 `XIU_PROVIDER`。
3. `~/.xiu/providers.json` 中的 `active`。
4. 默认 `openai`。

模型、Base URL、代理和上下文窗口仍允许命令行显式覆盖 Profile。恢复会话时，如果用户没有通过命令行或环境变量强制指定 Provider，则恢复会话记录的 Provider ID 和模型。

## 交互命令

- `/providers`：列出内置和自定义 Profile，显示类型、默认模型、能力与地址；选择后先测试连接，再切换并设为默认。
- `/provider test`：调用当前 Provider 的模型列表接口，报告连接结果和发现数量。
- `/provider add`：创建自定义 OpenAI-compatible Profile；支持环境变量名或隐藏输入的本机 Key。
- `/provider edit`：逐项编辑自定义 Profile；空输入保留原值，本地 Key 默认保留，修改后重新测试连接。
- `/provider key`：使用隐藏输入为任意内置或自定义 Provider 保存本机 Key。
- `/provider remove`：删除非当前的自定义 Profile；内置 Profile 不可删除。
- `/models`：从当前 Provider 在线发现模型，并显示能力、上下文、Provider 来源和发现来源。

连接探测优先调用模型列表接口；若兼容服务没有实现模型发现，则回退到一次最小聊天请求。只有两种方式均失败时才判定连接失败。

## 能力与安全边界

- Profile 明确声明文本、工具、视觉、图片和视频能力。
- 没有声明工具能力时，不向模型发送工具定义。
- 没有声明视觉能力时，不注册图片分析工具。
- 目前图片和视频生成后端只对已适配的 Agnes 能力开放；自定义兼容端点不会因名称相似而获得生成能力。
- Provider 切换不会清空对话消息、计划、检查点或项目索引，只替换模型适配器、模型元数据和与能力相关的工具。
- 工作区信任、Plan 模式、风险审批、危险操作确认和验证门禁均不由 Provider 控制。

## 验收标准

- [x] 内置 OpenAI、Anthropic、Agnes、Ollama、LM Studio、vLLM Profile 可列出。
- [x] 自定义 OpenAI-compatible Profile 可持久保存、切换和删除。
- [x] 用户可选择环境变量或本机持久 Key；Key 输入不回显。
- [x] 项目会话日志、状态界面和 Provider 列表不输出 API Key 值。
- [x] 本地 Profile 在没有云端 Key 时可创建客户端并测试本地服务。
- [x] 切换前执行连接测试，失败时保留当前 Provider。
- [x] `/models` 展示能力、上下文、Provider 和来源。
- [x] 会话保存并恢复 Provider ID；用户显式覆盖时尊重覆盖。
- [x] 不支持的工具和多模态能力不向模型暴露。
- [ ] 在 Ollama、LM Studio、vLLM 的真实本地实例上完成人工编码任务验收。
- [ ] Provider 自动降级、按步骤路由、成本策略和请求缓存进入后续 v0.10.x。
