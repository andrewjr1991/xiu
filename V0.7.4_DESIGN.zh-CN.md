# Xiu v0.7.4：面向进展的循环保护

## 一、真实失败

v0.7.3 的真实长任务复测已能保持原始预标注目标，但循环保护在解析两个约 1.4 MB 的 HTML 时误杀任务。记录中对规则文件、HTML、助手代码和项目状态的读取相隔着大量不同操作，甚至发生了上下文压缩，仍因“整场任务累计到第三次”而被阻断。

旧算法维护每个工具签名在整个 `Agent.run()` 中的永久计数：

```text
read rules -> many new calls -> read rules -> compaction -> read rules = loop
```

这不是循环，而是长任务重新建立证据上下文的合理行为。

## 二、目标

1. 只阻断近期连续、没有有效新进展的重复行为。
2. 允许长任务在不同调查阶段重新读取相同文件或状态。
3. 保留对连续相同调用和短周期工具循环的保护。
4. 上下文压缩和真实写入后不继承过时的循环证据。

## 三、新判定

### 连续相同调用

只有最近三个签名完全相同时才阻断：

```text
A A A
```

`A B C A D A` 不属于循环。

### 短周期循环

继续检测长度 1 至 4、连续重复三轮的尾部模式，例如：

```text
A B A B A B
A B C A B C A B C
```

### 阻断衰减

一次阻断后，如果 Agent 连续完成四个非循环调用，旧阻断次数清零。互不相关的两次局部卡顿不会在很久以后叠加成任务终止。

### 进展重置

以下事件会完全重置循环历史：

- 自动上下文压缩完成。
- 成功执行会修改工作区的工具。

## 四、验收标准

- 同一文件被读取三次但中间存在不同有效调用时不阻断。
- 连续第三次相同调用仍被阻断。
- `ABABAB` 等短周期循环仍能在最大 Turn 前终止。
- 重置后旧调用不参与新循环判定。
- 主目标保持、任务契约审计、安全审批、验证门和失败恢复不退化。
- typecheck、全部测试、build、npm 审计、pack 和隔离安装通过。
