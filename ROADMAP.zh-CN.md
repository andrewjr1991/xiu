# Xiu 产品与工程路线图

> 本文档是 Xiu 的唯一版本规划与交接入口。历史实现细节保存在 Git 历史，不再为每个补丁版本保留独立设计稿。

## 1. 项目定位

Xiu 是由静然开发的自主终端编码助手，目标是在真实项目中可靠地完成“理解、修改、验证、恢复和交付”，同时让用户始终看得见进展、掌握权限并能追回执行依据。

固定信息：

| 项目 | 当前值 |
| --- | --- |
| npm 包 | `@xiu-ai/cli` |
| 全局命令 | `xiu` |
| 当前公开版本 | `0.14.1`，插件 API 阶段 A、B 已发布 |
| 本地开发版本 | `0.14.3`，插件供应链阶段 D3 候选版，尚待发布 |
| 主要运行时 | Node.js 20+ / TypeScript |
| 重点平台 | Windows PowerShell |

## 2. 当前产品基线

截至 `0.13.8`，已发布能力包括：

- 交互式终端 UI、中英文切换、命令面板、文件与图片引用、进度详情和文件 Diff。
- 工作区信任、Plan 只读模式、风险审批、危险操作确认、检查点和验证门禁。
- 项目索引、Repository Map、符号定义/引用/调用检索以及 HTML/JSON/CSV 结构化提取。
- 项目会话隔离、恢复、历史、上下文压缩、Token/耗时/失败与停滞诊断。
- OpenAI、Anthropic、OpenAI-compatible、本地模型和 Agnes 等 Provider 架构；能力探测、持久选择、阶段路由与安全故障转移。
- 文本、视觉、图片和视频模型路由。
- Skills 与 MCP 工具、Resources、Prompts、Streamable HTTP、stdio、OAuth、权限清单和扩权确认。
- Provider/MCP 凭证的兼容存储、可选系统凭证库、显式迁移、回退、清理、注销和统一脱敏。
- 多 Agent 基础调度、角色、独立 Worktree 和诊断记录。

仍需外部环境验证：

- Windows ARM64。
- 另一台受企业策略约束的 Windows 机器。
- 真实 Cloudflare OAuth 凭证迁移、重启读取与注销闭环。

这些验证完成前，Windows 系统凭证后端保持显式选择，不设为默认。

## 3. 当前版本：v0.13 可靠执行与无人值守长任务

### 3.1 目标

v0.13 的核心不是增加更多按钮，而是让长任务在断网、限流、工具失败、终端关闭、进程崩溃和多 Agent 并发修改下仍能安全继续，并给出可复查的执行报告。

### 3.2 已发布的 v0.13.0：审计事实基础

`0.13.0` 已发布隐私优先的安全审计账本：

- 本机版本化 JSONL 账本，默认位于 `~/.xiu/security-audit.jsonl`。
- 审批与 Provider/MCP 凭证生命周期事件。
- `/audit`、`/audit approvals` 和 `/audit credentials`。
- 进程内串行写入、损坏行隔离、2 MiB 有界轮转和符号链接拒绝。
- 不记录命令正文、工具参数、Prompt、模型回复、文件内容、完整路径或凭证。
- 333 项完整测试连续复跑通过；类型检查、构建和 npm 打包预览通过。

这部分是“完整执行报告”和恢复诊断的底层事实来源，不代表 v0.13 的可靠执行阶段已经完成。

### 3.3 实施阶段

#### v0.13.1 / 阶段 A：任务运行日志与崩溃恢复

- 已为任务、模型轮次、工具调用、验证、检查点和用户 steering 建立稳定 ID。
- 已在用户目录按工作区不可逆摘要隔离版本化运行记录，并在安全边界原子更新，不依赖模型回忆。
- 启动时会识别异常中断任务，展示恢复点与未决副作用，并要求用户明确选择恢复、放弃或退出。
- 已成功和状态未知的副作用操作均不会自动重放；恢复任务先核验文件、Git、进程或远端状态。
- `/recover` 可在交互会话中重新检查并处理异常中断任务；日志损坏、版本未知或不可写时安全失败。

验收：在模型等待、工具执行前后、文件修改后和验证期间强制终止进程，重启后均能解释状态并从安全边界继续。

#### v0.13.2 / 阶段 B：统一重试与副作用边界

- 已让模型、工具、MCP 和媒体调用使用统一错误分类与重试决策。
- 已限制只有明确瞬时且可安全重放的操作才能按服务端提示或指数退避进行最多三次尝试。
- 工具声明显式重放契约；只读 MCP 可安全恢复，写入、命令、外部修改和媒体提交均按副作用操作处理。
- 已输出流式内容、已完成工具副作用或结果不确定时禁止静默重放；媒体状态查询和已有资源下载可安全恢复，但不会重新提交付费生成。
- 已覆盖认证、参数、权限、取消、限流、超时、网络错误、5xx、流式输出与未知提交状态的回归测试。

验收：认证、参数、权限和用户取消不会误重试；限流、超时和服务端瞬时错误按策略有界恢复。

#### v0.13.3 / 阶段 C：任务预算、超时和停滞控制

- 已支持任务级 Token、模型调用、工具调用、失败次数和墙钟时间预算；默认不限制，必须由用户显式配置。
- 已区分工作中、等待用户、等待审批、后台进程、限流退避和真正停滞，等待状态不会被误判为卡死。
- 接近预算时只预警一次；触达预算后在模型与工具之间的安全边界暂停、保留恢复记录且不伪装完成。
- `/diagnostics` 和状态栏读取同一份预算与停滞快照；停滞阈值可配置。

验收：预算可预测、可解释、可恢复，不因等待用户而误判停滞。

#### v0.13.4 / 阶段 D：后台任务与断线续跑

- 已将进程内后台 Map 改为独立守护进程和用户目录中的工作区隔离状态；正常退出 Xiu 不再取消后台任务。
- `/background start|list|read|cancel` 支持稳定 ID、增量输出游标、状态发现、退出证据和显式取消；模型后台工具读取同一事实。
- 长 Xiu 任务可通过后台命令运行，使用同一工作区和 Provider/凭证配置，并可显式携带任务预算；无交互时遇到新审批会在操作前安全暂停并写入 `/recover` 证据，危险后台命令启动前还要求强化确认。
- 请求文件仅用于守护进程启动握手并立即删除；持久记录只保存脱敏截断的命令预览，后台输出在写盘前统一脱敏。

验收：终端关闭再打开后能继续任务，不丢失输出、不重复副作用、不绕过审批。

#### v0.13.5 / 阶段 E：完整执行报告

- 已从任务事件、精确会话回放、诊断和当前工作区安全审计账本生成有界报告。
- 已包含目标、阶段、文件变化、验证证据、失败与重试、预算、未完成项和恢复点。
- `/report` 只预览默认脱敏摘要；Markdown/JSON 导出路径和 `summary|details` 内容范围由用户明确选择。
- 同一原始目标的失败、未验证与继续运行按任务链聚合，保留原始目标、累计文件变化和最终验证证据。
- 报告不包含思维链、完整源码、Prompt、凭证或安全审计主体；未知副作用只报告、不重放。

验收：报告可独立回答“做了什么、为什么、验证了什么、失败在哪里、能否继续”。

#### v0.13.6：安全修复

- 一次性任务与 `-y` 不再绕过工作区信任；确认前不读取项目指令、不加载项目 Skills/MCP。
- 所有工作区文件入口按真实路径约束，拒绝符号链接、Junction、重解析点和 Glob 越界。
- 新 MCP 权限清单首次使用必须显式批准，不再因没有历史记录而自动放行。
- 子 Agent 取消/重试按有副作用执行操作审批且禁止自动重放；持久日志权限和常见秘密脱敏得到加强。
验收：拒绝信任时没有项目内容泄漏或模型调用；路径逃逸和首次 MCP 自动授权均有真实回归测试。

#### v0.13.7：首次启动修复

- 新安装缺少当前 Provider API Key 时不再在交互界面出现前退出。
- Xiu 进入配置模式，提供当前 Key、其他 Provider 或稍后配置三种选择，并保留 `/provider key` 与 `/providers` 命令。
- 一次性任务缺少凭证时输出明确配置说明，不显示未处理异常堆栈。

验收：在隔离用户目录且没有任何 API Key 的环境中，交互启动仍能进入 `xiu>` 并正常退出。

#### v0.13.8 / 阶段 F：多 Agent 冲突检测与安全合并

- 已在合并前检测主工作区与 Agent Worktree 的文件、符号、依赖清单和 Git 补丁冲突。
- 已保留未提交修改、补丁、未验证结果和冲突 Worktree；失败合并不会自动清理或部分覆盖。
- Reviewer/Tester 会在目标实现 Worktree 上只读检查，并必须以 `VERDICT: PASS` 提供证据；主 Agent 或用户仍需明确确认合并。
- 已将分析、阻断项、证据和合并状态写入多 Agent 运行记录，并提供有界补丁预览。
- 已阻止运行期空回车形成重复 `补充>`，并区分 Agent Worktree 的只读 Git/PowerShell 检查与真实工作区修改，避免验证通过后被误判为未验证。

验收：冲突不会静默覆盖用户或其他 Agent 的修改，失败合并可恢复。

完整设计与阶段约束见 `V0.13_DESIGN.zh-CN.md`。

## 4. 当前版本：v0.14 扩展生态与分发

### 4.1 目标与边界

v0.14 建立可版本化、可审计、可撤销的扩展边界。插件可以逐步贡献 Provider、Tool、Skill 和工作流，但不能绕过工作区信任、Plan 只读、风险审批、权限清单或危险操作确认。

非目标：不在首个阶段执行任意第三方 JavaScript，不静默联网安装，不将项目插件提升为用户级，不把“清单可读取”误报为“插件已激活”。

### 4.2 实施阶段

#### v0.14.0 / 阶段 A：版本化清单与安全发现

- 定义 `apiVersion: 1` 的 `xiu.plugin.json`，包含稳定 ID、语义版本、Xiu 兼容范围、权限声明以及 Provider、Tool、Skill、工作流贡献项路径。
- 扫描 `~/.xiu/plugins/*/xiu.plugin.json`；只有工作区已信任后才扫描 `<项目>/.xiu/plugins/*/xiu.plugin.json`。
- 拒绝绝对路径、父目录穿越、符号链接/Junction 逃逸、未知权限、过大清单、过多贡献项和不支持的 API 版本。
- 项目级同 ID 插件显式遮蔽用户级插件并报告；不静默合并两个来源。
- 插件采用按需扫描，不增加普通启动路径的磁盘遍历；`/plugins` 清点状态，`/plugins reload` 重新扫描，`/plugin inspect <id>` 展示来源、兼容性、权限、贡献项与失败原因。
- 本阶段只校验声明，不执行插件代码、不注册贡献项。

验收：不可信项目无法影响插件清单；恶意路径和权限扩张失败关闭；无插件环境启动行为不变；清单状态可由命令复查。

#### v0.14.1 / 阶段 B：声明式贡献与精确授权

- 在精确权限确认后加载声明式 Provider、MCP Tool、Skill 和工作流贡献。
- 每类贡献使用现有安全注册表与审批边界，不新增旁路执行器。
- 清单或权限变化使旧授权失效，必须重新确认。
- `/plugin approve <id>` 只授权当前版本、权限、贡献路径和内容摘要的指纹；`/plugins reload` 重新校验并加载已授权贡献。
- Provider 和 MCP Tool 只接受有界 JSON；Skill 和工作流只接受有界 Markdown。插件 JavaScript、安装脚本和任意可执行入口不会运行。
- Provider 不能携带明文 Key；只能声明环境变量引用。MCP Tool 继续受 MCP 自身权限清单、风险审批和 Plan 只读约束。

验收：未授权插件保持 inactive；授权后四类声明式贡献可被现有注册表发现；版本、权限、贡献路径或内容变化立即撤销激活；无插件环境不引入可感知启动回归。

#### v0.14.2 / 阶段 C：安装、更新、禁用与卸载恢复

- 可信本地路径和 HTTPS Git 来源安装；显示来源与版本。
- 更新前预览权限变化并保留可恢复备份。
- 禁用和卸载不删除用户生成的数据，失败时原子回退。
- `/plugin install` 先将包复制到同盘暂存目录，拒绝符号链接、Junction、非 HTTPS 远端、URL 内嵌凭证和超限包，完整校验后才原子安装。
- `/plugin update` 只使用安装时记录的来源，显示旧版、新版及新增/移除权限；替换后旧精确授权失效，必须重新执行 `/plugin approve`。
- `/plugin disable|enable` 使用本地状态标记，不删除插件或用户数据；`/plugin uninstall` 将包移动到备份，`/plugin recover` 恢复最近备份。
- 安装、更新和卸载均不执行插件 JavaScript、Git Hook、安装脚本或二进制入口；失败更新保留原版本。

验收：本地安装、权限扩张更新、禁用/启用、可恢复卸载、损坏更新和不安全来源均有确定性回归测试；阶段 B 的精确授权及全部永久安全边界不退化。

#### v0.14.3 / 阶段 D：签名、团队配置与供应链门禁

- 来源摘要、可选签名验证、锁定版本与团队共享声明。
- 团队配置只能收紧或建议，不能绕过本地信任和审批。
- 建立恶意包、篡改更新、依赖混淆和回滚失败测试矩阵。
- D1 已实现确定性完整包 SHA-256、HTTPS Git 精确提交记录、旧安装兼容状态和安装后篡改失败关闭；本地禁用标记不进入包摘要。
- D2 已实现可选 Ed25519 分离签名、可信发布者指纹与显式信任/撤销命令；无效签名失败关闭，发布者信任不绕过当前包的精确审批，未签名插件保持本地兼容。
- D3 已实现版本化 `xiu.plugin-policy.json`、只收紧的签名/来源/发布者/权限门禁，以及恶意包、依赖混淆、签名替换、策略竞态和回滚失败矩阵；团队策略不能写入本机信任或授权。

## 4.3 后续版本：v0.15 原生联网搜索

- 当前没有统一的内置 Web Search 工具；现阶段可通过具备搜索能力的 MCP、Provider 原生能力或经审批的外部命令间接联网。
- v0.15 将提供只读的原生 Web Search Provider，统一查询、打开页面、引用来源、域名策略、代理、超时、重试和隐私提示。
- 搜索结果始终视为不可信外部内容，不得作为系统指令执行；涉及登录、写入或账号操作时仍走 MCP/OAuth 和独立审批。
- v0.15 在 v0.14 插件授权、安装恢复和供应链门禁闭环后启动，避免搜索能力绕过统一扩展安全模型。

## 5. v1.0 稳定产品化门槛

- Windows、macOS、Linux 的安装和升级流程稳定。
- 崩溃恢复通过压力测试。
- Provider、Tool、Skill、MCP 和插件 API 有兼容承诺。
- 自动更新、版本回滚和供应链验证完善。
- 完整中英文文档与首次配置向导。
- 真实项目固定评测无明显质量或成本退化。
- 隐私、安全和诊断上传策略清晰且默认保守。

## 6. 永久安全边界

- 不绕过工作区信任、Plan 只读、风险审批、检查点和危险操作确认。
- 不静默扩大 MCP、Skill、插件或子 Agent 权限。
- 不默认上传代码、会话、审计或诊断数据。
- 不在不确定副作用后自动重放操作。
- 不把 Key、Token、Client Secret、Cookie 或 Authorization 写入日志、会话或错误。
- 不在测试失败或外部验收缺失时宣称能力已经稳定。

详细边界见 `SECURITY.zh-CN.md`。

## 7. 发布与验证规则

每个版本必须：

1. 明确目标、非目标、风险和验收标准。
2. 补充失败场景测试，再实现最小闭环。
3. 运行 `npm run typecheck`、`npm test`、`npm run build` 和 `npm pack --dry-run`。
4. 在干净目录安装包并验证 `xiu --version`。
5. 不覆盖已经发布的 npm 版本。
6. 发布后从官方 Registry 回读版本和 `latest`。
7. 同步 README、使用说明、发布说明和本路线图。

## 8. 持续评测指标

- 端到端任务成功率和首次修改正确率。
- 自动测试通过率及无关文件修改率。
- 重复失败率、重试成功率和未知副作用次数。
- Token、费用、完成时间与首字延迟。
- 中断、崩溃和关闭终端后的恢复成功率。
- 审批拒绝率、权限扩张率和秘密泄漏回归。
- 大型项目检索命中率。
- 多 Agent 冲突率与安全合并成功率。

## 9. 下一步

如果没有更高优先级的 Bug 或安全问题：

1. npm 官方 Registry 已确认 `0.14.1` 已发布，不得覆盖该版本。
2. `0.14.2` 的安装、更新、禁用、卸载与恢复闭环已完成本地验收；发布状态仍须由 npm Registry 回读确认。
3. `0.14.3` D1/D2/D3 已完成本地实现：包锁、Ed25519 签名、发布者身份、团队只收紧策略和供应链攻击矩阵均已闭环。
4. `0.14.3` 已通过类型检查、全量测试、构建、打包检查和干净目录安装验收；下一步由用户进行终端外部验收，确认后再提交、推送和发布，不覆盖任何已发布版本。
5. `0.14.3` 发布并从官方 Registry 回读确认后，关闭 v0.14，启动 v0.15 原生只读联网搜索。
