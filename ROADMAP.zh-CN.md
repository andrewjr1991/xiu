# Xiu 产品与工程路线图

本文档是 Xiu 的长期规划和跨会话项目记忆。开始新版本、重大功能或架构调整前，应先完整阅读本文档。完成里程碑后，要同步更新状态、实际结果和下一步，不能只依赖聊天记录。

## 一、长期目标

把 Xiu 打造成世界级终端编码 Agent：不仅功能丰富，而且在真实项目中更可靠、更清楚、更安全、更容易扩展。

长期优化围绕五个核心指标：

1. 任务完成率。
2. 修改正确率。
3. 大型项目理解能力。
4. 交互体验和过程可见性。
5. 安全、审批和可恢复性。

不以“功能数量”作为主要成功标准。任何新增能力都必须有验收标准、自动化测试和失败恢复方案。

## 二、项目固定信息

| 项目 | 当前值 |
| --- | --- |
| 产品名称 | Xiu |
| npm 包 | `@xiu-ai/cli` |
| 全局命令 | `xiu` |
| npm 组织 scope | `xiu-ai` |
| 公开 npm 页面 | `https://www.npmjs.com/package/@xiu-ai/cli` |
| 当前已发布版本 | `0.11.2` |
| 主要运行时 | Node.js 20+ / TypeScript |
| 当前重点平台 | Windows PowerShell |

API Key、Token 和个人代理凭证不得写入本文档或提交到项目。

## 三、当前状态（2026-08-10）

### 已发布的 v0.6.0

当前 npm 已公开发布 `@xiu-ai/cli@0.6.0`，包括 v0.5 的全部能力以及多 Agent、任务调度和 Worktree 隔离。

- OpenAI、Anthropic、Agnes Provider。
- Agnes 文本、视觉、图片和视频能力路由。
- 文件、补丁、Shell、Git、项目检测和验证工具。
- 风险分级审批与危险操作确认。
- 工作区信任。
- 交互式终端和 Slash 命令菜单。
- 会话保存、恢复、历史和自动上下文压缩。
- 项目索引、相关代码检索和技术栈识别。
- Token、耗时和调用统计。
- 流式输出和模型重试。
- 任务计划与只读 Plan 模式。
- Diff、检查点和确认回退。
- Skills 发现、浏览和本地/HTTPS Git 安装。
- stdio MCP、风险映射、热重载和取消传播。
- 响应式启动页和输入框状态栏。
- 多 Agent 任务图、依赖调度和默认 3 路并发。
- Explorer、Implementer、Reviewer、Tester 角色及独立上下文与统计。
- 只读共享工作区的双重权限边界。
- Implementer Git Worktree 隔离、Diff 预览和冲突检查集成。
- `/agents` 状态、单任务取消、重试、持久化与中断恢复。

### 已发布的 v0.7.0

npm 官方 Registry 的 `latest` 已确认是 `@xiu-ai/cli@0.7.0`。原 `0.6.1` 品牌身份补丁已经并入 `0.7.0`：系统提示和文档明确声明 Xiu 由“静然”开发，并禁止把模型 Provider 或其他公司误认为 Xiu 的开发者。

- 身份规则回归测试确保 Provider 不会再次覆盖 Xiu 的开发者声明。
- 多行 Unicode 输入、任意位置光标编辑和窄终端自动换行。
- `@` 项目文件补全、`Ctrl+R` 历史搜索和项目草稿恢复。
- Resize 重排和严格物理行宽测试。
- 默认拒绝的方向键审批菜单。
- 有界活动摘要与 `/details` 完整输出浏览。
- 状态栏展示 Plan 阶段、Agent、MCP、后台任务和上下文。
- v0.7 架构与验收设计文档 `V0.7_DESIGN.zh-CN.md`。

### 本地 v0.7.1 候选版本（尚未发布）

v0.7.1 聚焦解决长任务运行时无法继续输入的问题：

- Agent 运行期间继续显示复用 v0.7 编辑能力的 `xiu[working]>` 输入框。
- 普通输入进入最多 20 项的有界后续任务队列，并按提交顺序在同一会话执行。
- `/queue`、`/clear-queue`、`/cancel` 和 `/exit` 提供即时控制。
- Ctrl+C 取消当前任务但保留队列；退出会清空队列，防止退出后继续修改。
- 状态栏动态显示当前阶段和排队数量。
- Agent 输出使用有界缓冲，审批时安全挂起输入框，保持默认拒绝和原有风险边界。
- 运行期未提交草稿继续恢复；尚未开始的队列项暂不声明跨进程持久化。
- 设计与验收记录在 `V0.7.1_DESIGN.zh-CN.md`。

真实 Windows 长任务验收发现 v0.7.1 存在发布阻断问题：运行进展不可见、成功工具调用可以反复循环、普通补充被当成独立任务、失败后队列自动继续，以及未验证修改仍显示绿色完成。因此 v0.7.1 不作为稳定候选发布；修复合并到 v0.7.2。

### 本地 v0.7.2 候选版本（尚未发布）

- 状态栏显示 Turn/上限、阶段、耗时、steering、显式队列和最近工具活动。
- `Ctrl+O` 与运行期 `/details` 展开或收起最近八条实时活动。
- 普通运行期输入注入当前目标；只有 `/queue <task>` 安排独立下一项任务。
- 相同工具调用和短调用周期第三次重复时阻断，三次循环阻断后提前终止。
- 失败、取消或未验证后暂停，默认停止并让用户选择是否重试或继续显式队列。
- Agent 结果显式区分 completed、unverified、failed 和 cancelled。
- 修改文件但验证未通过时显示 `Stopped unverified`；Implementer 子 Agent 也不能把未验证修改报告为完成。
- 设计与真实失败回归记录在 `V0.7.2_DESIGN.zh-CN.md`。

真实闪购任务复测确认 v0.7.2 的实时进展、Ctrl+O 和 steering 传递正常，但模型可能把较新的 steering 当成替代目标，只完成建议类补充并忽略原始交付物，随后错误显示 `Done`。因此 v0.7.2 不作为稳定候选发布；修复合并到 v0.7.3。

### 本地 v0.7.3 候选版本（尚未发布）

- 每次运行固定不可被 steering 覆盖的 PRIMARY GOAL。
- steering 明确作为附加要求注入，同时重复展示原始主目标。
- 有 steering 的任务首次尝试结束时触发任务契约审计，逐项检查主目标和全部补充要求；未完成则继续调用工具。
- 系统提示明确禁止用较新或较容易的补充要求替代主目标。
- 明确 HTML、Markdown、JSON 和源代码应作为文本读取，不能直接传给图片分析工具。
- 真实失败场景已加入自动化回归测试。
- 设计记录在 `V0.7.3_DESIGN.zh-CN.md`。

真实长任务复测确认 v0.7.3 能保持主目标，但暴露循环保护误杀：同一文件或项目状态在整场任务中第三次出现，即使中间已有大量不同工具证据或发生上下文压缩，也会被累计为循环。该问题会阻断大型文件解析，因此 v0.7.3 不作为稳定候选发布；修复合并到 v0.7.4。

### 本地 v0.7.4 候选版本（尚未发布）

- 相同调用只在近期连续出现三次时判定为重复，不再使用整场任务累计次数。
- `ABABAB` 等长度 1 至 4 的近期短周期循环仍会被识别。
- 连续获得四个非循环工具结果后，旧的阻断次数衰减清零。
- 自动上下文压缩和成功工作区修改会重置循环历史。
- 增加“多次读取同一规则但中间存在有效新证据”的真实长任务回归测试。
- 设计记录在 `V0.7.4_DESIGN.zh-CN.md`。

真实闪购长任务复测确认 v0.7.4 已消除循环保护误杀，但任务在多次自动压缩后反复重新调查规则和大 HTML，最终触发 30 Turn 安全上限。根因是旧压缩只保存模型自由摘要，没有确定性保留主任务、steering、计划和下一步；同时 `read_file` 默认整文件读取，不适合百万字符的单行 HTML。该发布阻断问题优先于 AST 索引，修复进入 v0.8.0。

### 已发布的 v0.8.0

- 上下文压缩升级为结构化检查点，包含进度、完成证据、决定、失败方法、文件与命令、下一步、验证状态和约束。
- 主任务、steering 和当前计划由程序确定性写入权威任务契约，不依赖模型摘要记忆。
- 压缩后明确要求从下一步继续，不重复已有证据支持的调查。
- `read_file` 默认按 200 行分页并返回明确的下一页参数。
- 单行或压缩 HTML/JSON 支持 `start_character` 与 `max_characters` 字符窗口。
- 过长通用工具输出保留头尾并标明中间省略，不再只保留开头。
- 主 Agent 默认取消固定 Turn 上限：只显示当前轮次并持续到完成、用户取消、真实循环或明确失败；`--max-turns` 仅作为用户主动设置的可选上限。
- 第二次真实闪购复测成功运行 103 轮、276 次工具调用并完成验证，证明无限轮次和任务契约有效；但 16 次压缩、约 197 万输入 Token、128 次文件读取和 26 次命令失败暴露效率问题。
- 检查点增加程序级工具证据账本，跨压缩保留已执行调用、结果摘要和重复次数。
- 单条模型上下文工具结果限制为 32000 字符，完整结果仍保存在会话日志；恢复会话也使用有界版本。
- `read_file` 即使显式指定超大结束行也最多返回 500 行，字符窗口最多 20000 字符。
- Windows 命令环境为 Python 子进程强制 UTF-8，减少中文验证脚本的编码失败。
- 真实失败场景已加入自动化回归测试；设计记录在 `V0.8_CONTEXT_DESIGN.zh-CN.md`。

真实效率复测在保持正确输出的前提下，从 103 轮、276 次工具调用、16 次压缩、约 197 万输入 Token、772 秒，下降到 29 轮、67 次工具调用、1 次压缩、约 104 万输入 Token、223 秒。工具调用、压缩和耗时改善明显，但 8 次失败命令和仍然较高的输入 Token 说明后续仍需优化结构化提取与 Shell 策略。

### 已发布的 v0.8.1

- 修复自定义验证脚本成功后仍显示 `Stopped unverified` 的发布阻断问题。
- `verify_output.py`、`check-result.js`、`output_validate.py` 等带验证语义的脚本可以作为完成证据。
- 只有 `Exit code: 0` 才会通过门禁，失败验证不会被误判为成功。
- Agent 和工具层复用同一个验证命令分类器，避免“已尝试”和“已通过”规则漂移。
- 运行期输入框默认常驻紧凑任务进度，不再要求用户先按 `Ctrl+O` 才知道正在做什么。
- 正式任务计划显示完成、进行中、等待和阻塞步骤；没有正式计划时使用五阶段自动进度兜底。
- 默认进度明确显示 `Now`、`Next` 和最近成功的文件写入/修改，`Ctrl+O` 继续用于详细工具活动。
- 进度面板按终端宽度逐行裁剪，保持输入框光标和窄终端布局稳定。

### 已发布的 v0.8.2

- 完成、进行中、等待和阻塞图标改为 `√`、`→`、`○`、`!`，避免 `[x]` 被误读为失败。
- 系统提示要求模型在重要阶段切换时给出简短用户可见进展，运行面板以 `Update:` 展示。
- 运行期隐藏工具日志不再在审批前或任务结束后倾倒；结束只显示最终回答和完成摘要。
- 完整工具证据继续保存在活动详情和会话日志，不因终端简洁输出而丢失。
- 支持包含“验证”或“校验”的多行内联检查成功通过完成门禁。
- 新增只读 `verify_output`，用明确的必需内容、禁止内容和字节范围确定性验收生成的 HTML、JSON、Markdown 与 CSV。
- 验证命令即使退出码为 0，只要输出包含“验证失败”、`Verification: false` 或非零失败计数等反证，也不会通过完成门禁。
- 修复完成门禁把真实工具名 `validate_project` 错写为 `verify_project` 的规则漂移。
- Agnes 2.5 Flash 使用官方 `512K` 上下文窗口，默认自动压缩点从固定 60000 改为模型窗口的 80%（409600）。
- 未知模型明确使用 `128K fallback`；支持 `--context-window`/`XIU_CONTEXT_WINDOW` 覆盖，用户压缩阈值不得超过窗口的 90%。
- Token 估算区分 ASCII 与中文/Emoji，并避免同时重复计算 assistant 文本和原始响应。
- 压缩检查点确定性保留最近真实用户要求原文，并支持 `/compact <重点>` 指定本次摘要重点。
- `/status` 分开展示当前估算量、自动压缩点、模型窗口以及 metadata 来源，不再把压缩点误称为模型窗口。

### v0.8.3 本地里程碑（并入 v0.8.4，未单独发布）

- 成功的文件修改从临时状态栏中分离，成为不会随重绘或任务结束消失的永久终端记录。
- `File changes` 卡片区分创建、修改和删除，显示文件路径、增删行数与最多 6 行关键内容。
- 超过 1 MB 的文本和二进制文件使用修改前后字节规模摘要，防止大文件内容刷屏。
- 文件变化会安全中断并重绘运行期输入框，未提交草稿继续保留。
- 任务结束前补打印尚未展示的变更，避免工具完成与输入循环退出之间的竞态丢失。
- 只有实际前后内容不同才输出卡片；失败或无变化的写入不会制造虚假记录。
- 详细设计和验收标准记录在 `V0.8.3_DESIGN.zh-CN.md`。

### 本地 v0.8.4 候选版本（尚未发布）

- Windows `Ctrl+V` 支持普通文字、剪贴板截图和资源管理器复制文件。
- 截图保存为工作区 `.xiu/attachments/*.png`，外部文件复制进附件目录，模型只接收工作区内引用。
- `/paste` 为终端拦截 `Ctrl+V` 的场景提供可靠备用入口。
- 粘贴引用插入当前 Unicode 多行编辑器的真实光标位置，并沿用草稿保存。
- 单次最多 10 个文件，单文件 25 MB、总计 50 MB；拒绝目录和符号链接。
- 系统提示明确要求图片使用视觉工具、文本使用文件工具，附件不得因粘贴而自动执行。
- Windows Constrained Language 下通过固定的本地 .NET 剪贴板读取器工作；真实截图剪贴板冒烟已通过。
- v0.8.3 的持久文件修改卡片一并包含在 v0.8.4，不单独占用 npm 发布。
- 设计与安全边界记录在 `V0.8.4_DESIGN.zh-CN.md`。

### 本地 v0.8.5 候选版本（尚未发布）

- 模型最终回复支持终端 Markdown 渲染，覆盖标题、粗体、行内代码、列表、代码块和常见表格。
- 运行期输入改为临时 `↳`，结束后不再把 `xiu[working]>` 写入永久滚动历史；每轮使用明确的 Xiu 回答标题和完成分隔。
- 文件变化卡片进一步摘要化：创建文件显示类型和大小，修改文件最多显示 4 行关键增删与 `@@` 位置，完整差异保留在 `/diff`。
- 重要执行和验证结果以“关键操作”回执永久保留，完整工具日志继续只在 `/details` 和会话日志中提供。
- 启动页按中文显示宽度补齐边框，并为终端末列预留安全空间，避免右边框自动换行断裂。
- 新增 `/language`、`--language` 和 `XIU_LANGUAGE`；持久设置保存在 `~/.xiu/settings.json`。
- 中文模式统一覆盖启动页、命令菜单、审批、任务进度、计划、状态和完成摘要，并通过系统提示要求模型的计划、用户可见推理摘要、阶段说明与最终回答使用简体中文。
- 私有思维链不展示；代码、命令、路径、工具名、模型名和外部原始输出保持原文。
- 设计与验收标准记录在 `V0.8.5_DESIGN.zh-CN.md`。

### 已发布的 v0.8.6

- 内置工具活动说明、媒体进度、重试、恢复点和多 Agent 状态在中文模式中确定性本地化。
- 中文任务计划拒绝纯英文自然语言标题；旧会话英文计划在中文进度面板中使用中文占位。
- 中文进度面板不展示模型返回的纯英文阶段叙述，自动中文阶段继续可见。
- 模型阻塞等待用户决定时进入高亮“Xiu 需要你的回答”状态，下一输入提示为 `请回答> `，不再显示绿色完成。
- 普通与运行期提示改为 `xiu> `、`补充> `，避免 Windows 终端字体把特殊箭头显示成方框。
- Xiu 身份增加程序级守卫；明确身份问题不会再被 Agnes/Sapiens AI 等 Provider 默认身份覆盖，规范声明为“Xiu 由静然开发”。
- 修复 Windows PowerShell 中任务结束后的首轮输入不回显问题；输入组件切换时不再暂停共享 stdin。
- `/language` 改为运行时即时切换：空闲页面立即重绘，运行中进度下一帧更新，Agent 系统提示同步重建，无需重启。
- 设计和验收记录在 `V0.8.6_DESIGN.zh-CN.md`。

### 本地 v0.8.7 候选版本（尚未发布）

- 修复 Windows PowerShell/ConPTY 将 Ctrl+C 送为原始 `0x03` 时未能取消任务的问题，同时保留标准解析键路径。
- 取消后立即显示“正在取消”，既有 `AbortSignal` 继续中止模型、Shell、验证、MCP 与媒体请求。
- 明确右键粘贴边界：文字和 Explorer 文件沿用终端右键；剪贴板纯位图使用 `Ctrl+V` 或 `/paste`，不启用会破坏选择与滚轮的全局鼠标捕获。
- 启动页使用连续框线字符和“当前会话”分隔线；每一行右边框通过绝对终端列定位，不再依赖中英文混排的字符宽度推算。
- 设计和验收记录在 `V0.8.7_DESIGN.zh-CN.md`。

### 已发布的 v0.9.0

- 首个里程碑聚焦 Windows 可靠进程执行：新增 `program + args[]` 原生执行通道，避免复杂参数经过 PowerShell 二次解析。
- `run_command` 保留给 PowerShell cmdlet、变量和管道；解析失败会引导迁移到参数化进程工具。
- 参数化执行保留审批、超时、取消和验证证据，并阻止通过 `powershell`、`cmd`、`bash` 等外壳绕过命令风险判断。
- 已覆盖空格、单双引号、中文、JSON、美元符号、分号、管道符和 `&` 等复杂参数的原样传递。
- 后续依次进入结构化 HTML/JSON/CSV 提取、增量索引与 Repository Map、符号关系、长任务诊断和 Provider 扩展。
- 设计与验收标准记录在 `V0.9_DESIGN.zh-CN.md`。

### 已发布的 v0.9.1

- Windows 交互输入期间临时开启终端鼠标上报，右键与 `Ctrl+V` 共用剪贴板附件导入逻辑。
- 支持普通文字、剪贴板图片和 Explorer 复制文件；右键释放不会重复触发，退出输入后恢复终端模式。
- 不支持或拦截鼠标事件的终端继续使用 `Ctrl+V` 或 `/paste`；输入期间使用 `Shift+拖动` 选择终端文本。
- 设计和验收记录在 `V0.9.1_DESIGN.zh-CN.md`。

### 已发布的 v0.9.2

- 修复 Node.js 将 SGR 鼠标报告拆成多个 keypress 后，坐标尾部被当作普通文本插入输入框的问题。
- 使用 250 毫秒、64 字符上限的有界状态机重组按下与释放事件，异常序列不会令编辑器长期停留在鼠标解析状态。
- 回归测试直接复现真实 `ESC[<2;51;21M`/`m` 输入，验证右键事件正确且无文本泄漏。
- 设计和验收记录在 `V0.9.2_DESIGN.zh-CN.md`。

### 已发布的 v0.9.3

- 文字和 Explorer 文件优先使用 Windows 自带 `Get-Clipboard` 与一次性 UTF-8 JSON，不再依赖自生成 helper EXE。
- 只有纯位图才尝试旧 helper；企业策略阻止时明确要求先保存为文件，不绕过组织安全边界。
- 原生 PowerShell 后端不可用且没有缓存 helper 时不启用鼠标上报，保留终端原生右键文字粘贴。
- 已在 Windows PowerShell `ConstrainedLanguage` 下完成真实文字剪贴板冒烟验证。
- 设计和验收记录在 `V0.9.3_DESIGN.zh-CN.md`。

### 已发布的 v0.9.4

- 不再开启终端鼠标上报或截获右键，右键粘贴完全交还 Windows Terminal、PowerShell 等终端宿主。
- `Get-Clipboard` 的 FileDropList、Text、Image 能力分别尝试，单项被集团策略拦截不会直接中断其他格式。
- 文字或文件读取被策略禁止时不再尝试 helper；只有成功识别为纯位图后才允许可选 helper 保存像素。
- PowerShell 的 CLIXML、内部命令和策略诊断不再直接显示给用户，只保留简短恢复建议。
- 设计和验收记录在 `V0.9.4_DESIGN.zh-CN.md`。

### v0.9.5 里程碑（已包含在 v0.9.6 发布）

- 新增只读 `extract_html`，按 CSS 根选择器和相对字段提取文本、HTML 与属性。
- 新增只读 `extract_json`，使用 RFC 6901 JSON Pointer 选择值并分页对象或数组。
- 新增只读 `extract_csv`，支持 CSV/TSV、表头、列选择、精确过滤以及带引号换行字段。
- 三项工具统一返回有界有效 JSON，包含总数、返回数、下一偏移和截断统计。
- 单文件限制 50 MB，单页最多 100 条、20 个字段、60000 字符；工作区边界和 Plan 模式只读约束保持不变。
- 自动识别 UTF-8/BOM/UTF-16，并为非 UTF-8 中文数据提供 GB18030 回退和显式编码覆盖。
- Agent 系统提示优先使用结构化工具，避免重复 `read_file` 和一次性解析脚本。
- 设计和验收记录在 `V0.9.5_DESIGN.zh-CN.md`。

### 已发布的 v0.9.6

- 项目索引缓存升级为增量清单；启动时比较路径、大小和修改时间，只重新读取新增或变化文件。
- 未变化项目跨 Xiu 进程复用全部有界词项，不再按五分钟 TTL 周期性重读所有源文件。
- 工作区写入、检查点恢复和 Agent 补丁集成继续标记索引为脏，下一次检索前执行增量刷新。
- 新增、修改、删除、复用、实际重读数量和刷新耗时进入确定性索引状态。
- `/status` 显示全量构建、增量更新或缓存复用模式以及耗时。
- 缓存版本、结构、字段边界和相对路径经过校验；损坏、旧版或越界缓存自动完整重建。
- 索引枚举不跟随符号链接，词项数量和长度继续有界；缓存使用同目录临时文件写入后再替换。
- 设计和验收记录在 `V0.9.6_DESIGN.zh-CN.md`。

### 已发布的 v0.9.7

- 新增增量 Repository Map，按路径分页展示模块、主要符号、内部依赖和被依赖数量。
- JavaScript、TypeScript、JSX、TSX、MJS 和 CJS 使用 Xiu 自带的官方 TypeScript AST 解析器。
- 新增只读 `find_symbol`，查询定义位置、种类、容器、导出状态和有界签名。
- 新增只读 `find_references` 和 `find_callers`，支持同文件关系、命名导入别名、命名空间访问、直接调用和构造调用。
- 同名定义不会静默合并；工具返回歧义候选并要求用 `defined_in` 明确目标文件。
- 四项代码智能工具统一分页并保持有效有界 JSON，可在 Plan 模式使用。
- 每文件符号、依赖、引用数量和字符串长度均有上限；符号数据与文件词项一起增量复用。
- 非 JS/TS 语言本版只进入模块地图，不宣称精确符号关系；跨语言 Tree-sitter 与 LSP 后续按实际需求扩展。
- `/status` 增加已分析模块、符号和依赖边统计。
- 设计和验收记录在 `V0.9.7_DESIGN.zh-CN.md`。

### 已发布的 v0.9.8

- 新增每任务有界诊断账本，独立于累计会话统计记录 Token、模型耗时、工具耗时、审批等待、重试和失败。
- 运行期状态栏、`/diagnostics`、`/status` 和完成回执展示同一份程序级诊断事实。
- 诊断明确区分模型运行、工具运行、等待审批和空闲阶段；慢请求或等待用户不会误报为停滞。
- 连续三次失败，或至少八次操作且两分钟没有新证据时，提供可解释的停滞依据和切换策略建议，但不自动取消任务。
- 首次成功的具体工具输入、真实工作区变化、计划更新、验证通过和 steering 确定性刷新有效进展时间；重复相同读取不制造虚假进展。
- 最近失败、工具聚合和操作说明均限长限量，常见 Key、Token、密码、Authorization 和 Cookie 自动脱敏，不复制完整 stdout/stderr。
- 诊断快照随现有项目会话保存；恢复未完成快照时标记为已中断，旧会话或损坏诊断字段保持兼容。
- 设计和验收记录在 `V0.9.8_DESIGN.zh-CN.md`。

### 已发布的 v0.9.9

- 简体中文模式在显示前确定性转换模型自然语言回复和任务计划，代码块与行内代码保持原样。
- 验证通过后的模型收尾阶段明确显示“验证已通过，正在整理最终结果”，避免被误认为验证卡死。
- 技能发现兼容项目和用户级 `.agents/skills`；`/skills` 打开前刷新，任务结束后加载外部安装的新技能。
- 选择器兼容 Windows 原始方向键序列，并提供数字键直接选择兜底。
- `/resume` 和启动参数恢复会话后展开最近对话，完整内容继续由 `/history` 提供。
- 长进展按终端宽度换行，正式计划和详细活动展示范围扩大并明确说明省略内容。
- 工作区变化不再中断运行期输入读取，修复空“补充>”不断刷屏。
- 设计和验收记录在 `V0.9.9_DESIGN.zh-CN.md`。

### 已发布的 v0.9.10

- `/resume`、`xiu --resume` 和 `/history` 统一使用完整结构化终端回放，不再维护互相漂移的摘要格式。
- 移除恢复界面的最近 12 条和单条 600 字限制，保留原始换行、Markdown、代码块与表格结构。
- 旧会话从全部 task、assistant、steering 和 diagnostics 事件重建；内部重试任务不会作为超长用户输入倾倒。
- 新会话在每轮结束时写入版本化 `ui_turn` 语义记录，持久化用户输入、补充要求、文件变化、关键操作、最终回复、等待问题和完成状态。
- 回放保存语义数据而非 ANSI 控制码，因此可根据当前终端宽度与语言重新排版，同时保持与正常交互相同的组件结构。
- 设计和验收记录在 `V0.9.10_DESIGN.zh-CN.md`。

### 已发布的 v0.10.0

- 新增版本化用户级 Provider Registry：`~/.xiu/providers.json`，可保存密钥环境变量名或本机明文凭证；凭证使用隐藏输入、原子写入并从会话日志和界面中脱敏。
- 内置 OpenAI、Anthropic、Agnes、Ollama、LM Studio 和 vLLM Profile，并支持自定义 OpenAI-compatible 服务。
- `/providers` 可查看、测试并切换 Provider；`/provider add`、`/provider edit`、`/provider remove`、`/provider test` 提供完整管理闭环。编辑默认保留原值和本地 Key。
- 连接探测在服务不支持模型列表时回退到最小聊天请求，避免将部分兼容的服务误判为不可用。
- 切换 Provider 时保留对话、计划、检查点和索引，按能力重建媒体工具；切换失败不会污染当前配置。
- 会话事件保存 Provider ID，恢复时在没有显式命令行覆盖的前提下恢复原 Provider 与模型；环境变量只作为没有交互选择时的启动默认值。
- `/models` 增加能力、上下文、Provider 和发现来源展示。
- 本地与自定义服务默认不宣称视觉、图片或视频能力；未声明工具能力时不向模型发送工具定义。
- 本版本只完成 Provider 基础架构。自动路由、故障降级、价格策略和请求缓存按计划进入后续 v0.10.x。
- 设计和验收记录在 `V0.10_DESIGN.zh-CN.md`。

### 已发布的 v0.10.1

- 目标是按 Provider 与模型真实探测文本连接、工具调用和视觉输入能力，并将结果缓存到用户级 Provider Registry。
- 工具探测只调用无副作用的内置虚拟工具；视觉探测发送内置纯色色块 PNG 并校验模型识别结果，不读取或上传项目文件，也不会把静默忽略图片的端点误判为支持。
- 探测状态区分支持、不支持、未知和未测试；未知能力按保守策略不暴露给模型。
- `/provider test` 强制刷新当前模型能力；Provider 切换优先复用仍有效的缓存，避免重复请求。
- `/providers` 与 `/models` 展示能力来源和最近探测状态；图片和视频生成能力仍由已实现的适配器声明，不自动生成付费内容进行探测。
- 兼容服务返回常见上下文窗口元数据时自动按模型缓存；手动 Provider 配置具有更高优先级。切换回执显示当前窗口和压缩点。
- Windows 选择器限制为最大 88 列并截断次要详情；Provider 菜单隐藏 URL 和探测时间，规避 PowerShell 缓冲区宽度与可见窗口不一致导致的物理换行和上下键重绘残留。
- Windows 输入会话使用代际隔离和 Raw 模式守护，避免旧提示清理让新输入退回行缓冲；继续保留终端原生右键粘贴，不接管经典 PowerShell 的鼠标行为。
- OpenAI-compatible 工具探测兼容不支持强制指定工具的推理模型：强制探测没有返回结构化调用时退回 `tool_choice: auto`；探测协议升级会自动使旧的误判缓存失效。
- `/providers` 与 `/models` 会持久化最近选择的 Provider 和该 Provider 的模型；显式 CLI 参数优先，其次为最近交互选择、环境变量和内置默认值，修复重启后被旧环境变量恢复为默认 Provider 的问题。
- 设计和验收记录在 `V0.10.1_DESIGN.zh-CN.md`。

### 已发布的 v0.10.2

- 启动欢迎卡片改为“启动时配置 · 实时状态见底栏”，避免滚动历史中的旧模型被误认为当前值。
- 输入框下方的实时状态栏同时显示 Provider ID 与模型 ID，切换后立即更新。
- 不清屏、不回写终端历史，继续保持 PowerShell、Raw 输入和终端原生右键粘贴兼容性。
- 设计和验收记录在 `V0.10.2_DESIGN.zh-CN.md`。

### 已发布的 v0.11.0

- 每个主 Provider 可保存最多 8 项的有序备用链，并通过 `/provider fallback add|remove|clear` 管理。
- 当前 Provider 仅对网络、超时、限流和服务端瞬时错误进行三次有界退避重试；认证、参数、模型不存在和用户取消不会触发降级。
- 备用候选按当前工具能力和上下文安全预算筛选，已尝试或能力不足的候选会被跳过并解释原因。
- 故障转移只发生在本轮尚未输出任何流式文字的模型请求边界，不重放已完成工具调用、文件修改或命令副作用。
- 自动切换只更新当前运行时，不覆盖用户手动保存的下次启动 Provider。
- 终端、会话日志和 `/diagnostics` 记录来源、目标、模型、原因与跳过项。
- 当前阶段覆盖主 Agent 的文本/工具模型请求、无工具且无流式输出的上下文压缩请求，以及拥有独立失败游标和安全工具集合的并行子 Agent。
- 压缩失败时仍保留本地检查点摘要兜底。
- 真实双 Provider 人工验收已通过，包括 OpenAI SDK 通用 `APIConnectionError: Connection error.`、嵌套网络错误、备用 Provider 工具能力筛选、实时状态和会话回执。
- 图片与视频生成使用持久化幂等键、请求 ID、任务 ID 和资产缓存；相同完成请求直接复用，轮询/下载中断可恢复，提交结果不确定时禁止自动重放，只有明确批准可能重复计费后才能强制新建。
- 媒体审批支持“仅允许一次”或“本次会话始终允许”同类操作；图片、视频授权相互隔离并在退出时失效，强制重复生成不继承会话授权。
- 真实视频生成验收发现状态轮询触发 429 后模型会改写提示词重复提交；现已改为同一工具调用内低频退避轮询，并为被 429/503 拒绝的新任务建立 Provider/模型级持久化冷却。强制生成不能用于新提示词。
- 失败或拒绝的工具不再产生绿色成功回执；最后一次工具仍失败时任务以“未完成”结束并进入恢复流程。
- 工具参数异常已做调用级隔离；同一轮中的冗余坏调用不会覆盖已经成功的生成与验证结果。
- 媒体生成始终保持单 Provider 边界，不参与文本请求的自动故障转移。
- 设计和验收记录在 `V0.11_DESIGN.zh-CN.md`。

### 已随 v0.11.2 发布的 v0.11.1 能力

- `/media` 确定性列出项目最近的媒体请求、稳定请求 ID、状态、Provider/模型、任务 ID 和保存路径，不依赖模型记忆提示词与参数。
- 新增只读媒体任务列表工具，以及按稳定请求 ID 恢复的执行工具。
- 恢复工具只能复用缓存、继续轮询已有视频任务或重新下载已有资产，不包含创建图片或视频任务的代码路径。
- 提交结果未知、歧义和终态失败仍保持阻断；Provider 不匹配时要求先切换，不跨供应商重放。
- 列表和工具结果不展示签名下载 URL，输出路径继续遵守工作区边界、审批和检查点机制。
- 自动化覆盖 ID/前缀解析、歧义拒绝、缓存复用、视频任务续查，以及恢复期间生成 API 零调用。
- 设计与人工验收清单记录在 `V0.11.1_DESIGN.zh-CN.md`。

### 已发布的 v0.11.2（包含 v0.11.1）

- 主 Agent 支持规划、实现、验证三阶段的显式 Provider 路由；用户通过 `/routing set <stage>` 绑定已有 Profile，并用 `/routing on|off` 控制总开关。
- 阶段判定由程序规则完成；未绑定阶段恢复本次任务开始时的用户手动模型，不依赖模型自述。
- 自动切换只发生在新模型请求边界，并检查工具能力和上下文安全预算；不满足时保留当前模型并说明原因。
- 路由成功、跳过和任务结束恢复均不会覆盖下次启动默认选择；成功与跳过事件写入终端、会话和 `/diagnostics`。
- 真实多 Provider 任务验收后，诊断升级为记录所有阶段调用和判定依据；无切换的首轮规划也可见，验证失败返回实现再复验具有明确原因。
- Windows `npm`/`npx` 通过 Node CLI 直启，标准 package.json 检查优先使用 `validate_project`；系统提示加入确定性参数示例，减少 `EINVAL`、Shell wrapper 和陈旧文本替换失败。
- 审批账本区分实际提示、`--yes` 自动放行、会话规则放行与策略检查；普通写入、编辑、直接程序和项目验证支持窄范围会话授权，危险操作边界不变。
- Token 明确为跨模型请求累计量并显示平均输入；完成任务若中途存在已恢复失败，会显示可恢复失败与工具成功率。
- 工作区信任、风险审批、Plan 模式、恢复点、危险操作确认和媒体幂等边界保持不变。
- 当前版本不对模型价格和质量做未经验证的猜测；用户通过阶段绑定表达策略。并行子 Agent 自动阶段路由和 Prompt Cache 延后。
- 设计与验收记录在 `V0.11.2_DESIGN.zh-CN.md`。

### 本地 v0.11.3 候选版（尚未发布）

- 主 Agent 回答不进入本地结果缓存，工具调用、文件修改、审批、流式回答和媒体生成均不存在缓存重放路径。
- OpenAI/OpenAI-compatible 读取 Provider 返回的 cached token；原生 OpenAI 使用不含提示原文的稳定缓存键，Anthropic 为稳定系统提示设置 ephemeral 缓存边界。
- `/diagnostics` 显示 Provider 回报的缓存命中请求、读取 Token、写入 Token 和命中率，不根据相似度猜测命中。
- 模型发现使用 60 秒、有界的内存缓存；完全相同的并发模型发现与能力探测使用单航班合并，失败不会写入完成缓存。
- Provider 配置升级为版本 2；能力缓存带协议版本和配置指纹，旧配置自动迁移，端点、代理、模型、能力声明或鉴权变化会让探测失效。
- 缓存键不持久化 API Key、项目内容或提示原文；媒体幂等和安全审批边界保持不变。
- 设计与验收记录在 `V0.11.3_DESIGN.zh-CN.md`。

### 当前自动化基线

- 自动化测试覆盖本机保存 Key 的真实鉴权请求、每次连接测试的最小文本调用、无副作用工具探测、推理模型自动选择回退、文本伪工具调用拒绝、视觉内容校验及成功/拒绝/瞬时失败分类、Provider/模型跨进程选择优先级、按模型缓存和 Profile 变更失效、有序备用链与阶段路由持久化、瞬时故障分类与脱敏、阶段切换/能力跳过/任务结束恢复、压缩请求安全切换、流式输出后禁止不安全切换，以及媒体请求去重、未知提交阻断和资源恢复。
- TypeScript 类型检查通过。
- 正式构建通过。
- npm 打包预览通过。

测试数量会随版本变化；以后以“全部通过”和实际测试报告为准，而不是永久固定为 58。

## 四、v0.5.1 稳定补丁（已并入 v0.6.0）

### 目标

原计划单独发布的 UI 修复、文档、许可证和 GitHub 收尾已直接并入本地 `0.6.0` 候选版本，避免在多 Agent 开发已经完成后再发布功能落后的 `0.5.1`。

### 发布前任务

- 添加正式 `LICENSE` 文件。
- 建立 GitHub 仓库并补充 `repository`、`homepage`、`bugs`、`keywords`。
- 在全新 Windows 环境验证 npm 安装。
- 检查文档命令和实际 CLI 一致。
- 运行 typecheck、全部测试和 build。
- 使用 `npm pack --dry-run` 检查敏感文件。
- 将版本升级为 `0.6.0`，不能重新发布 `0.5.0`。
- 发布后查询官方 registry，并在干净环境安装验证。

### 验收标准

- 新用户只阅读文档即可安装、配置并完成第一个任务。
- `/skills` 等选择器退出后没有残留。
- 安装包不包含 `.env`、会话、Key、日志和个人文件。
- 发布后 `npm view @xiu-ai/cli version` 与目标 dist-tag 显示 `0.6.0`。

## 五、v0.6：多 Agent、任务调度与 Worktree 隔离

这是完成 v0.5.1 后的首要开发版本。目标是把 Xiu 从单 Agent 循环升级为可组织多个专业 Agent 的工程系统，并让 Superpowers 中的并行和 Subagent 工作流真正可用。

### 本地已实现能力

- 主 Agent 将复杂目标拆成任务图。
- Explorer、Implementer、Reviewer、Tester 等角色。
- 有依赖关系的任务串行，无依赖任务并行。
- 每个子 Agent 使用独立上下文和预算。
- 使用 Git Worktree 隔离可能冲突的文件修改。
- 只读调查 Agent 可以共享主工作区。
- `/agents` 查看所有 Agent、角色、任务、耗时和 Token。
- 单独取消和重试 Agent。
- 主 Agent 汇总结果、检测冲突并执行最终审查。
- 并发数量、单 Agent 轮数、Token 和时间统计。
- 子 Agent 状态写入会话，终端关闭后可以恢复。
- Superpowers 并行、计划执行和 Subagent Skills 兼容。

暂停/接管、精确费用预算和自动清理 Worktree 尚未实现，不属于 `0.6.0` 的已交付声明；前两项进入后续任务控制版本，Worktree 清理必须先完成未集成修改保护设计。

### 安全设计

- 子 Agent 不得绕过主 Agent 的审批策略。
- Worktree 路径必须经过边界校验。
- 不自动删除包含未合并修改的 Worktree。
- 主 Agent 合并前展示 Diff 并运行验证。
- dangerous 操作仍需用户明确批准。

### 验收标准

- 至少三个独立调查任务可以并行完成。
- 并行修改不会互相覆盖。
- 用户始终能看到每个 Agent 正在做什么。
- 任一 Agent 可以单独取消而不破坏其他任务。
- 主 Agent 合并前完成审查和测试。
- 关闭终端后可以恢复未完成的多 Agent 任务。

## 六、v0.7：专业级终端 UI

### v0.7.0 已实现能力

- 真正的多行输入编辑器和 Unicode 光标状态机。
- `@` 文件自动补全。
- 输入历史搜索和草稿恢复。
- 工具与 Agent 活动摘要和完整详情。
- 审批请求使用方向键选择并默认拒绝。
- 状态栏展示 Plan 阶段、Agent、MCP 和后台任务。
- 终端 Resize 后正确重排。

拖入文件规范化、固定任务树/多 Agent 面板、交互式 Diff、主题和中英文 UI 延后到 v0.7.x；这些能力必须复用 v0.7.0 的编辑状态与响应式渲染模型。

### v0.7.1 本地已实现能力

- 任务运行期可编辑输入和有界后续任务队列。
- 排队查看、清空、当前任务取消和安全退出控制。
- 运行阶段/队列状态栏与 Agent 输出有界缓冲。
- 审批期间单一终端所有权和草稿恢复。

固定任务树/多 Agent 面板、交互式 Diff、拖入文件规范化、队列跨进程持久化、主题和中英文 UI 继续留在 v0.7.x。

### v0.7.2 本地已实现能力

- `Ctrl+O` 内联实时进展、Turn 上限、耗时和最近工具活动。
- Steering 与显式独立队列分离。
- 成功/失败工具调用循环检测和策略切换提示。
- 失败暂停、默认停止和基于现有证据重试。
- 未验证结果不再显示绿色完成。

### 验收标准

- 长任务不依赖滚动日志才能理解状态。
- 输入、候选菜单、状态栏和弹窗不出现残影。
- 窄终端、中文、Emoji 和长路径布局稳定。
- 用户可以清楚区分计划、执行、审查和等待审批状态。

## 七、v0.8：大型项目代码智能

v0.8 的首个里程碑根据真实长任务失败数据调整为“可靠上下文检查点与大文件窗口读取”。这些基础能力已在本地 v0.8.0 实现；没有可靠的任务延续和有界输入，后续 AST、Repository Map 和语义检索只会进一步放大上下文浪费。以下代码智能能力继续按增量里程碑实现。

### 计划能力

- 增量文件索引。
- Tree-sitter 多语言 AST。
- 函数、类、符号、引用和调用关系图。
- 可选 LSP 集成。
- 关键词与语义检索混合排序。
- 自动 Repository Map。
- Git 历史和代码变化关联。
- Monorepo、Workspace 和多包识别。
- 生成目录、大文件、二进制和依赖智能过滤。
- 只重建受修改影响的索引。
- 可解释的相关性评分。

### 验收标准

- 十万文件级项目仍能在可接受时间内建立和更新索引。
- 常见跨文件任务优先找到真正相关的符号和调用链。
- 不把大量无关代码注入模型上下文。
- 索引损坏后可以自动重建，不影响项目源文件。

## 八、v0.9：可靠性、任务控制与恢复

### 计划能力

- 显式任务状态机和步骤级持久化。
- 会话 Branch/Fork。
- 确定性 Replay 和诊断记录。
- 更完整的操作前后恢复点。
- Shell 副作用记录和风险说明。
- 自动选择最小相关测试范围。
- 失败归因、重复失败阻断和策略切换。
- Provider 故障时切换备用模型。
- Token、费用、时间和修改文件数预算。
- 长任务后台运行。
- 用户定义验收条件和完成清单。
- Reviewer Agent 作为完成门禁。

### 验收标准

- 网络中断、模型失败或终端关闭后无需从头开始。
- 测试未通过时不会无依据宣布任务完成。
- 用户可以从明确步骤 Fork 新方案，而不破坏原会话。
- 每个失败都能定位到模型、工具、审批或验证阶段。

## 九、v0.10：模型与多模态平台

### 计划能力

- OpenAI、Anthropic、Agnes 原生能力适配。
- Ollama、vLLM、LM Studio 等本地模型。
- 用户自定义 OpenAI-compatible 服务。
- 自动发现模型上下文、工具、视觉和生成能力。
- 文本、视觉、图片、视频统一路由。
- 根据任务步骤自动选择模型。
- 主模型失败时自动降级。
- 模型速度、质量、成本策略。
- Prompt Cache、请求缓存和重复调用合并。
- `/models` 展示能力、上下文、来源和可选价格信息。
- 可共享但不包含密钥的模型预设。

### 验收标准

- 用户描述目标即可，不必手动理解内部模型分工。
- 不向模型宣传 Provider 实际不支持的能力。
- 模型切换不破坏当前会话和工具调用结构。
- 本地模型没有云端 Key 也可以完成基础编码任务。

## 十、v0.11：弹性模型路由与扩展基础

### 计划能力

- 主 Provider 有序故障降级，按能力、上下文和安全边界筛选。
- 自动切换的永久回执、会话事件和任务诊断。
- 压缩请求、媒体请求和子 Agent 的独立降级策略。
- 根据任务步骤自动选择模型。
- 可解释的速度、质量和成本策略。
- Prompt Cache、请求缓存和重复调用合并。
- Provider 与能力缓存的版本化迁移。
- 完成路由稳定性后，继续推进以下扩展基础：

- Streamable HTTP MCP。
- MCP OAuth。
- MCP Resources、Prompts 和 Sampling。
- `/mcp add`、`/mcp remove` 和安装向导。
- Skills 搜索、更新、卸载和版本锁定。
- Skill 安全检查和能力声明。
- Hooks：任务前、工具前、修改后、验证后、完成后。
- 自定义 Slash Commands。
- Provider、Tool 和 Agent 插件接口。
- Agent 模板。
- 社区插件与 Skill 市场。

### 验收标准

- 任一自动切换都不能重复已经输出的回答或已完成的工具副作用。
- 能力或上下文不满足时必须跳过候选并说明原因。
- 自动降级不覆盖用户手动保存的默认 Provider。
- 网络、认证、配额、上下文和能力失败能够被区分，且可从诊断记录复盘。

- 第三方无需修改 Xiu 核心代码即可增加 Provider、Tool 或工作流。
- 插件 API 有版本和兼容策略。
- 第三方扩展不能绕过工作区信任、审批和 Plan 模式。
- 扩展安装、更新和卸载过程可审计、可恢复。

## 十一、v0.12：安全与团队能力

### 计划能力

- 命令沙箱和进程资源限制。
- 文件和网络访问策略。
- API Key、Token 和敏感信息自动脱敏。
- Prompt Injection 检测和不可信内容边界。
- MCP 与 Skill 权限清单。
- 项目、用户和组织级权限策略。
- 审计日志。
- 团队共享配置和规则。
- 企业代理、私有 Registry 和私有模型。
- CI/CD 非交互运行模式。
- 组织级预算、Provider 和模型控制。

### 验收标准

- 第三方 Skill/MCP 不能静默扩大权限。
- 日志和会话默认不泄露密钥。
- 企业可以复现“谁在何时批准了什么操作”。
- 无人值守模式仍有明确的权限边界和失败策略。

## 十二、v1.0：稳定产品化

只有达到以下条件后才发布 1.0：

- Windows、macOS 和 Linux 持续集成通过。
- npm、独立安装器和便携版本可用。
- 自动更新和版本回滚。
- 签名发布包和供应链验证。
- 完整中英文文档和首次配置向导。
- 真实项目任务评测体系稳定运行。
- 崩溃恢复通过压力测试。
- Provider、Tool、Skill、MCP 和插件 API 有兼容承诺。
- 有明确的隐私说明和安全模型。
- 诊断数据必须可选、匿名且经过用户明确授权。

## 十三、所有版本共同遵守的工程原则

### 13.1 先可靠，再增加功能

- 不为了版本号堆半成品功能。
- 新功能必须包含失败路径、取消和清理。
- 任何状态都应能向用户解释。

### 13.2 安全边界不能倒退

- 工作区信任必须先于项目指令、Skill 和 MCP 执行。
- read/write/execute/dangerous 风险分级保持统一。
- Plan 模式必须在 Agent 边界阻止非只读工具。
- `--yes` 不能自动批准 dangerous 操作。
- 删除、回退、覆盖和外部发布必须明确确认。

### 13.3 每次发布必须验证

- `npm run typecheck`。
- `npm test`。
- `npm run build`。
- `npm pack --dry-run`。
- 干净环境安装和 `xiu --version` 冒烟测试。
- 发布后从官方 registry 查询版本和 dist-tag。

### 13.4 文档必须跟随行为

- CLI 参数变化要更新 `USAGE.zh-CN.md`。
- 发布流程变化要更新 `PUBLISHING.zh-CN.md`。
- 里程碑和优先级变化要更新本文档。
- README 保持简洁，并链接到详细文档。

### 13.5 不覆盖用户工作

- 保留无关修改。
- 使用小而聚焦的补丁。
- 修改后展示 Diff 并运行相关验证。
- 不在未确认的情况下删除或硬重置用户文件。

## 十四、持续评测指标

后续建立固定评测仓库和任务集，每个版本使用相同任务回归：

- 端到端任务成功率。
- 首次修改正确率。
- 自动测试通过率。
- 无关文件修改率。
- 重复工具失败率。
- 平均 Token 与费用。
- 平均完成时间和首字延迟。
- 中断、崩溃和关闭终端后的恢复成功率。
- 审批拒绝率及原因。
- 大型项目检索命中率。
- 多 Agent 冲突率和合并成功率。

性能或成功率退化必须被视为发布阻断问题，不能只因为新增功能测试通过就忽略。

## 十五、推荐开发节奏

每个版本按照以下循环：

1. 从用户反馈和评测中确定一个主要问题。
2. 写清目标、非目标、风险和验收标准。
3. 先补失败场景测试或评测用例。
4. 实现最小完整闭环。
5. 使用 Xiu 自己完成一部分开发和验证，持续 Dogfooding。
6. 运行完整自动化和干净安装测试。
7. 发布 Beta 给小范围用户。
8. 收集真实失败案例，修复后再提升为 latest。
9. 更新文档和路线图状态。

## 十六、明确不做的事情

- 不把未经验证的实验功能直接设为 `latest`。
- 不为了展示“自主”而隐藏执行过程或审批。
- 不默认上传用户代码、会话或诊断数据。
- 不让 Skills、MCP 或子 Agent 绕过核心安全策略。
- 不宣称支持尚未真正实现或验证的平台和模型能力。
- 不在测试失败时发布。

## 十七、下一会话的默认行动

如果用户没有改变优先级，后续应按以下顺序继续：

1. 使用 OpenAI-compatible 与 Anthropic/OpenAI 真实 Provider 验收 Prompt Cache 统计、能力缓存迁移和重复模型发现合并。
2. 修复人工验收发现的阻断问题；执行完整自动化、构建、打包和干净安装后发布 v0.11.3。
3. 发布后进入 Streamable HTTP MCP、MCP OAuth、Skills 生命周期管理与扩展接口。

如果用户提出新的高优先级 Bug、安全问题或发布阻断问题，应先处理这些问题，再回到上述顺序，并在本文档记录优先级变化。

## 十八、路线图维护规则

每次完成版本后：

- 把“当前已发布版本”更新为真实 npm 版本。
- 将已完成内容从“本地尚未发布”移动到对应发布记录。
- 记录删减、延期和新增的关键决策。
- 更新自动化基线和已知限制。
- 明确写出下一会话默认行动。
- 不删除历史里程碑；可以标记完成、调整或取消，并说明原因。

路线图是决策参考，不是不可改变的承诺。修改路线时应基于用户反馈、评测数据、安全风险和实际工程成本，而不是单纯追逐其他产品的功能列表。
