# Xiu v0.8.6 中文交互可靠性修复

## 目标

v0.8.6 修复 v0.8.5 真实使用中发现的三个发布阻断问题：中文模式仍泄漏英文活动说明、模型需要用户回答时没有醒目的等待状态，以及底层模型把自己的 Provider 身份错误当成 Xiu 产品身份。

## 交付范围

1. 中文模式确定性本地化内置工具的运行说明、媒体进度、重试、恢复点和多 Agent 状态；代码、命令、路径、工具名和第三方原始输出保持原文。
2. 中文计划的目标、步骤标题和说明必须使用简体中文；旧会话中残留的英文计划标题在中文界面使用中文占位，不继续泄漏到进度面板。
3. 中文模式不展示模型返回的纯英文阶段叙述，继续显示程序生成的中文自动阶段。
4. 模型确实需要输入才能继续时使用 `USER_INPUT_REQUIRED` 协议；终端将它渲染为高亮的“Xiu 需要你的回答”，并把下一输入提示改为 `请回答> `。答案会与原始任务和问题组成继续任务契约，不会被误当成新的独立目标。
5. 普通输入提示统一为兼容 Windows 字体的 `xiu> `，运行期补充提示为 `补充> `，不再依赖容易显示成方框的特殊箭头字符。
6. Xiu 身份增加程序级守卫。对“你是谁”“谁开发了 Xiu”等身份问题，即使 Provider 返回 Agnes 或 Sapiens AI 身份，也只保存并显示规范答案：Xiu 由静然开发。
7. 运行期补充输入框结束后只解除按键监听并退出 raw mode，不再暂停进程级 stdin，确保 Windows PowerShell 中下一轮 `xiu> ` 输入立即回显。

## 身份边界

- Xiu 是产品与 Agent 身份。
- Agnes、OpenAI、Anthropic 等是底层模型或 Provider，不是 Xiu 的开发者。
- 对 Sapiens AI 等第三方本身的知识问题仍交给模型正常回答；身份守卫只拦截明确的 Xiu 自身身份问题。
- 旧会话中的错误身份回答不会被再次作为本轮规范答案保存。

## 验收标准

- 中文关键操作显示“验证生成结果 snake.html”，不显示 `verify generated output snake.html`。
- 中文计划工具拒绝纯英文自然语言步骤，代码路径和命令不受影响。
- 阻塞问题具有独立高亮标题、问题正文、等待状态和 `请回答> ` 输入提示。
- Provider 故意回答“我是 Agnes，由 Sapiens AI 开发”时，Agent 最终回答和会话历史仍为“Xiu，由静然开发”。
- 一个任务结束后，下一轮输入应立即显示，无需先盲输并按一次 Enter 来恢复终端回显。
- 全量测试、类型检查、构建、npm 打包和隔离安装通过。
