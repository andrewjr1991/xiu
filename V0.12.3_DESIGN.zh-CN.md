# Xiu v0.12.3 凭证安全设计

## 1. 背景

Xiu 目前允许用户把 Provider API Key 保存在 `~/.xiu/providers.json`，并把 MCP OAuth Token 与动态客户端信息保存在 `~/.xiu/mcp-auth.json`。两者都使用隐藏输入、严格结构校验、同目录临时文件替换和用户级文件权限，但敏感值仍是明文 JSON。

v0.12.3 的目标不是立即删掉现有配置，而是先建立一条可验证、可迁移、可回退的凭证生命周期。任何安全后端都必须在真实 Windows 和企业受限环境中通过能力探测后才能成为默认选项。

## 2. 当前审计结论

### 2.1 Provider

- Profile、活动模型、路由、能力探测和明文 `credentials` 当前共存于 `~/.xiu/providers.json`。
- 运行时优先读取声明的环境变量，其次读取本地保存的 Key。
- Key 会进入 Provider 与媒体适配器内存，但 Session 只记录 `configured`，不会记录原值。
- Provider 删除时会同步删除对应本地 Key；Profile 编辑默认保留原 Key。
- 能力探测的进程内 single-flight 键目前包含凭证参与的 SHA-256 输入。该键不会落盘，但统一接口接入后仍应改用凭证 revision 触发失效，减少秘密不必要的派生和驻留。

### 2.2 MCP OAuth

- `~/.xiu/mcp-auth.json` 当前保存 Access Token、Refresh Token、ID Token、到期时间和动态注册客户端信息；客户端信息可能包含 Client Secret。
- 凭证按 canonical resource、Issuer、Client ID 三元组隔离，并使用 SHA-256 稳定键索引。
- 刷新、轮换、注销和清理均通过同一存储类串行执行，写入使用原子替换。
- MCP 项目配置只能声明认证方式或 Bearer 环境变量，不能直接保存 OAuth Token。

### 2.3 已有保护与缺口

已有保护：

- 隐藏输入，不在欢迎页、状态页和普通 Session 中显示原值。
- 诊断按字段名与常见 Token 格式脱敏；OAuth 错误额外清理 URL 参数和 JSON 字段。
- 项目索引默认排除 `.env`、私钥和常见 credentials/secrets 文件。
- 敏感文件使用原子写入并尝试设置为仅当前用户可读。

仍需解决：

- 本机其他同权限进程、用户目录备份、同步软件和误上传可直接读取明文文件。
- 配置、密钥和运行状态耦合，无法独立轮换、审计或迁移。
- 缺少统一的凭证引用、后端能力诊断、显式迁移和回退状态。
- 崩溃、并发进程、后端暂时不可用和迁移一半时的恢复规则尚未定义。
- 当前脱敏规则分散，新增 Provider、MCP 或诊断出口时容易漏接。

## 3. 目标与非目标

### 3.1 目标

1. Provider API Key、MCP OAuth Token 和 OAuth Client Secret 使用统一接口管理。
2. 配置文件只保存不含秘密的凭证引用和元数据。
3. 保持环境变量和现有明文文件完全兼容，升级后不会突然失去连接。
4. 迁移必须显式、可预览、可验证、可中断、可重试、可回退。
5. 安全后端不可用时给出可解释状态，不静默降级或复制秘密。
6. 凭证永不进入项目文件、Session、诊断、Diff、命令行参数或普通终端输出。
7. Windows PowerShell、ConstrainedLanguage 和企业策略环境均有明确行为。

### 3.2 非目标

- v0.12.3 不实现云端同步、团队共享或组织级密钥下发。
- 不把 API Key 写入 Git 仓库、项目 `.env` 或 Xiu Session。
- 不自行绕过企业安全软件、凭证策略或系统 Keychain 限制。
- 不承诺一次覆盖 Windows、macOS 和 Linux 的全部系统凭证库。
- 不自动删除旧凭证，也不把“文件权限为 0600”宣传为系统级加密。

## 4. 统一凭证模型

### 4.1 凭证引用

配置文件只保存引用：

```json
{
  "backend": "environment | legacy-file | system",
  "id": "provider:dashscope:api-key",
  "revision": 1
}
```

- `backend` 表明解析来源，不包含秘密。
- `id` 是稳定逻辑标识，不使用 Key、Token 或其哈希生成。
- `revision` 只用于缓存失效和轮换，不反映秘密内容。
- MCP OAuth 仍以 resource、Issuer、Client ID 作为身份元数据，Token 集合整体对应一个凭证引用。

### 4.2 凭证类型

- `provider-api-key`：单个 Provider API Key。
- `mcp-oauth-tokens`：Access、Refresh、ID Token、Scope 和到期元数据。
- `mcp-oauth-client`：Client ID、可选 Client Secret 和动态注册元数据。

Access Token 与 Refresh Token 必须作为同一原子记录轮换，不能分别写入造成新旧组合。

### 4.3 接口草案

```ts
interface CredentialStore {
  readonly backend: "environment" | "legacy-file" | "system";
  probe(): Promise<CredentialBackendStatus>;
  get<T extends CredentialKind>(ref: CredentialRef<T>): Promise<CredentialValue<T> | undefined>;
  set<T extends CredentialKind>(ref: CredentialRef<T>, value: CredentialValue<T>): Promise<CredentialWriteReceipt>;
  delete(ref: CredentialRef): Promise<boolean>;
  has(ref: CredentialRef): Promise<boolean>;
}
```

约束：

- `list` 只能返回 ID、类型、后端、更新时间和状态，不能返回秘密。
- 错误对象不得包含传入值、请求 Header 或系统凭证库原始输出。
- `get` 的结果只能存在于当前请求需要的最短内存生命周期内。
- 每次写入都返回 revision 和可验证回执，调用方不能根据异常猜测是否成功。

## 5. 后端策略

### 5.1 Environment 后端

- 只读，继续保持最高优先级。
- 配置保存环境变量名，不保存变量值。
- 环境变量缺失时明确显示 `missing`，不能自动回退到不相关凭证。

### 5.2 Legacy File 后端

- 包装现有 `providers.json` 与 `mcp-auth.json` 行为，保证升级兼容。
- 标记为 `legacy-file`，界面明确提示“本机明文兼容存储”。
- 在用户完成安全迁移前仍可正常读写，不能因版本升级锁死已有配置。
- 新接口接入后，Provider Registry 和 MCP OAuth 不再直接解析秘密字段。

### 5.3 System 后端

Windows 候选实现必须先做隔离技术验证，至少满足：

- 密文绑定当前 Windows 用户，复制存储文件到另一用户后不可解密。
- 不依赖 PowerShell 方法调用或 ConstrainedLanguage 放行。
- 不在命令行、标准输出、错误输出、临时文件中传递秘密。
- 安装包来源、原生二进制、架构兼容和企业拦截行为可解释。
- 后端缺失或被策略阻止时安全失败，不创建空凭证或覆盖旧记录。

因此阶段 A 不直接引入 `keytar`、自生成 EXE 或 PowerShell/.NET helper。阶段 B 将比较 Windows Credential Manager、DPAPI 封装和可信原生模块的真实安装与策略表现，再决定实现。此前剪贴板 helper 被企业策略拦截，说明“额外可执行文件”不能未经验证成为唯一后端。

## 6. 解析优先级

单个 Provider 的解析顺序：

1. 用户显式配置的环境变量。
2. 配置中的安全系统凭证引用。
3. 配置中的 Legacy File 引用。
4. Provider 的默认环境变量。
5. 缺失并提示修复方法。

规则：

- 只沿同一逻辑凭证的已声明来源回退，不能跨 Provider 猜测。
- System 后端暂时不可用时，不自动把秘密复制回明文文件。
- 安全引用损坏时不静默使用可能过期的 Legacy 值；只有迁移记录明确保留且用户选择回退时才使用。
- 本地无鉴权 Provider 保持无需凭证。

## 7. 迁移事务

迁移采用复制优先的五阶段事务：

1. **预检**：探测后端，列出将迁移的逻辑 ID、类型和来源，不显示秘密。
2. **复制**：写入 System 后端，旧记录保持不变。
3. **回读验证**：从 System 后端读取，并在内存中做恒定时间一致性比较。
4. **切换引用**：原子更新配置引用和 revision；写入不含秘密的迁移回执。
5. **可选清理**：只有用户再次明确确认后才删除 Legacy 原值。

失败规则：

- 第 1～3 步失败：不修改配置，保留旧凭证。
- 第 4 步失败：System 中的新副本标为孤立候选，后续可安全重试或显式清理。
- 第 5 步失败：仍视为已安全切换，但提示旧副本尚存；不得反复复制。
- Ctrl+C、进程崩溃和断电后，根据迁移回执恢复，不凭文件是否存在猜测状态。
- 不提供批量“迁移并立即删除旧值”的无确认快捷路径。

## 8. 生命周期命令设计

后续 CLI 计划提供：

- `/credentials`：列出逻辑凭证、来源、后端状态、更新时间和是否存在旧副本。
- `/credentials status`：诊断后端可用性，不读取或显示秘密。
- `/credentials migrate [id|--all]`：预览并执行复制、回读和引用切换。
- `/credentials cleanup [id]`：迁移完成并经过重启验证后单独执行；再次输入完整 Provider ID 才清理一个已验证的 Legacy 副本，不提供迁移后立即删除或批量清理快捷方式，`-y` 也不能代替确认。
- `/credentials rollback [id]`：显式切回 Legacy 引用；若旧副本已经清理，则从当前有效的系统凭据重新生成兼容文件副本后切回。
- `/credentials forget <id>`：删除指定凭证，需要显示影响范围并确认。

Provider 的现有“保存本地 Key”、MCP `/mcp login` 与 `/mcp logout` 保持可用；当 System 后端稳定后，它们只改为调用统一接口。CLI 永远不提供 reveal/copy-secret 命令。

## 9. 脱敏与审计边界

建立共享的 `redactSecrets` 模块，所有以下出口必须调用：

- Provider/MCP 请求错误与失败切换回执。
- Task diagnostics、活动详情、审批说明和工具输入摘要。
- Session 与 UI replay。
- MCP stderr、OAuth URL、HTTP Header 与服务端响应摘要。
- 导出的匿名诊断包。

脱敏输入包括：

- 当前任务解析过的实际敏感值（仅内存集合）。
- 字段名：key、token、secret、password、authorization、cookie、credential。
- Bearer/JWT、常见 Provider Key 和 OAuth URL 参数模式。

脱敏发生在截断和持久化之前。测试必须把 Canary Secret 注入每个出口，并扫描构建后的 Session、诊断、错误文本和临时文件。

## 10. 并发、备份与恢复

- 同一凭证 ID 的写入和轮换必须串行；跨进程使用锁文件或等价原语，锁文件不含秘密。
- 配置引用与凭证写入不能假装成一个跨系统原子事务，必须依赖可恢复迁移回执。
- 临时文件、备份和崩溃转储不得包含 System 后端明文值。
- 删除 Provider 或 MCP 配置时先显示关联凭证；默认不删除共享凭证，避免误伤其他 Profile。
- 卸载 Xiu 不自动删除系统凭证；由 `/credentials forget` 明确处理。

## 11. 威胁模型

本版本主要缓解：

- 用户目录文件、备份或同步副本被读取。
- 错误、诊断、Session、终端和临时文件泄漏。
- 迁移中断造成凭证丢失或引用悬空。
- Provider/MCP 重命名、Token 轮换和多进程写入造成错配。

不承诺防御：

- 已能以当前用户身份读取进程内存或调用系统凭证 API 的恶意程序。
- 被攻陷的操作系统、Node.js 运行时或 Xiu 依赖。
- 用户主动复制、截图或把凭证交给模型。

## 12. 分阶段交付

### 阶段 A：统一接口与兼容层

- 新建类型安全的 CredentialStore 接口、引用模型和错误分类。
- 用 Legacy File 后端承接现有 Provider/MCP 存储，默认行为不变。
- Provider 与 MCP 调用方不再直接依赖秘密所在 JSON 的具体结构。
- 建立共享脱敏模块并覆盖现有持久化和显示出口。
- 增加只读凭证状态诊断；明确显示当前使用 environment 或 legacy-file。
- Provider 能力缓存改用凭证 revision/引用失效，不使用秘密参与指纹。

阶段 A 不迁移、不删除、不加密任何用户凭证，也不修改现有文件路径。

### 阶段 B：Windows 安全后端技术验证

- 对候选方案做安装、读取、写入、删除、用户绑定、x64/ARM64、Node 20+ 和受限企业环境测试。
- 输出能力探测和失败原因，不把实验后端设为默认。
- 只有真实验证通过后才确定 System 后端实现。

阶段 B 当前实际结果：

- `cmdkey.exe` 可以写入和删除通用凭证，但没有可靠、受支持的秘密回读接口，不能实现 `CredentialStore`，因此排除。
- 直接通过 PowerShell/.NET 调用 DPAPI 在企业 `ConstrainedLanguage` 环境会被方法调用策略阻断，不能成为唯一实现，因此排除。
- 已选择仍在维护、提供 Node-API 预编译包的 `@napi-rs/keyring` 作为可选候选模块；Windows x64、ARM64 和 ia32 均有独立可选包，模块缺失或被策略拦截时 Xiu 必须安全降级到现有兼容路径。
- 已实现 `WindowsSystemCredentialStore` 和 `/credentials probe`。普通状态检查只加载模块；写探针必须由用户明确确认，使用唯一随机 ID 和随机秘密完成写入、回读、删除及删除后不存在校验，并在失败路径尝试精确清理。
- 当前 Windows x64、Node 22、PowerShell `ConstrainedLanguage` 真实探针已通过，探针前后均无残留；单元测试覆盖轮换 revision、损坏信封、大小限制和原生错误脱敏。
- 当前阶段 B 自动基线为 316 项测试，并通过类型检查、正式构建、npm 打包预览及 Node 22 独立目录安装。正常安装可加载系统后端；使用 `--omit=optional` 安装时 CLI 仍可启动，并把系统后端报告为不可用。
- Node 官方便携版 v20.20.2 已校验官方 SHA-256，并在同一 Windows x64、`ConstrainedLanguage` 机器完成 CLI 启动、原生模块加载及真实写入、回读、删除探针，无需切换全局 Node。
- 尚未完成 Windows ARM64 和另一台企业策略机器验收，因此不把该后端设为默认来源。阶段 C 只有经用户确认、复制并回读成功的 Provider 引用才会选择它；不会因系统中碰巧存在同名记录而推断迁移状态。
- Windows Credential Manager 单条秘密容量有限，当前实现设置 `2400` UTF-8 字节安全上限。阶段 C 可先覆盖 Provider API Key；阶段 D 在迁移 MCP OAuth 记录前必须设计并验证超限处理，不能静默截断或降级为明文。

### 阶段 C：Provider 显式迁移

- 已实现 `/credentials` 状态、迁移、单项清理、显式回退和忘记命令。
- 只支持 Provider API Key；单项与 `--all` 批量迁移均先复制和常量时间回读比较，再一次性切换非秘密引用。
- 成功迁移默认保留旧明文，运行时不会从系统引用静默回退；清理必须再次确认，批量迁移不附带批量清理快捷方式。
- `/credentials rollback` 在旧副本已清理时可从当前有效系统凭证恢复 Legacy 副本；`/credentials forget` 经影响预览后删除系统、兼容文件及确定性中断残留。
- MCP OAuth Token/Client Secret 不在本阶段迁移。

### 阶段 D：MCP OAuth 迁移

- 已将 Access/Refresh/ID Token 与 Client Secret 组成一个原子秘密记录；Scope、到期时间、Token 类型和 Client 注册元数据留在非秘密文件层，避免 Cloudflare 等超长 Scope 触发 Windows 单条记录容量限制。
- 已实现 `/mcp credentials status|migrate|cleanup|rollback [name]`。迁移使用准备意图、系统回读常量时间校验、引用切换和可恢复回执；默认保留旧明文，清理要求输入完整 MCP 名称。
- 已迁移凭证的刷新轮换整体更新系统秘密；注销同时清除系统与保留旧副本中的 Token，保留 Client 和全部忘记两种语义不变；系统引用不可用时不静默回退。
- 定向自动测试已覆盖超长公开 Scope、秘密不落文件、清理后回退、轮换 revision、OAuth 刷新、Scope 升权、注销及 MCP 连接兼容性。仍需真实 Cloudflare OAuth 迁移、重启、刷新/注销和企业策略机器验收后进入阶段 E。

### 阶段 E：发布收口

- 已完成全量自动化 Canary 脱敏扫描：Provider、媒体、MCP 启动/工具/Resource/Prompt、OAuth 刷新与注销错误会在截断和持久化前使用运行时实际秘密值脱敏。
- 已完成同进程并发、迁移中断续跑、损坏系统副本拒绝清理与旧副本保留测试；失败不会覆盖有效恢复数据。
- 已通过 329 项全量测试、TypeScript 类型检查、正式构建、200 文件 npm 包审计和官方 Registry 隔离安装，安装候选返回 `0.12.3`。
- README、使用说明、发布说明与路线图已同步；真实企业策略机器、Windows ARM64 和 Cloudflare OAuth 迁移/重启/注销保留为发布前外部验收项。

## 13. 阶段 A 验收标准

1. 现有 `providers.json`、`mcp-auth.json` 无需用户操作即可继续使用。
2. 升级、运行和测试不会移动、删除、重写或暴露真实用户凭证。
3. Provider API Key、MCP Token、Client Secret 均通过统一接口读取。
4. 环境变量优先级、本地无鉴权 Provider、Provider 编辑保留 Key、MCP 刷新和注销行为不退化。
5. 凭证引用、错误、状态和迁移元数据中不存在秘密或秘密哈希。
6. 能力探测缓存失效不再依赖秘密内容。
7. Canary Secret 不出现在 Session、诊断、OAuth 错误、失败切换、UI replay 或测试临时输出。
8. 损坏文件、缺失凭证、无权限、同进程并发写入和 Ctrl+C 都产生可解释错误，且不覆盖有效旧数据；跨进程锁与冲突恢复作为阶段 B 安全后端验证项，不在兼容层中虚假承诺。
9. Windows PowerShell 与 ConstrainedLanguage 不需要额外放权即可继续使用 Legacy File 兼容路径。
10. 全量 typecheck、test、build、pack 与隔离安装通过后，才可进入阶段 B。

## 14. 关键决策

- **兼容优先**：系统凭证库不是一次性替换，而是可选后端和显式迁移目标。
- **不静默降级**：安全后端不可用时提示修复或显式回退，不复制秘密到明文。
- **不静默清理**：回读验证和切换成功后，旧副本仍需再次确认才删除。
- **先抽象后选型**：阶段 A 不绑定未经真实企业环境验证的原生依赖。
- **秘密不参与身份**：引用、缓存键和审计 ID 只来自稳定元数据与 revision。
- **核心安全边界不变**：凭证存储不能绕过工作区信任、风险审批、Plan 模式、检查点或 dangerous 确认。
