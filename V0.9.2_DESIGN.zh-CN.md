# Xiu v0.9.2：修复右键鼠标报告泄漏

## 问题

Windows Terminal 发出的 SGR 右键报告形如 `ESC[<2;51;21M`。Node.js `readline.emitKeypressEvents()` 会把它拆成一个 `ESC[<` 前缀事件和多个普通字符事件，导致 v0.9.1 的完整序列解析没有命中，坐标尾部 `2;51;21M` 被插入输入框。

## 修复

- 在交互编辑器中增加有界鼠标序列重组状态机。
- 识别 Node.js 分片的 SGR 按下与释放报告，也兼容一次性交付和 X10 报告。
- 从前缀开始吞掉完整坐标序列，任何片段都不会进入编辑文本。
- 序列等待窗口限制为 250 毫秒，最大长度限制为 64 字符，异常输入会立即退出鼠标解析状态。
- 右键按下触发一次剪贴板读取，释放只结束事件，不重复粘贴。

## 回归样本

自动化测试直接向 Node.js `PassThrough` 输入真实问题序列 `ESC[<2;51;21M` 和 `ESC[<2;51;21m`，验证收到一次右键按下、一次释放，且编辑器泄漏文本为空。
