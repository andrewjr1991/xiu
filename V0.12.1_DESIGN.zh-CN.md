# Xiu v0.12.1 设计：MCP Resources 与 Prompts

## 目标

在不扩大现有写入权限的前提下，让用户可以发现、查看并显式引用 MCP Server 提供的 Resources、Resource Templates 与 Prompts。

本版本不把远端 Resource 或 Prompt 自动注入模型上下文，也不允许它们绕过工作区信任、风险审批、Plan 只读和危险操作确认。

## 命令

- `/mcp resources [server]`：列出 Resource 与 Resource Template；未指定服务时可选择。
- `/mcp read [server] [uri]`：通过 MCP `resources/read` 读取资源；参数不足时使用选择器。
- `/mcp prompts [server]`：列出 Prompt 与参数说明。
- `/mcp prompt [server] [name] [JSON]`：调用 `prompts/get` 并只在终端展示结果；缺少必填参数时交互询问。

## 安全边界

1. Resource URI 只交给原 MCP Server 解析，Xiu 不自行访问该 URI。
2. Resource 和 Prompt 内容均显示“不可信远端内容”提示；终端查看不等于模型指令。
3. 文本单块最多显示 32K 字符、单次合计最多 64K 字符；超出部分明确标记截断。
4. Blob、图像、音频等二进制内容不输出 Base64，仅显示 MIME、估算字节数和省略原因。
5. 单类列表最多读取 20 页、500 项；拒绝循环游标和异常响应。
6. 所有调用支持取消和超时；OAuth 错误继续经过脱敏处理。
7. 本版本的 Resources / Prompts 只供用户显式浏览，不注册为 Agent Tool。

## 协议实现

- stdio：使用现有 JSON-RPC 通道增加四组请求和统一分页器。
- Streamable HTTP：使用官方 MCP Client 对应 API，保持现有 Session、OAuth 刷新和 URL 安全策略。
- Manager：提供按 Server 查询的规范化结果，CLI 不直接接触传输对象。

## 验收标准

- stdio 与 Streamable HTTP 均可列出并读取 Resource、列出并获取 Prompt。
- 不支持某项能力的 Server 返回清晰提示，不影响 Tools 连接。
- 分页、循环游标、超长文本、Blob、省略和取消均有自动化测试。
- 中文与英文界面一致，帮助文档和路线图同步。
- typecheck、完整测试、build、npm pack 与洁净安装通过。
