# Xiu v0.17.0 设计：可验证的产品基础

## 目标

让陌生开发者能够快速理解、安装和验证 Xiu，并把发布规则转化为机器执行的门禁。v0.17.0 不新增模型能力，重点是发布追溯、CI、准确的中英文入口文档、平台边界和首次运行验证。

## 交付范围

- 建立 Windows、macOS、Linux 的自动化类型检查、测试、构建、打包和干净安装冒烟验证。
- 将 README 中的版本堆叠迁出第一屏，提供准确的英文和简体中文入口。
- 建立 `CHANGELOG.md`、`QUICKSTART.md` 和 `CONTRIBUTING.md`。
- 将路线图收敛为唯一当前状态、唯一当前版本目标和少量下一步动作。
- 将“不伪造隐藏思维链”固化为永久安全边界。
- 明确 Windows 是当前验收最充分的平台；macOS/Linux 只声明由 CI 或真实外部验收证明的能力。

## 非目标

- 不实现 macOS Keychain 或 Linux Secret Service 迁移。
- 不自动发布 npm、不自动更新或修改全局 npm/PATH。
- 不进行提示词裁剪、工具集裁剪或大规模 `cli.ts` 重构。
- 不声称满足某项监管认证，也不把尚未验证的平台能力写成稳定支持。

## 安全约束

- CI 的 Pull Request 路径不读取模型、npm 或其他外部服务密钥，不运行真实模型任务。
- 打包冒烟测试只在临时目录安装当前工作区生成的包，不修改全局安装。
- 发布仍需用户显式决定；tag 必须指向与已发布内容对应的精确提交。
- 现有工作区信任、Plan 只读、风险审批、检查点、凭证与恢复边界保持不变。

## 验收标准

- 三平台 Node.js 20.18.1 CI 均执行 typecheck、完整测试、build、pack 和临时目录安装验证。
- README 前 30 行能够说明定位、安装命令和第一个任务，且所有本地链接存在。
- 中英文 README 的平台声明、限制和安全表述一致。
- `ROADMAP.zh-CN.md` 只有一处当前开发版本和一组下一步动作。
- `npm run typecheck`、`npm test`、`npm run build`、`npm pack --dry-run --json`、`npm run smoke:package` 全部通过。
