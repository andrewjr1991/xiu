# Xiu v0.13：可靠执行与无人值守长任务

## 1. 版本目标

v0.13 要让 Xiu 的长任务具备三个程序级保证：

1. **可恢复**：进程或终端中断后，能从明确的安全边界继续。
2. **不重复副作用**：不能因为重试或恢复再次执行已经成功或结果未知的写操作。
3. **可解释**：用户能看到预算、重试、停滞、恢复点、验证和最终执行报告。

## 2. 非目标

- 不在本版本默认启用云端控制台或上传诊断。
- 不允许模型自行决定跳过信任、审批、验证或危险操作确认。
- 不承诺任意外部工具都天然幂等。
- 不在恢复链路稳定前实现完全无人监管的自动发布或自动合并。
- 不把安全审计日志本身等同于任务恢复。

## 3. 关键模型

### 3.1 Task Run

每次任务使用稳定 `runId`，记录主目标、工作区摘要、会话、当前阶段、预算和终止原因。用户 steering 是附加约束，不能替换主目标。

### 3.2 Operation

模型请求、工具调用、MCP 调用、文件变更、验证和检查点都是有稳定 `operationId` 的操作。操作状态只允许：

- `planned`
- `started`
- `succeeded`
- `failed`
- `cancelled`
- `unknown`

`unknown` 表示请求可能已经产生副作用但没有获得确定回执，恢复时必须等待用户或专用查询证明，禁止自动重放。

### 3.3 Recovery Point

恢复点只建立在程序能够证明状态一致的位置，例如：

- 模型请求开始前。
- 只读工具完成后。
- 文件写入和 Diff 已落盘后。
- 命令退出码与输出已记录后。
- 验证结果已形成证据后。

恢复点保存必要的结构化状态和引用，不复制完整 Prompt、源码、秘密或无界输出。

## 4. 阶段计划

### v0.13.0 / 阶段 0：审计事实基础（已发布）

- 本机有界安全审计账本。
- 审批与 Provider/MCP 凭证生命周期事件。
- `/audit` 分类查询、损坏行隔离、秘密脱敏与轮转。

它为后续报告提供安全事实，但不承担任务恢复。

### v0.13.1 / 阶段 A：崩溃恢复最小闭环

- 已定义 Task Run、Operation、Recovery Point 和有界事件历史的 `version: 1` 存储格式。
- 已在模型、工具、文件变更、检查点、steering 和验证边界原子更新运行状态。
- 已实现启动检测与 `/recover`，展示最后证据、未决操作和可选动作。
- 第一版只允许用户确认后恢复；已成功或状态未知的副作用操作通过调用签名阻止自动重放。
- 恢复会建立新的 Task Run 并引用旧 `runId`；放弃在旧记录中追加事件，不删除原证据。

### v0.13.2 / 阶段 B：统一重试

- 已建立跨模型、工具、MCP 和媒体的统一错误分类。
- 重试决策同时检查错误类型、显式重放安全性、流式输出和副作用提交状态。
- 使用最多三次的有界退避、`Retry-After` 上限和可取消等待。
- 认证、权限、参数、取消、已输出内容和未知提交结果不重试；只读 MCP、媒体轮询与已有资源下载可以安全恢复。

### v0.13.3 / 阶段 C：预算与停滞

- 任务级 Token、时间、模型、工具和失败预算已经实现；重试计入模型/工具调用与失败预算。
- 已明确区分运行、等待用户、等待审批、退避、后台和停滞。
- 预算耗尽时在安全边界保存恢复点并解释，不将其标记为完成。

### v0.13.4 / 阶段 D：后台与断线续跑

- 后台进程有稳定句柄、输出游标、取消状态和退出证据。
- 前台退出不等于取消；重新启动后可以发现和接管。
- 任何需要新审批的步骤必须停下等待用户。

### v0.13.5 / 阶段 E：执行报告

- 将任务运行记录、诊断、文件变化、验证和安全审计汇总为有界报告。
- 报告默认脱敏，可导出 Markdown 或 JSON。
- 导出是用户显式动作，不自动上传。

### v0.13.6 / 阶段 F：多 Agent 安全合并

- 检测文件、符号、依赖、测试和 Worktree 冲突。
- 未验证或存在未知副作用的结果不能自动合并。
- 保留用户修改和所有无法安全清理的 Worktree。

## 5. 阶段 A 验收矩阵

必须自动覆盖：

- 模型请求前、等待中和返回后强制终止。
- 只读工具与写工具开始前、执行中和回执后强制终止。
- 文件写入成功但验证尚未开始。
- 验证进程运行中和验证完成后。
- 状态文件部分写入、损坏、版本未知、并发启动和工作区移动。
- 恢复时 Provider、权限、文件内容或 Git 状态已经变化。
- `unknown` 操作不能自动重放。
- 日志不可写时安全失败，不绕过审批或假装可恢复。

## 6. 数据与隐私

- 运行状态默认保存在本机用户目录，项目之间隔离。
- 使用不可逆工作区摘要和稳定对象 ID，避免记录完整路径。
- 不保存凭证、Authorization、Cookie、完整 Prompt、模型思维链或无界命令输出。
- 文件内容继续由工作区和 Git/检查点承担，不复制进运行日志。
- 所有显示、导出和错误在截断前经过统一秘密脱敏。

## 7. 完成定义

v0.13 只有在以下条件全部满足后才能发布稳定版：

- 阶段 A 至 F 的承诺均实现或在发布说明中明确降级范围。
- 强制终止与恢复矩阵通过。
- 重试不会重复已知或未知副作用。
- 预算和停滞状态与实际运行一致。
- 后台任务能在终端重启后安全接管。
- 执行报告足以独立复查任务结果。
- 多 Agent 冲突不会静默覆盖修改。
- 全量自动化、构建、打包和干净安装通过。
