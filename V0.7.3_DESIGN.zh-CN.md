# Xiu v0.7.3：主目标保持与任务契约审计

## 一、真实失败

v0.7.2 在真实闪购预标注任务中成功接收了运行期 steering“看下助手还可以优化什么，给些建议”，但模型随后只输出助手优化建议，没有完成原始要求中的两份 HTML 预标注和最终在线表格，CLI 仍显示：

```text
Done - 12 turn(s), 33 tool call(s), no changes
```

这说明“消息送达”不等于“目标语义保持”。较新的、容易回答的补充要求可能覆盖较早的复杂主任务。

## 二、目标

1. 原始任务在一次 Agent 运行内始终是不可覆盖的主目标。
2. 普通运行期输入只能增加要求，不能降低或替换主目标。
3. 有 steering 的任务在结束前必须重新审计主目标与全部补充要求。
4. 不增加普通无 steering 任务的额外模型调用。

## 三、状态模型

每次 `Agent.run()` 保存两类状态：

- `primaryTask`：本次运行最初提交的完整任务。
- `steeringHistory`：本次运行期间接受的全部附加要求。

每次注入 steering 时，发送给模型的内容同时包含：

```text
PRIMARY GOAL (still mandatory)
ADDITIONAL REQUIREMENTS
NEWLY RECEIVED IN THIS TURN
```

这样即使上下文较长，最新消息仍会重新锚定主目标。

## 四、结束拦截

当模型收到 steering 后首次返回无工具调用的完成响应，Xiu 不立即接受，而是追加一次任务契约审计；如果审计后又收到新的 steering，结束前会再次审计新增后的完整任务契约：

- 重新列出 PRIMARY GOAL。
- 重新列出全部 ADDITIONAL REQUIREMENTS。
- 要求模型基于具体证据逐项检查。
- 任一项目未完成时必须继续使用工具。

每一批 steering 最多触发一次审计，避免模型无法完成时形成无限结束循环。之后仍受原有计划门、验证门、最大 Turn 和循环保护约束。

## 五、工具选择纠正

真实记录还出现了把 `.html` 直接传给图片分析工具的无效调用。系统提示新增明确约束：HTML、Markdown、JSON、源代码等是文本，应使用读取或搜索工具；需要视觉检查时先渲染为截图，再分析图片。

## 六、验收标准

- steering 消息必须同时包含原始主目标和新增要求。
- 模型只回答新增要求并尝试结束时，Agent 必须再发起一轮任务契约审计。
- 审计消息必须包含原始主目标和全部 steering。
- 原有循环保护、验证门、审批、安全边界和无 steering 任务行为不退化。
- typecheck、全部测试、build、审计、pack 和隔离安装通过。
