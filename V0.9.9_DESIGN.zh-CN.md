# Xiu v0.9.9 稳定性修复设计

## 目标

修复真实长任务中暴露出的交互可靠性问题，确保语言、验证、技能、恢复会话、进度和后台输入状态都可预测、可核验。

## 范围

1. 简体中文模式对模型自然语言输出执行确定性简体化，代码块和行内代码保持原样。
2. 验证工具通过后明确显示“正在整理最终结果”，避免用户误以为仍在验证。
3. 技能目录兼容 `.agents/skills`，每次打开 `/skills` 前刷新，并在任务结束后发现外部安装的技能。
4. 终端选择器兼容 Windows 原始方向键序列，并支持数字键直接选择。
5. 恢复会话后展示最近的用户与 Xiu 对话摘要，而不是只显示消息数量。
6. 长进度按终端宽度换行，详情模式展示更多活动并标明省略数量。
7. 工作区变化不再中断后台补充输入，杜绝空“补充>”反复刷屏。

## 验收标准

- 中文模式下普通回复不出现繁体字，代码内容不被转换。
- 验证通过后的等待阶段有明确状态提示。
- `.agents/skills` 中的技能能被 `/skills` 立即发现。
- Windows 终端中方向键和数字键均可选择技能或会话。
- `/resume` 后能看到最近会话内容，并可用 `/history` 查看完整记录。
- 长进度不再横向截断，详情显示有明确的总数信息。
- 文件修改期间输入框保持稳定，不产生空补充循环。

## 非目标

- 本版本不扩展新的模型 Provider。
- 不跳过模型最终总结，也不把“命令执行成功”误判为“业务验证通过”。
