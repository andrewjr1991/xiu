# Xiu v0.9.3：企业环境无 helper 剪贴板后端

## 问题

部分企业终端会阻止 Xiu 在用户目录编译或运行 `xiu-clipboard.exe`。v0.9.2 仍会开启鼠标上报，因此 helper 不可用时既无法读取附件，也会接管终端原生右键文字粘贴。

## 方案

- 优先调用 Windows PowerShell 自带的 `Get-Clipboard` cmdlet读取文字、文件拖放列表以及位图存在状态。
- PowerShell 将结构化结果写入一次性 UTF-8 JSON，Node.js 读取后立即清理；不编译、不下载、不运行 Xiu 自有 EXE。
- 文件和文字不再依赖 clipboard helper。
- 纯位图仍需要能够读取像素并保存 PNG 的系统接口；此时才尝试旧 helper。若被策略阻止，提示用户先把图片保存为文件。
- 启动交互输入前探测 `Get-Clipboard`。若原生后端不可用且没有已缓存 helper，则不开启鼠标上报，保留终端原生右键文字粘贴。

## 安全与兼容性

- 不使用 `ExecutionPolicy Bypass`，不绕过集团策略。
- PowerShell 使用 `-NoProfile -NonInteractive -STA`，结果文件位于受信工作区附件目录并采用唯一名称。
- 原有附件数量、单文件大小、总大小、目录与符号链接限制保持不变。
- `Ctrl+V` 和 `/paste` 继续使用相同后端与错误说明。

## 验收标准

- helper 不可用时，文字和 Explorer 文件仍可通过 `Get-Clipboard` 导入。
- 原生后端不可用时不接管右键。
- 检测到位图但 helper 被阻止时给出可执行的保存文件提示。
- Unicode 文本和中文路径通过 UTF-8 JSON 保真。
