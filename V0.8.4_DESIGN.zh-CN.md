# Xiu v0.8.4 剪贴板附件设计

## 目标

让用户无需手工保存截图、输入长路径或把项目外文件暴露给工具，即可在终端输入中引用图片和文件，同时保持普通文字粘贴、工作区边界和安全审批不变。

## 用户交互

- `Ctrl+V` 在当前光标位置粘贴剪贴板内容。
- 普通文字直接进入编辑器。
- Windows 剪贴板图片保存为 `.xiu/attachments/<timestamp>-clipboard.png`，输入框插入对应 `@` 引用。
- 资源管理器复制的外部文件被复制到附件目录；工作区内文件直接生成相对引用。
- `/paste` 执行相同导入，适用于终端自身拦截 `Ctrl+V` 的情况。
- 输入框底部显示读取中、成功或失败状态；失败不清空现有输入。

## Windows 实现

当前 PowerShell 可能处于 Constrained Language，不能调用图片对象的 `Save` 方法。Xiu 首次需要二进制剪贴板时，使用 Windows 自带 .NET Framework `csc.exe` 将固定 C# 源码编译到 `~/.xiu/cache/clipboard-helper-v1/`。辅助程序以 STA 模式运行，只执行三项操作：读取 FileDropList、把 Clipboard Image 保存为指定 PNG、读取普通文字。输出值使用 UTF-8 Base64 传回 Node，避免中文路径和控制字符破坏协议。

辅助程序不会访问网络、执行附件或修改项目源文件。若系统没有 `csc.exe`，返回明确错误；普通终端文字粘贴和 `@path` 不受影响。

## 导入和路由

附件最终必须位于当前工作区：

- 图片由辅助程序直接写入 `.xiu/attachments/`。
- 项目外文件通过 `copyFile` 导入，并使用时间戳和清理后的原文件名避免碰撞。
- 已在工作区内的普通文件不复制。
- 输入引用使用正斜杠；包含空格时使用 `@"path with spaces"`。

系统提示把 `.xiu/attachments` 视为用户提供的附件。支持的图片调用 `analyze_image`，文本和代码调用 `read_file`；粘贴本身永远不授权执行附件。

## 安全限制

- 单次最多 10 个文件。
- 单文件最多 25 MB，总计最多 50 MB。
- 只接受普通文件，拒绝目录和符号链接。
- 后端返回的图片路径必须与 Xiu 指定的输出路径完全一致。
- 附件目录继续由 `.gitignore` 排除，不进入 npm 包或 Git 提交。
- 所有后续模型工具仍受工作区信任、风险审批、Plan 只读和 dangerous 明确确认约束。

## 验收标准

- 普通中英文文字粘贴不创建附件。
- Windows 截图剪贴板生成可读取的 PNG 和正确 `@` 引用。
- 外部文件被复制，工作区内文件不重复复制，空格路径正确引用。
- 目录、符号链接、超数量和超大小输入得到明确错误。
- 异步粘贴不破坏 Unicode 光标、运行期输入框、草稿或 Slash 命令菜单。
- `/paste` 在普通输入和任务运行期间都可用。
- 图片附件可以被视觉工具读取，文本附件可以被文件工具读取。
- 类型检查、全部自动化测试、构建、官方 Registry 审计、打包和干净安装通过。
