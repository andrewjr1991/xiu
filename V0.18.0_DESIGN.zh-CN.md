# Xiu v0.18.0 设计：可重复评测与成本基线

> 状态：设计与 B 阶段（评测基础设施及首批 10 题）已于 2026-08-21 验收通过。真实模型基线仍未授权，不调用真实模型、不产生评测费用。

## 1. 决策摘要

v0.18.0 先建立可重复、可比较、保持现有安全边界的评测系统，再判断是否优化提示词或工具集。正式比较以精确的已发布 v0.17.0 工件为基线；候选版本必须使用相同任务修订、Provider、模型、参数和试验次数。

普通 PR CI 只执行确定性、无网络、无密钥的模拟 Provider 冒烟。真实模型评测只能由受信任环境显式触发，且必须先确认运行配置和预算。任何优化都不得绕过工作区信任、Plan 只读、风险审批、检查点、危险操作确认或秘密脱敏。

## 2. 目标与非目标

### 目标

1. 定义稳定的任务、套件、运行结果和基线比较协议。
2. 首批交付 10 个固定任务，发布前扩展到 20 个。
3. 用确定性断言衡量完成、验证、改动范围、恢复和安全拒绝。
4. 记录质量、Token、调用、耗时、重试、审批、文件改动和可选费用。
5. 区分任务失败、模型失败、Provider 故障和评测基础设施故障。
6. 让 v0.17.0 与 v0.18.0 的结果可复查、可重跑、可追溯。

### 非目标

- 不在真实基线配置另行获批前运行付费评测。
- 不采集用户遥测，不默认上传代码、会话、报告或诊断数据。
- 不让 fork PR、普通 PR 或不受信任工作区接触真实 Provider 密钥。
- 不使用主观模型裁判决定硬性通过与否。
- 不在基线获批前裁剪提示词或工具集，不借本版本大规模重构 CLI 或 Agent。
- 不自动发布 npm 包，不覆盖已发布版本。

## 3. 评测目录与协议

计划新增：

```text
evals/
  README.md
  schema/
    task.schema.json
    result.schema.json
  suites/
    smoke.json
    baseline.json
  tasks/<task-id>/
    task.json
    repo/
    assert.mjs
  lib/
    isolation.mjs
    provider.mjs
    result.mjs
  validate.mjs
  run.mjs
  report.mjs
  compare.mjs
  baselines/v0.17.0.json
```

每个 `task.json` 至少固定：协议版本、任务 ID 与修订号、类别、用户请求、fixture 内容摘要、允许能力、禁止副作用、验证命令、预算和超时。套件引用精确任务修订，不以目录当前内容隐式漂移。

每次运行记录：Xiu 版本与源码提交/包完整性、评测器版本、任务与套件摘要、Node/操作系统、Provider/模型、非秘密参数、试验编号、开始结束时间和结果模式版本。报告不得写入密钥、Authorization、Cookie、完整环境变量或 Provider 未返回的隐藏思维链。

计划命令：

```powershell
npm run eval:validate
npm run eval:smoke
npm run eval:run -- --suite baseline --trials 3
npm run eval:report -- --input <results-directory>
npm run eval:compare -- --baseline evals/baselines/v0.17.0.json --candidate <report.json>
```

这些命令在设计审核通过后才实现，参数名称可在实现评审中微调，但安全模式和结果字段不能静默放宽。

## 4. 任务集

首批 10 个任务建议分布：

| 类别 | 数量 | 核心断言 |
| --- | ---: | --- |
| 单文件缺陷修复 | 3 | 指定测试通过；修改保持在允许范围 |
| 跨文件修改 | 2 | 接口与调用方一致；回归测试通过 |
| 补充测试 | 1 | 新测试能捕获给定缺陷且全套通过 |
| 架构问答 | 1 | 命中预定义事实，并引用正确仓库路径/符号 |
| 崩溃恢复 | 1 | 从安全检查点恢复；未知副作用不自动重放 |
| 安全拒绝 | 2 | 明确拒绝或请求确认；禁止副作用为零 |

发布前扩展为 20 个：单文件 5、跨文件 4、补测试 3、架构问答 3、崩溃恢复 2、安全拒绝 3。fixture 应最小、可再分发、无真实秘密，不依赖浮动外部服务或当前时间。

硬性结论由 `assert.mjs`、测试退出码、结构化事件和受控文件系统快照决定。架构问答使用预定义事实、路径和符号集合校验；自然语言质量仅作为人工备注，不改变机器通过结论。

## 5. 运行模式与安全隔离

### 5.1 PR 确定性冒烟

- 使用脚本化模拟 Provider，固定输出与工具调用序列。
- 禁止外网，禁止读取 Provider 密钥，使用临时 HOME、配置和工作区。
- 覆盖任务加载、预算终止、断言、报告、崩溃恢复与安全拒绝路径。
- 同一提交连续两次运行应产生相同的通过结论；时间戳和临时路径等非确定字段在比较前规范化。

### 5.2 真实模型基线

- 仅允许本地显式命令或受保护的手动工作流触发，不由 `pull_request` 自动触发。
- 运行前展示 Provider、模型、任务数、试验数及 Token/调用/时间/费用上限，得到明确确认后开始。
- 每个试验使用新的临时工作区和配置目录；仅注入明确允许的 Provider 凭证，不继承项目或用户的其他秘密。
- 保留正常工作区信任与风险审批语义；评测配置只能预声明允许动作，不能用全局 `--yes` 绕过确认。
- 安全任务使用虚假凭证、不可路由目标和拦截器，不能依靠“期望模型拒绝”来保护真实系统。
- 达到任一预算、超时或取消信号立即停止后续调用，并写入可恢复的部分报告。

默认单试验硬上限建议为 10 分钟、30 次模型调用、80 次工具调用、20 万输入 Token 和 2 万输出 Token；套件费用上限必须由运行者按当时价格另行确认。正式实现时可收紧默认值，不得自动扩大。

## 6. 指标与失败分类

每个试验至少记录：

- `passed`、`verified`、断言明细和安全结果；
- 输入/输出/缓存 Token（Provider 可提供时）、模型调用与工具调用；
- 总耗时、重试、失败、审批请求和拒绝；
- 修改文件数、允许范围外文件数和工作区残留；
- Provider/模型错误、预算终止、超时、中断和恢复结果。

费用由 Token 与带日期的价格快照派生，作为可选指标；跨时间比较以 Token 为主，避免价格变化伪造成优化。

失败分为 `task_assertion`、`model_behavior`、`provider`、`harness`、`timeout`、`budget`、`interrupted` 和 `safety`。Provider 与 harness 故障不计入模型成功率分母，但必须单列发生次数，不能删除失败试验或静默重跑。模型重试仍属于同一试验并进入成本与重试指标。

## 7. 基线与比较方法

1. 锁定任务修订、评测器提交、Provider、模型、参数和预算。
2. 从 npm 官方 Registry 获取精确 v0.17.0 包，记录版本、完整性摘要和文件清单；不得用附近源码提交替代。
3. 先做一次小规模连通性试跑，修复的只能是评测基础设施或无歧义任务缺陷；任何任务变更都提升修订号并使旧结果不可直接比较。
4. v0.17.0 与候选版本按相同顺序各运行三次；保存逐试验 JSON 和汇总报告。
5. 人工复查所有安全失败、无关改动、未知副作用和机器断言边界案例。
6. 小样本只报告计数、比率、中位数和范围，不宣称统计显著性。

基线报告经审核并提交后，才允许优化。每次优化只改变一个有记录的变量，并生成与 v0.17.0 基线及优化前候选的双重对照。

## 8. 验收计划

### A. 设计验收（已通过）

- 用户已确认任务类别、10/20 个任务分布、安全隔离、失败分类和发布门槛。
- 真实基线所用 Provider、模型和总费用上限仍是进入 C 阶段前的独立授权门禁；三次试验方案保持不变。
- A 阶段通过后只授权实现无网络、无真实凭证的 B 阶段，不隐含付费评测授权。

### B. 评测基础设施与首批 10 题

当前状态：已验收通过；本地连续两次模拟运行均为 10/10，通过结论一致，三平台 CI 接入、结果脱敏和 npm 包排除本地报告均已完成。

- `eval:validate` 能拒绝缺字段、摘要不匹配、越界权限和无预算任务。
- 10 个 fixture 均无秘密、无外网依赖、许可清晰且能独立复位。
- 模拟 Provider 冒烟在 CI 连续两次结论一致，且进程环境中无真实 Provider 密钥。
- 结果 JSON 通过模式校验；中断后保留部分结果，报告器可区分所有失败类型。
- 针对路径逃逸、符号链接/联接点、预算耗尽、断言自身崩溃和秘密脱敏补充测试。

### C. v0.17.0 正式基线

- Registry 工件版本与完整性可复查，配置与价格快照归档。
- 10 个任务各三次运行完成；基础设施失败单列并按预先规则补跑。
- 报告含逐题证据、聚合指标、异常与人工复核结论，不含秘密。
- 用户审核并接受基线后，才开始优化或扩题。

### D. 扩展、对照与发布

- 扩展到 20 个任务后冻结修订，v0.17.0 与候选各完成三次正式对照。
- 安全拒绝正确率 100%，无秘密泄漏、未知副作用或越界写入。
- 候选成功率、验证率不低于基线；允许范围外改动数不高于基线。
- Token、耗时和费用完整报告；将 40% Token 降幅作为优化目标，不以伪造缓存命中、减少验证或扩大失败率达成。
- `npm run typecheck`、`npm test`、`npm run build`、`npm pack --dry-run --json`、`npm run smoke:package` 全部通过，文档与唯一当前设计稿一致。
- 所有报告和差异由用户人工审核后，才决定合并与发布。

## 9. 风险与控制

| 风险 | 控制 |
| --- | --- |
| 针对固定题过拟合 | 任务与 runner 分离；保留扩展题；所有任务修订可追溯 |
| 模型和网络波动 | 三次试验；记录完整配置；基础设施故障单列 |
| 费用失控 | 运行前确认总预算；多维硬上限；可取消并保留部分报告 |
| 密钥或源码泄漏 | 临时隔离环境；最小凭证注入；统一脱敏；默认本地保存 |
| 安全题产生真实副作用 | 虚假目标、调用拦截和文件系统边界；禁止依赖模型自律 |
| fixture 或断言错误 | 任务模式校验、内容摘要、独立测试和人工复核 |
| 指标被优化目标扭曲 | 成功、验证和安全为硬门槛；成本仅在门槛满足后比较 |

## 10. 审核后实施顺序

1. 实现模式、校验器、隔离层、结果写入和报告器。
2. 实现模拟 Provider 冒烟及其 CI，不接入真实密钥。
3. 编写并逐题审核首批 10 个任务与断言。
4. 运行并审核精确 v0.17.0 基线。
5. 决定是否进行一次有限优化，并扩充到 20 个任务。
6. 完成正式对照、标准发布验证和发布审核。
