# Xiu 安全与隐私边界

本文档描述跨版本长期有效的安全规则。版本计划见 `ROADMAP.zh-CN.md`，具体使用方法见 `USAGE.zh-CN.md`。

## 1. 核心边界

- 未信任工作区不得读取项目指令、加载项目 MCP、修改文件或执行命令。
- 一次性命令、`-y` 和初始 Prompt 不得绕过工作区信任；确认前不得加载项目 Skills。
- Plan 模式保持只读，不能通过 Skill、MCP、插件、子 Agent 或模型文本绕过。
- 风险审批由程序策略执行，模型不能自行宣告获得权限。
- 删除、覆盖、回退、发布、凭证清理及其他危险操作必须显式确认。
- 检查点和验证证据不能由模型自然语言替代。

## 2. 工具与命令

- 优先使用 `program + args[]` 的原生进程执行，减少 Shell 二次解释。
- Shell 命令保留给确实需要 PowerShell 语法的场景，并接受更严格风险判断。
- 已产生输出或副作用、结果未知、用户取消和权限失败的操作不得静默重放。
- 工作区外写入、网络写入、进程执行和凭证访问分别判断权限，不能互相替代。
- 工作区文件边界按真实路径而非字符串前缀判断；符号链接、Junction、重解析点和 Glob 不得穿越工作区。

## 3. MCP、Skill 与外部内容

- MCP 和 Skill 的声明权限用于展示、少报阻断和扩权检测，不是绕过核心审批的授权。
- 新增或变化的 MCP 权限清单必须由用户确认；首次使用不能因缺少历史清单而自动批准。
- MCP Resource、Prompt、网页和项目文件都视为不可信内容，不自动变成系统指令或新工具。
- OAuth 使用 PKCE、一次性 state、本地回调和受约束发现；Token、授权码和 Client Secret 不写入普通输出。

## 4. 凭证

- 凭证来源包括环境变量、兼容文件和可选系统凭证库。
- 系统凭证迁移必须经过预检、复制、回读验证、原子切换、独立清理；不得静默删除或静默回退明文。
- Provider 与 MCP OAuth 支持显式迁移、回退、清理、遗忘和注销。
- 配置只保存必要的非秘密引用和 revision。
- Windows 系统凭证后端在 ARM64、企业策略和真实 OAuth 迁移矩阵完成前保持可选。

## 5. 日志、会话与审计

- Key、Token、Client Secret、Cookie、Authorization 和运行时已知秘密必须在截断和持久化前脱敏。
- 不保存模型思维链；只保留用户可见回复和完成任务所需的结构化状态。
- 安全审计账本只记录固定语义事件，不记录命令正文、工具参数、Prompt、模型回复、文件内容或完整路径。
- 审计和诊断默认仅在本机；任何上传都必须单独设计并由用户明确授权。
- 日志损坏或不可写不能改变审批决定，也不能导致安全策略降级。

## 6. 多 Agent 与恢复

- 子 Agent 不能获得高于主任务的权限。
- 子 Agent 的取消和重试属于有副作用的执行操作，不能按只读工具自动批准或静默重放。
- Worktree、用户未提交修改和未验证结果不得静默覆盖或删除。
- 崩溃恢复只能从有程序证据的安全边界继续。
- 任务运行记录保存在用户目录并按不可逆工作区摘要隔离；不得包含完整 Prompt、完整路径、源码、凭证或无界输出。
- 状态为 `unknown` 或已经成功的副作用操作不得在恢复时自动重放；必须先由专用查询、文件/Git/进程检查或用户确认建立新证据。
- 运行记录损坏、版本未知或不可写时必须在新的副作用发生前安全失败。
- 结果未知的外部操作或命令必须先核实或询问用户，禁止自动重放。

## 7. 安全问题发布门禁

- 运行类型检查、完整测试、构建和 npm 包审计。
- 使用不规则 Canary 验证秘密不会进入错误、日志、会话、媒体、MCP 或诊断路径。
- 新权限、新凭证后端和外部写入必须有失败、取消、回退和企业策略场景。
- 测试失败、安全边界不明确或真实验收缺失时，不发布为稳定版本。
