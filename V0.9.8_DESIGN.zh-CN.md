# Xiu v0.9.8 长任务诊断设计

## 目标

- 让用户在任务运行中和结束后看清 Token、模型耗时、工具耗时、审批等待、失败操作与停滞依据。
- 使用程序记录的确定性事件生成诊断，不依赖模型自行总结“为什么慢”。
- 诊断随项目会话保存；关闭终端或恢复历史会话后，仍能查看最近任务报告。
- 只提示风险和建议，不因诊断状态擅自取消任务、跳过审批或改变用户目标。

## 非目标

- 本版本不计算 Provider 费用；不同服务的价格和缓存计费留给模型能力目录阶段。
- 不展示私有思维链，也不把诊断事件注入模型上下文。
- 不实现远程遥测或上传；诊断只保存在工作区现有 `.xiu/sessions/*.jsonl` 中。
- 不把“运行时间较长”直接判定为停滞。模型请求、工具执行和等待审批必须明确区分。

## 权威数据模型

每个主任务建立一份有界诊断快照，包含：

- 任务开始、更新时间、完成时间、结果和当前活动阶段。
- 模型请求尝试数、成功数、失败数、重试数、输入/输出 Token、总耗时和最慢请求。
- 工具调用数、失败数、总耗时、最慢工具，以及按工具名聚合的有界统计。
- 审批请求数、拒绝数和等待时间。
- 压缩次数、最后一次有效进展时间、无新证据操作数和连续失败数。
- 最近 12 条失败摘要；只保存脱敏、限长后的操作和首行错误，不保存完整输出。

快照版本独立校验。旧会话没有诊断字段时正常恢复并显示“暂无诊断”，损坏或越界字段被忽略，不阻断会话。

## 有效进展与停滞

下列事件确定性视为有效进展：

- 首次成功完成一种具体工具输入并获得新证据。
- 工作区发生真实变化。
- 计划发生更新、验证通过或用户 steering 被接收。
- 任务完成。

相同工具和相同输入反复成功不会重复刷新有效进展时间。诊断状态分为：

- `healthy`：正常推进。
- `waiting`：正在等待用户审批，不是停滞。
- `attention`：单次模型/工具明显偏慢、出现失败，或较多操作没有新证据。
- `stalled`：连续三次失败，或至少八次操作且两分钟没有新证据。

`attention` 和 `stalled` 只显示依据与建议。既有循环保护仍按自己的严格规则决定是否阻断，诊断器不调用取消。

## 交互

- 运行期状态栏增加本任务 Token、模型/工具耗时、失败数和健康状态。
- `/diagnostics` 在运行中和空闲时都可用，显示最近任务的结构化报告。
- `/status` 增加最近任务诊断摘要。
- 任务完成回执增加本任务 Token 与失败数，不倾倒完整日志。
- 中文和英文界面由程序确定性本地化；命令、路径、模型名和工具名保持原文。

## 安全与边界

- `api_key`、`token`、`password`、`authorization`、Cookie 和常见 Bearer/`sk-` 凭证模式统一脱敏。
- 操作说明、失败消息、工具聚合和失败历史均限制数量与字符长度。
- 会话中已有完整工具日志仍遵守原安全策略；诊断快照不会复制大段 stdout/stderr。
- 诊断功能为只读展示，不扩大工作区、Plan 模式、审批、危险操作或检查点权限。

## 验收标准

- 模型和工具调用能记录 Token、耗时、失败、重试及最慢操作。
- 慢模型、慢工具和等待审批显示为不同阶段，不被误报为停滞。
- 连续失败和长期无新证据能产生可解释的停滞状态，但不会自动终止任务。
- 失败命令和错误摘要经过脱敏与限长，报告不会包含密钥或完整输出。
- 诊断随会话恢复；旧会话和损坏诊断字段保持兼容。
- 运行期 `/diagnostics`、空闲 `/diagnostics`、`/status` 和完成摘要均可读。
- 全部测试、类型检查、构建、npm 审计、打包和隔离安装通过。
