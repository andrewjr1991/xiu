# Xiu v0.16.4 设计：官方发布元数据核验

## 目标

在 v0.16.3 安装路径诊断的基础上，让 `xiu --update-doctor` 与 `/update doctor` 精确核验当前安装版本在官方 npm Registry 中的发布元数据，帮助用户识别错误包名、版本错配、非官方下载地址和缺失的 SHA-512 完整性声明。

## 用户能力

- 诊断查询 `@xiu-ai/cli` 当前精确版本，而不是只读取 `latest` 标签。
- 成功时显示包名、版本和截断后的 SHA-512 SRI 指纹。
- 明确说明本次只核验 Registry 元数据，没有下载发布包，也没有对本地安装文件计算哈希。
- Registry、DNS 或代理出口暂时不可用时显示外部警告，不影响 Xiu 的普通离线能力。

## 核验规则

- 查询地址固定为官方 `https://registry.npmjs.org/%40xiu-ai%2Fcli/<version>`。
- 返回包名必须精确等于 `@xiu-ai/cli`，版本必须精确等于当前安装版本。
- tarball 必须使用 HTTPS，主机名必须精确等于 `registry.npmjs.org`，且 URL 不得包含用户名或密码。
- `dist.integrity` 必须是可解码为 64 字节摘要的规范 `sha512-...` SRI。
- 若 Registry 同时返回 `dist.shasum`，其格式必须是 40 位十六进制 SHA-1。
- 响应沿用 256 KiB 上限、禁用重定向、500 至 60000 毫秒超时和独立更新代理边界。

## 非目标

- 不下载 tarball，不解压发布包，不比对本地文件内容。
- 不声称本地安装与官方发布包逐字节一致。
- 不读取 npm Token，不运行 npm，不安装、降级、修复或删除任何内容。
- 不改变普通 `xiu` 启动行为；普通启动仍不扫描 PATH，也不发起版本检查网络请求。
- 本版本不实现签名透明日志、Sigstore 或 npm provenance attestation 验证。

## 判定模型

- 当前包文件缺失、Node.js 版本过低或更新代理不安全仍属于本地硬失败。
- Windows 下的 `npm test` 不依赖 shell 通配符展开，并与当前依赖声明一致要求 Node.js 20.18.1 或更高版本。
- 精确版本不存在、Registry 元数据不符合规则或外部网络不可用属于发布元数据警告。
- 发布元数据警告不会令一次性诊断返回非零退出码，因为它不能证明当前本地安装不可运行。
- 未发布的开发版本会自然得到“无法核验当前安装版本的发布元数据”警告；发布后再次运行即可完成核验。

## 验收标准

- 精确版本请求、官方 tarball 主机和规范 SHA-512 SRI 均有确定性测试。
- 错误包名、版本错配、非官方 tarball、无效完整性值和 HTTP 404 均被拒绝。
- 中文界面不会泄漏内置英文诊断文案；外部底层错误仍按原文保留，便于排障。
- 元数据核验失败只产生外部警告，不会被误判为本地硬失败。
- 类型检查、全量测试、构建、发包预览与干净安装验收全部通过。
