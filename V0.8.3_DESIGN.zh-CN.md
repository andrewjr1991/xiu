# Xiu v0.8.3 持久文件变更输出设计

## 问题

v0.8.2 已把阶段说明显示为 `Update:`，并在运行面板显示最近修改，但这两类内容都会随输入框重绘变化。为了避免任务结束时倾倒全部隐藏工具日志，Xiu 又会丢弃运行缓冲，因此用户最终只能依赖模型总结，无法可靠看到实际发生过哪些文件增删改。

## 目标

- 文件修改成功后尽快形成永久终端记录。
- 区分创建、修改和删除，并展示有界的增删统计与关键内容。
- 不让大文件、压缩 HTML、二进制资源或生成媒体刷满终端。
- 不破坏运行期多行输入、草稿恢复、审批菜单、steering 和 `Ctrl+O`。
- 记录来自程序比较，不依赖模型主动说明，也不把失败或无变化操作显示为成功修改。

## 交互

每批修改使用一张紧凑卡片：

```text
File changes
  √ Modified src/example.ts +2 -1
      - const oldValue = 2;
      + const newValue = 3;
      + const enabled = true;
```

普通文本最多显示 6 行预览，每行限制宽度。超过 1 MB 或不能安全作为文本读取的文件只显示修改前后的字节规模。`Update:` 继续承担阶段沟通，状态栏继续显示当前/下一步，`Ctrl+O` 继续展示最近工具活动。

## 数据与渲染

Agent 在已知工作区路径的写操作前后各采集一次有界快照。文本统计通过前后行集合计算实际新增和删除行；创建和删除按文件行数统计。相同内容不产生事件。变化事件同时进入：

1. 状态栏的最近修改摘要；
2. 待永久输出队列；
3. 原有会话工具审计记录。

交互模式收到变化事件后中断当前输入渲染，保留草稿，打印卡片并重新打开输入框。任务结束时再次清空待输出队列，覆盖工具完成和输入循环同时结束的竞态。非交互单任务模式直接打印卡片。

## 安全与边界

- 快照只处理工具参数中已知、且解析后仍位于工作区内的路径。
- 单次最多跟踪 6 个路径，文本详细读取上限为 1 MB。
- 卡片不改变工作区信任、风险审批、Plan 只读、检查点和 dangerous 明确确认。
- 通用 Shell 命令无法可靠声明具体修改路径时，仍依靠最终 Git Diff、模型总结和会话审计；本版本不扫描整个工作区猜测 Shell 副作用。

## 验收标准

- 新建文件显示创建、正确新增行数和最多 6 行预览。
- 修改文件显示实际新增/删除行及关键内容。
- 内容没有变化时不显示卡片。
- 连续修改期间输入框可恢复，未提交草稿不丢失。
- 任务恰好在修改后结束时，变更卡片仍会出现在最终回答之前。
- 类型检查、全部自动化测试、构建、官方 Registry 审计、打包和干净安装全部通过。
