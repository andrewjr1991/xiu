# Xiu v0.11.4：Streamable HTTP MCP

## 目标

在保留现有 stdio MCP 的基础上，接入官方 Streamable HTTP 传输，并提供用户级远程 MCP 的添加、移除、测试和重载闭环。远程工具必须继续使用 Xiu 现有的工作区信任、风险审批、Plan 模式、取消和有界输出机制。

## 配置格式

stdio 配置保持兼容：

```json
{ "command": "npx.cmd", "args": ["-y", "some-server"] }
```

Streamable HTTP 配置使用单一 MCP endpoint：

```json
{
  "transport": "streamable-http",
  "url": "https://example.com/mcp",
  "headers": { "Authorization": "Bearer ${MY_MCP_TOKEN}" },
  "risk": "execute"
}
```

`command` 与 `url` 互斥。远程地址必须是 HTTPS；明文 HTTP 只允许 `localhost`、`127.0.0.1` 或 `[::1]`。Header 值允许环境变量引用，缺失变量会阻止连接；配置、状态和错误不得展开显示凭证。

## 协议实现

- 使用官方 `@modelcontextprotocol/client` 的 Streamable HTTP transport，支持 JSON 与 SSE 响应、会话 ID、协议版本 Header、取消和关闭语义。
- v0.11.4 不实现旧 HTTP+SSE 回退，不把过时传输静默伪装成 Streamable HTTP。
- v0.11.4 不实现交互式 OAuth。Bearer Token 使用环境变量；OAuth 浏览器授权、动态注册和刷新进入后续版本。
- stdio 继续使用已经验证的 Xiu 实现，减少一次版本内同时迁移两种传输的风险。

## 管理闭环

- `/mcp add`：添加用户级 Streamable HTTP Server，可交互输入名称、URL、可选 Bearer 环境变量和风险等级。
- `/mcp remove [name]`：只删除用户级配置；项目级配置必须由项目维护者修改。
- `/mcp test [name]`：重连并显示指定 Server 的实际连接结果。
- `/mcp reload`：继续重载用户级与已信任项目级配置。
- 配置通过同目录临时文件原子替换；写入前完整校验，失败不破坏旧配置。

## 安全边界

- MCP 工具默认 `execute`，不会因为使用 HTTP 而降为只读。
- `read` 只能由用户明确配置；非只读 MCP 工具在 Plan 模式继续被阻止。
- Header、URL 和工具返回均有长度/数量限制；保留现有 60,000 字符输出上限和二进制省略规则。
- 保留的协议 Header（Accept、Content-Type、MCP Session/Version）由官方传输管理，用户配置不能覆盖。
- 添加远程 MCP 是显式用户操作；项目级远程 MCP 仍必须等工作区信任后才连接。

## 验收标准

- 可连接返回 JSON 响应的 Streamable HTTP MCP，发现命名空间工具并完成调用。
- 初始化产生的 Session ID 会用于后续请求，关闭时尝试终止会话。
- AbortSignal 能取消正在运行的远程工具调用。
- Bearer 环境变量正确展开，但不会出现在状态、摘要或错误中。
- 非 HTTPS 远程地址、同时包含 command/url、保留 Header、非法风险或缺失环境变量会被拒绝。
- 用户级 add/remove 使用原子写入；项目配置覆盖与禁用规则保持兼容。
- stdio MCP、审批、Plan 模式、工作区信任和全部既有测试继续通过。
