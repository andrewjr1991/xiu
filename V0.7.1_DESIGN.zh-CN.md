# Xiu v0.7.1 设计：任务运行期输入队列

## 1. 要解决的问题

v0.7.0 的输入编辑器只在 Agent 空闲时工作。任务一旦开始，主循环会等待 `agent.run()` 返回，用户只能观看输出，不能提前补充要求、排队下一步或直接在输入区取消当前任务。

v0.7.1 建立一个最小完整闭环：Agent 运行时继续显示可编辑输入框；新要求按提交顺序排队；当前任务完成后，Xiu 在同一会话上下文中依次执行后续要求。

## 2. 已实现范围

- Agent 运行时显示 `xiu[working]>`，复用 v0.7.0 的多行、Unicode、`@` 路径、历史和草稿编辑能力。
- Enter 提交普通文本时加入当前进程的后续任务队列。
- 队列最多保存 20 项，满载后明确拒绝，不静默丢弃输入。
- `/queue` 查看待执行要求，`/clear-queue` 清空尚未开始的要求。
- `/cancel` 或 `Ctrl+C` 取消当前任务，但保留已排队要求。
- `/exit` 取消当前任务、清空队列并安全退出。
- 运行状态栏持续显示当前阶段和排队数量。
- Agent 流式输出在输入期间进入有界缓冲区，任务完成后统一显示；完整工具结果仍保留在 `/details`。
- 需要审批时，运行期输入框先被挂起并清除，再显示默认拒绝的审批菜单；审批结束后恢复输入。
- 用户尚未提交的运行期草稿继续写入项目 `.xiu/draft.json`，任务完成或审批切换不会丢失。

## 3. 终端所有权

同一时刻只允许一个组件控制 stdin/stdout：

1. 正常输入编辑器控制终端；
2. 任务开始后，运行期编辑器控制终端，Agent 事件写入 `RunningTaskView`；
3. 审批到来时，中止并清理运行期编辑器，刷出已有摘要，再把终端交给审批选择器；
4. 审批完成后重新创建运行期编辑器；
5. 任务结束时中止编辑器、刷出有界输出，再执行下一项队列任务。

这条规则避免输入框、Spinner、流式文本和审批菜单同时写屏导致残影或按键被错误组件消费。

## 4. 安全与失败策略

- 队列只改变任务调度，不改变工具风险等级、工作区信任、Plan 只读边界或 dangerous 审批要求。
- `--yes` 的 dangerous 行为保持不变。
- Ctrl+C 只取消当前任务，不等同于批准、退出或清空队列。
- `/exit` 明确清空尚未执行的进程内队列，防止退出意图之后继续执行修改。
- 队列是当前进程内状态；终端被强制关闭后，已提交给 Agent 的会话可以恢复，但尚未开始的队列项不宣称已持久化。
- 运行期展示缓冲上限为 256,000 字符；超限保留最近输出并显示截断提示，工具完整详情继续由 `/details` 保存。

## 5. 验收标准

- 长任务运行时可以输入并排队至少两个后续要求，完成顺序与提交顺序一致。
- 输入队列时，中文、Emoji、多行和 `@` 补全仍可使用。
- `/queue`、`/clear-queue`、`/cancel` 和 `/exit` 行为可解释且不会误执行普通模型任务。
- 审批出现和结束后无两个 stdin 监听器并存，不留下选择器或输入框残影。
- Agent 完成会主动结束运行期输入等待，不要求用户额外按 Enter。
- 队列溢出、Agent 失败、取消和输出超限都有明确反馈。
- typecheck、全部自动化测试、build、pack 和干净安装冒烟测试通过。

## 6. 后续增量

- 固定任务树、多 Agent 与后台进程面板。
- 交互式 Diff，按文件或区块接受、拒绝。
- 可选的待执行队列持久化及崩溃恢复。
- 拖入文件规范化、主题和中英文 UI。
