# Xiu v0.10.1 模型能力探测设计与验收

## 目标

让 Xiu 不再只依赖用户勾选来判断模型是否支持工具和视觉。文本连接、工具调用与视觉输入分别验证，结果按 Provider ID 与模型 ID 缓存，并以可解释状态展示。

## 探测流程

1. 文本连接会尝试读取模型列表，并且无论列表是否可用，都发送一次最小聊天请求验证当前模型确实能够回复。
2. 工具能力使用名为 `xiu_capability_probe` 的无副作用虚拟工具。先通过 Provider 原生的强制工具选择参数要求模型调用；若推理模型或兼容网关拒绝强制选择，或只返回普通文本，则改用 `tool_choice: auto` 重试。只有 API 返回结构化 `tool_calls` 才记为支持，文本形式的伪工具标签绝不执行。
3. 视觉能力通过媒体适配器发送 Xiu 内置的纯色色块 PNG，并要求模型准确识别像素颜色。只有回答与样本内容一致才记为支持；仅接受图片字段但静默忽略图片的端点不会误报。探测不会读取、上传或引用工作区文件。
4. 网络错误、超时或非确定性响应记为未知；明确拒绝或不返回要求的结构记为不支持。未知和不支持均不会向运行时暴露该能力。

图片和视频生成探测不在本版本执行，因为真实生成可能产生明显费用、等待和外部资产。它们继续由已实现的 Agnes 适配器能力声明控制。

## 缓存与失效

- 缓存保存在 `~/.xiu/providers.json`，不包含请求正文、模型回复或 API Key。
- 键由 Provider ID 与模型 ID 共同确定，同一供应商的不同模型分别记录。
- 缓存记录探测时间与 `text/tools/vision` 状态，最多保存 500 条。
- 如果模型列表接口返回 `context_window`、`context_length`、`max_model_len` 等常见元数据，同时按模型缓存上下文窗口；标准接口不提供时仍使用用户配置。
- 编辑 Provider 的端点、模型或能力声明后清除该 Provider 的旧探测结果。
- Provider 切换复用七天内同版本探测协议的缓存；协议升级会使旧缓存自动过期，`/provider test` 始终强制刷新。
- 最近通过 `/providers` 或 `/models` 选择的 Provider ID 与该 Provider 的模型 ID 一并保存。启动优先级为显式命令行参数、最近交互选择、环境变量、内置默认值，避免历史环境变量永久覆盖菜单选择。

## UI 与运行时

- `/providers` 显示当前 Profile 默认模型的能力状态和来源。
- `/models` 为当前模型显示已探测能力；其他仅在线发现但未探测的模型显示未测试。
- 上下文窗口优先级为命令行显式覆盖、Provider 手动配置、API 模型元数据、官方内置值、保守回退值；切换回执明确显示实际窗口和压缩点。
- Windows Provider/模型选择器使用最大 88 列的保守紧凑视图并截断次要详情；Provider 菜单不再展示 URL 和探测时间，完整信息在切换回执与 `/status` 中显示。这样可规避 PowerShell 把缓冲区宽度误报为可见宽度后触发物理换行、上下键重绘残留标题。
- Windows 原始输入使用代际隔离：旧输入框的延迟清理不能关闭新输入框的 Raw 模式；守护器会恢复被外部提示或控制台改写的输入模式。鼠标仍归终端宿主处理，以保留企业环境依赖的原生右键粘贴。经典 PowerShell 若进入 QuickEdit/选择模式会暂停整个进程，用户可按 Esc/Enter 退出，或在控制台属性中关闭 QuickEdit。
- 探测后的 `tools=false` 会阻止向模型发送工具定义；`vision=false` 会移除图片分析工具。
- 当前图片分析通过工具调用接入，因此“模型视觉支持但工具不支持”在 Xiu 的有效能力中仍显示为仅文本；探测详情保留原始视觉结果供诊断。
- 探测失败不会改变 API Key、会话、计划、检查点、索引或审批策略。

## 安全边界

- 探测工具没有执行函数，不接触文件、Shell、网络工具或 MCP。
- 视觉样本固定内置，不包含用户信息。
- 错误缓存不保存响应正文，避免服务端信息或凭据进入配置。
- 探测请求可能产生极少量 Token；终端和文档必须明确说明。

## 验收标准

- [x] OpenAI-compatible 工具调用支持与拒绝均能正确分类。
- [x] 不支持强制指定工具的推理模型可退回自动选择，文本伪工具调用不会被误执行。
- [x] 视觉成功、拒绝和瞬时失败不会被混淆。
- [x] 接受请求但忽略图片内容的端点不会被误判为支持视觉。
- [x] 同 Provider 不同模型使用独立缓存。
- [x] 编辑 Profile 会使旧缓存失效；Key 不受影响。
- [x] 未验证或未知能力不会向模型暴露对应运行时工具。
- [x] `/providers`、`/models`、`/status` 和 `/provider test` 使用同一份状态。
- [x] API 上下文元数据可识别并按模型保存；手动设置仍可覆盖。
- [x] 长 Provider 描述、URL 和探测时间不会让选择器物理换行或重复残留标题。
- [x] 输入框与选择菜单切换时旧清理不会使新输入退回“按回车才回显”的行缓冲模式。
- [x] Provider 与模型选择可跨进程恢复，旧环境变量不会覆盖较新的交互选择。
- [x] 类型检查、238 项测试和构建通过；打包和全局安装在最终候选验收时执行。
