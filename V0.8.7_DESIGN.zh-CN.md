# Xiu v0.8.7 Windows 输入与取消可靠性修复

## 目标

修复公开版 v0.8.6 真实验收中发现的 Ctrl+C 无法可靠取消当前任务问题，并明确 Windows Terminal 对纯位图右键粘贴的能力边界。

## 交付范围

1. 输入层同时识别 Node 已解析的 `Ctrl+C` 键和 Windows/ConPTY 可能直接送入的原始 `0x03` 控制字符。
2. Ctrl+C 触发后立即把运行阶段切换为“正在取消”，并通过既有 `AbortSignal` 中止模型、Shell、验证、MCP 和媒体请求。
3. 防止同一个控制字符被 keypress 的不同表示重复处理。
4. 保留文字及 Explorer 文件的终端右键粘贴；纯位图继续使用 `Ctrl+V` 或 `/paste`。
5. 不默认开启终端鼠标报告或 Win32 低层输入接管，避免破坏文本选择、右键菜单、滚轮及跨终端兼容性。

## 验收标准

- `{ ctrl: true, name: "c" }` 和原始 `\u0003` 都被识别为取消。
- 当前模型或工具调用收到取消信号，界面立即显示正在取消，任务不会继续运行。
- 普通字母 `c` 不触发取消。
- 文字、文件、`Ctrl+V` 图片和 `/paste` 图片仍正常工作。
- 全量测试、类型检查、构建、npm 打包和隔离安装通过。
