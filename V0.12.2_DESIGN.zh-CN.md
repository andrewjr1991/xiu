# Xiu v0.12.2：MCP / Skill 权限清单

## 目标

让用户在启用 MCP 或安装 Skill 前看清其权限上限、来源与变化；扩展更新新增权限时必须重新确认。权限声明只能展示或收紧能力，不能绕过工作区信任、风险审批、Plan 只读、危险操作确认和恢复点。

## 权限词汇

- `instructions:load`：把 Skill 指令加载到模型上下文。
- `workspace:read` / `workspace:write`：读取或修改当前工作区。
- `process:execute`：启动本地进程。
- `network:access`：访问网络服务。
- `external:read` / `external:write`：读取或修改工作区以外的远端状态。
- `credentials:access`：使用本机环境变量、Bearer Token 或 OAuth 凭证。

未知权限名不静默忽略。MCP 配置中的未知值使配置失效；兼容 Skill 中的未知值会显示警告并在安装时阻断，避免错误地把未知能力当成安全能力。

## MCP 规则

MCP 清单由传输方式、风险、工作区变化、认证方式、OAuth Scope 和显式 `permissions` 合成。显式声明不得少于 Xiu 从配置推导出的最低权限。

旧配置首次运行时建立兼容基线，不打断现有用户；此后风险、认证、Scope、工作区变化或显式权限扩大时，Server 进入 `permission-required`，不会连接或注册工具。用户通过 `/mcp permissions` 查看，通过 `/mcp permissions approve <name>` 明确确认。

`/mcp add` 是显式交互配置流程，保存成功时同步记录当时清单；后续变化仍需重新确认。

## Skill 规则

Skill 可在 `SKILL.md` frontmatter 中使用逗号或空格分隔的 `permissions`。未声明时仅显示默认的 `instructions:load`，不推断它实际要求模型调用哪些工具。

安装或替换 Skill 时先比较已确认清单。新增权限会展示差异并要求确认，拒绝后不修改现有安装；成功复制后才原子记录新清单。Skill 声明不会让其自动获得文件、命令或网络权限，实际工具调用仍走 Xiu 核心审批。

## 安全与存储

确认记录位于用户级 `~/.xiu/extension-permissions.json`，只保存扩展类型、名称、来源的哈希、权限、清单指纹和确认时间，不保存 Token、Prompt 或项目内容。写入采用同目录临时文件后原子替换。

## 验收标准

1. MCP 与 Skill 权限和来源在终端可见。
2. MCP 配置扩大权限后不连接、不注册工具，确认后才恢复。
3. Skill 更新新增权限时先提示；拒绝不会覆盖旧版本。
4. 未知或欠报权限不能被当作安全声明接受。
5. 权限确认不会绕过每次工具风险审批、工作区信任、Plan 模式或 dangerous 确认。
6. Windows stdio MCP 的本地编码 stderr 能输出可读摘要，不倾倒乱码或凭证。
