# Xiu v0.16.1 设计：可选、缓存、非阻塞的更新提醒

## 目标

在不牺牲启动速度、不静默联网、不自动修改全局 npm 的前提下，让主动选择开启提醒的用户及时知道 Xiu 有新版本。

## 用户能力

- `/update notifications on`：显式开启更新提醒。
- `/update notifications off`：关闭更新提醒；这是默认状态。
- `/update status`：查看提醒开关、24 小时缓存及自动行为边界。
- `/update` 与 `xiu --check-update`：继续提供立即、显式的官方版本检查。

## 执行模型

1. 默认关闭提醒，普通 `xiu` 启动不访问网络。
2. 开启后优先读取 `~/.xiu/update-cache.json`。
3. 24 小时内的缓存直接用于判断，不重复访问 Registry。
4. 缓存缺失或过期时，在交互界面已经可用后发起一次有界后台检查。
5. 后台结果不打断输入、任务、审批或选择器，只在下一次安全输入边界显示。
6. 失败保持静默，不阻止启动，也不把 Registry、DNS 或代理错误变成任务失败。

## 缓存与隐私

缓存只保存：

- 固定 schema 版本；
- npm 官方 Registry 地址；
- 官方 `latest` 版本；
- 检查时间。

缓存不保存本地版本、比较状态、代理、凭证、npm Token、项目路径或会话数据。读取时重新使用当前本地版本计算状态；损坏、过期、未来时间或非官方 Registry 缓存均安全忽略。

## 永久边界

- 不自动下载、安装、降级、回滚或执行 npm。
- 不读取 npm 登录凭证，不修改 npm 配置。
- 更新检查代理继续与模型 Provider、联网搜索和 MCP 代理隔离。
- 不在启动关键路径等待网络。
- 关闭提醒后立即取消当前进程待显示的提醒；显式检查仍可使用。

## 验收标准

- 全新配置默认关闭，普通启动零更新网络请求。
- 开启状态持久化，关闭后从配置中移除。
- 新鲜缓存不请求网络，过期缓存只触发一次后台刷新。
- 后台检查失败不退出、不报任务失败、不污染输入。
- 发现新版本时只显示固定官方升级命令，绝不执行。
- 缓存中不含代理、凭证、本地版本或比较状态。
- 中英文命令、状态和提醒一致。
- 类型检查、全量测试、构建和发包预览全部通过。
