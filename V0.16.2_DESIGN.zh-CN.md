# Xiu v0.16.2 设计：分发可靠性与更新诊断

## 目标

为安装、升级和故障反馈提供一个统一、只读、可复制的诊断入口，帮助用户区分“本地安装损坏”和“外部网络暂时不可用”，同时保持 Xiu 的离线可用性与更新安全边界。

## 用户能力

- `xiu --update-doctor`：无需进入交互会话即可运行更新与安装诊断。
- `/update doctor`：在交互会话中运行同一套诊断。
- 诊断输出包含 Node.js 运行时、当前 Xiu 版本、包关键文件、更新代理来源、更新缓存和 npm 官方 Registry 连通性。

## 判定模型

- **通过**：本地运行时和包结构有效，且被检查项目正常。
- **警告**：Registry、DNS、代理或网络不可用，或者缓存不存在/已过期；Xiu 本体仍可离线使用。
- **失败**：Node.js 低于项目声明的最低版本，或发布包关键文件缺失。
- 最终状态取最严重等级；只有本地硬失败令 `xiu --update-doctor` 返回非零退出码。

## 安全与隐私边界

- 只访问固定的 npm 官方 `@xiu-ai/cli/latest` 端点，拒绝重定向并限制响应大小。
- 不运行 npm，不安装、卸载、降级或修复任何内容。
- 不读取或显示 npm Token、登录配置、代理凭证、Provider Key、项目文件或会话内容。
- 代理只显示来源变量和脱敏后的地址；含用户名或密码的代理配置直接判为本地配置失败。
- 普通 `xiu` 启动仍不会因为本功能新增网络请求。

## 包完整性范围

诊断仅核对发布运行所必需且稳定的文件：

- `package.json`
- `dist/cli.js`
- `README.md`
- `USAGE.zh-CN.md`

它不是完整供应链签名验证；更强的包摘要、签名与回滚能力留给后续版本。

## 验收标准

- 两个入口输出同一诊断格式。
- Node.js 20+ 和完整包判定通过；低版本或缺文件判定失败。
- Registry 不可用只显示警告，不阻止 Xiu 离线使用。
- 更新代理来源可见，但不泄露凭证。
- 缓存状态区分无缓存、有效和过期。
- 诊断不执行 npm，不修改全局安装和配置。
- 类型检查、全量测试、构建和发包预览全部通过。
