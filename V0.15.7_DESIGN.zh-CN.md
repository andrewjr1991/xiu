# Xiu v0.15.7 设计：托管搜索部署升级与数据库迁移安全

## 目标

修复真实 VPS 升级中暴露的两类风险：安装器重写数据库路径后启动了空数据库，以及移除全部 Linux capabilities 的服务配置使 root 初始化容器无法递归修正命名卷权限。升级必须保留既有数据选择；发现历史数据库分叉时先停止并提供显式、可预览、可备份的迁移路径。

## 范围

- 已安装服务升级时保留 `auth.env` 中合法的 `XIU_AUTH_DATABASE`；只有首次安装或管理员显式传入 `--database` 时才选择新路径。
- 启动前只读检查命名卷中的 `*.sqlite3`。目标数据库为空而其他数据库含有设备记录时失败关闭，不自动猜测来源或启动空服务。
- `--migrate-database-from` 先输出迁移预览，再备份已有目标数据库并执行幂等迁移；相同设备记录跳过，同一设备 ID 对应不同凭证数据时在写入前拒绝。
- 迁入设备追加 `restored` 审计事件，但不输出设备 ID、密钥摘要、IP、Token 或数据库正文。
- 数据库工具只接受 `/data` 下有界文件名的普通 `.sqlite3` 文件，拒绝符号链接和目录逃逸；匹配的历史库损坏或 schema 不兼容时失败关闭，没有设备表的旧探针库不视为凭证库。
- 命名卷权限由一次性、无网络、只读根文件系统的 helper 初始化；只临时恢复 `CHOWN`、`DAC_OVERRIDE`、`FOWNER`。迁移 helper 限制为 128 MiB 内存和 64 个进程；长期授权服务仍使用 UID/GID `10001`、`cap_drop: ALL` 与 `no-new-privileges`。

## 非目标

- 不自动合并任意旧数据库，不删除旧数据库或迁移备份。
- 不迁移未知表、请求日志、限流内存状态或外部 SearXNG 数据。
- 不新增公网管理接口、浏览器后台或客户端设备撤销命令。
- 不把数据库、设备凭证或管理 Token 下载到开发机或 npm 包样本中。

## CLI 兼容性修复

- 当前信息任务的逐 URL 证据门禁保持不变：最终引用仍必须来自本次成功执行的 `web_open`。
- 最终回答同时包含已打开和未打开来源时，只要至少保留一个已核验来源，就按完整候选结果块移除未打开来源；该裁剪不再依赖页面失败预算是否耗尽。
- 不只删除 URL 而保留失去来源的陈述；无法安全分离候选块、裁剪后没有已核验 URL 或证据仍不足时继续失败关闭。

## 失败与恢复边界

- 迁移预览不写目标数据库；未传 `--apply` 的工具调用永远只读。
- 目标存在时必须先通过 SQLite backup API 生成权限为 `600` 的独立备份，再开始事务写入。
- schema 不兼容、设备凭证冲突、路径越界、符号链接、备份失败或 SQLite 错误均在启动服务前终止。
- 写入事务失败时回滚；备份和源数据库保留，由管理员核对后决定恢复或重试。
- 安装器不会自动重放结果未知的迁移。重新运行时通过相同记录跳过实现幂等，而不是覆盖。

## 验收

- 旧 `XIU_AUTH_DATABASE` 在无显式覆盖时保持不变。
- 空目标加有设备的历史数据库会阻止服务启动，并只显示候选文件名和设备数量。
- dry-run、首次迁移、重复迁移、冲突、路径越界和备份均有确定性测试。
- 在目标 VPS 验证首次安装、原路径升级、显式迁移、容器健康检查和 `/web devices` 数据连续性。
- 覆盖页面失败预算耗尽前的混合结果：已打开条目保留、未打开完整条目移除、任务以较少结果完成；全未核验和无法安全裁剪的回答仍被拒绝。
- 完成 Python 专项测试、Shell 语法检查、TypeScript 全量测试、类型检查、构建和 npm 包清单检查；包内不得包含数据库、备份、`.xiu/` 或凭证。
