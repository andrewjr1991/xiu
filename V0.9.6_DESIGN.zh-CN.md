# Xiu v0.9.6 增量项目索引设计

## 目标

- Xiu 再次进入未变化项目时，不重复读取和分词全部源文件。
- 新增、修改和删除文件只更新受影响的索引项，搜索结果立即反映当前工作区。
- 索引刷新过程可解释，能够报告全量构建、缓存复用、增量更新和耗时。
- 缓存损坏、版本不兼容或包含越界路径时自动重建，不影响项目源文件。

## 非目标

- 本版本不实现 AST、符号引用、调用关系和 Repository Map；这些建立在可靠的增量文件索引之上。
- 不使用常驻文件监听器，也不要求后台进程持续运行。
- 不把完整文件内容写入缓存，仍只保存有界检索词项和文件元数据。

## 缓存与刷新契约

索引继续保存在工作区 `.xiu/index.json`。缓存版本升级后，旧缓存只会被忽略和重建，不会原地迁移。

每个已索引文件保存规范化相对路径、字节数、修改时间和最多 300 个词项。刷新时先重新枚举允许的文件并读取元数据：

1. 路径、字节数和修改时间均未变化：直接复用缓存词项，不读取文件内容。
2. 新增或元数据变化：重新读取并分词该文件。
3. 已删除：从新索引移除。
4. 强制刷新、缓存损坏或缓存不可信：执行完整重建。

一次进程内若索引未被标记为脏，重复搜索不再扫描文件系统。Xiu 工具成功修改工作区、恢复检查点或集成 Agent 补丁后会标记索引为脏，下一次检索前执行增量刷新。

## 安全与可靠性

- 继续忽略 `.git`、依赖、构建产物、`.xiu`、密钥和环境文件。
- 不跟随符号链接，缓存路径必须是工作区内的规范化相对路径。
- 缓存字段、文件数量、词项数量与词项长度全部有界；任何结构异常都会触发重建。
- 文件读取失败时保留路径词项，不阻断整个索引。
- 新缓存先写入同目录临时文件再重命名；写缓存失败不破坏已建立的内存索引。
- 索引仍是只读检索数据，不改变工作区信任、审批和 Plan 模式规则。

## 可观测性

`/status` 的索引区域展示文件数、最近刷新模式和刷新耗时。程序状态额外包含：发现、复用、新增、更新、删除和实际重读文件数，供测试和后续诊断使用。

刷新模式：

- `full`：没有可信缓存或用户强制重建。
- `incremental`：检测到变化并只重建受影响项。
- `cache`：工作区未变化，全部复用。

## 验收标准

- 第二个 Xiu 实例打开未变化项目时，所有索引项均复用，实际内容重读数为 0。
- 单独修改一个文件时只重读一个文件；新增和删除能正确反映在路径与搜索结果中。
- 同一进程被工作区变更标记为脏后，下一次搜索自动刷新。
- 损坏、旧版本或伪造越界路径的缓存不会被使用，并能自动恢复为有效缓存。
- 中文、无扩展名、大文件、忽略目录和 8000 文件上限保持原有行为。
- 全部自动化测试、类型检查、构建、npm 审计、打包预览和隔离安装通过。
