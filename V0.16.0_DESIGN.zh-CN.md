# Xiu v0.16.0 设计：稳定分发与升级诊断

## 目标

让用户能够明确、快速地判断当前安装是否需要升级，并得到可直接复制的官方更新命令，同时不牺牲普通启动速度，不进行静默联网或自动修改全局 npm 安装。

## 范围

- 新增交互命令 `/update`，显式查询 npm 官方 Registry 中 `@xiu-ai/cli` 的 `latest` 版本。
- 新增一次性命令 `xiu --check-update`，无需进入交互界面即可检查版本。
- 显示本地版本、官方最新版本和“已是最新 / 可更新 / 本地版本更新”状态。
- 有更新时给出固定的官方 Registry 更新命令，但不替用户执行。
- 支持独立的更新检查代理 `XIU_UPDATE_PROXY`，并兼容 npm 与标准 HTTPS 代理环境变量。
- 命令面板、README、使用说明、发布说明和路线图同步说明该能力。

## 永久边界

- 普通 `xiu` 启动不访问 npm Registry，不增加启动延迟。
- 不自动下载、安装、覆盖、降级或回滚 Xiu。
- 不读取 npm 登录 Token，不调用私有镜像，不修改 npm 配置。
- 更新检查固定访问 `https://registry.npmjs.org/%40xiu-ai%2Fcli/latest`，拒绝重定向。
- 响应体、超时和版本字段均有界；异常响应失败关闭并给出可理解的错误。
- 更新代理与模型 Provider、联网搜索、MCP 代理隔离，不继承 `XIU_WEB_PROXY` 或 Provider 代理。
- 不修改项目文件、用户凭证、VPS、MCP、Skills 或插件状态。

## 版本判断

- 使用语义版本比较，包含预发布版本。
- 本地版本低于 Registry：显示可更新及官方命令。
- 本地版本等于 Registry：显示已是最新。
- 本地版本高于 Registry：显示本地为候选或尚未发布版本，不建议降级。
- Registry 返回无效版本时不猜测、不执行更新。

## 验收

- `/update` 与 `xiu --check-update` 输出一致且支持中英文。
- 本地候选版高于 Registry 时正确显示“本地版本更新”。
- 模拟低版本时正确显示更新命令，但测试可证明没有启动任何安装进程。
- 重定向、超时、超大响应、非 2xx、无效 JSON 和无效版本均被拒绝。
- 未配置更新代理时不继承联网搜索或模型代理；显式更新代理能够生效并被及时释放。
- 普通启动不触发更新请求。
- 类型检查、全量测试、构建和 `npm pack --dry-run` 全部通过，包内不包含 `.xiu/` 或本机状态。
